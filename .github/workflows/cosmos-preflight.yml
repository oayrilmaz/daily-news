

name: Cosmos Preflight (No OpenAI)

on:
  workflow_dispatch:
    inputs:
      write_emergence:
        description: "Generate and commit real emergence-current.json from current committed Cosmos data"
        required: false
        default: false
        type: boolean
      write_cosmos_state:
        description: "Generate and commit real cosmos-state-current.json from current committed Cosmos data"
        required: false
        default: false
        type: boolean
      write_cosmos_core_test:
        description: "Generate and commit controlled Cosmos Core v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_attention_test:
        description: "Generate and commit controlled Cosmos Attention v0.2 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_consequence_test:
        description: "Generate and commit controlled Cosmos Consequence v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_discovery_test:
        description: "Generate and commit controlled Cosmos Discovery v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_acquisition_test:
        description: "Generate and commit controlled Cosmos Acquisition v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_applicability_test:
        description: "Generate and commit controlled Cosmos Acquisition Applicability v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_decomposition_test:
        description: "Generate and commit controlled Cosmos Decomposition v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_candidate_validation_test:
        description: "Generate and commit controlled Cosmos Candidate Validation Planner v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_evidence_strategy_test:
        description: "Generate and commit controlled Cosmos Evidence Strategy v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_evidence_executor_test:
        description: "Generate and commit controlled Cosmos Evidence Executor v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_evidence_validator_test:
        description: "Generate and commit controlled Cosmos Evidence Validator v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_knowledge_admission_test:
        description: "Generate and commit controlled Cosmos Knowledge Admission v0.1 test output"
        required: false
        default: false
        type: boolean
      write_cosmos_graph_writer_test:
        description: "Generate and commit controlled Cosmos Graph Writer v0.1 snapshot and report"
        required: false
        default: false
        type: boolean
      write_cosmos_graph_integrity_test:
        description: "Generate and commit controlled Cosmos Graph Integrity v0.1 report"
        required: false
        default: false
        type: boolean
      write_cosmos_graph_reversal_test:
        description: "Generate and commit controlled Cosmos Graph Reversal v0.1 snapshot, report and post-reversal integrity report"
        required: false
        default: false
        type: boolean
      write_cosmos_causal_narrative_test:
        description: "Generate and commit controlled Cosmos Causal Narrative v0.1 output"
        required: false
        default: false
        type: boolean
      write_cosmos_graph_evolution_test:
        description: "Generate and commit controlled Cosmos Graph Evolution v0.1 planning output"
        required: false
        default: false
        type: boolean
      write_cosmos_temporal_knowledge_test:
        description: "Generate and commit controlled Cosmos Temporal Knowledge v0.1 output"
        required: false
        default: false
        type: boolean
      write_cosmos_first_party_identity_test:
        description: "Generate and commit controlled Cosmos First-Party Identity v0.1 output"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: cosmos-preflight-ptdtoday
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync With Latest Branch Tip
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin "${GITHUB_REF_NAME}"
          git checkout "${GITHUB_REF_NAME}"
          git reset --hard "origin/${GITHUB_REF_NAME}"
          echo "Workspace synchronized to origin/${GITHUB_REF_NAME}."

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify Cosmos Source Files
        shell: bash
        run: |
          set -euo pipefail

          REQUIRED_FILES=(
            "scripts/generate_ai_news.js"
            "scripts/resolve_entities.js"
            "scripts/update_entity_lifecycle.js"
            "scripts/normalize_map_signals.cjs"
            "scripts/validate_intelligence.cjs"
            "scripts/cosmos_state_delta.cjs"
            "scripts/cosmos_impact_engine.cjs"
            "scripts/cosmos_pattern_engine.cjs"
            "scripts/cosmos_emergence_engine.cjs"
            "scripts/cosmos_state_builder.cjs"
            "scripts/cosmos_core.cjs"
            "scripts/cosmos_attention.cjs"
            "scripts/cosmos_consequence.cjs"
            "scripts/cosmos_discovery.cjs"
            "scripts/cosmos_acquisition.cjs"
            "scripts/cosmos_acquisition_applicability.cjs"
            "scripts/cosmos_decomposition.cjs"
            "scripts/cosmos_candidate_validation.cjs"
            "scripts/cosmos_evidence_strategy.cjs"
            "scripts/cosmos_evidence_executor.cjs"
            "scripts/cosmos_evidence_validator.cjs"
            "scripts/cosmos_knowledge_admission.cjs"
            "scripts/cosmos_graph_writer.cjs"
            "scripts/cosmos_graph_integrity.cjs"
            "scripts/cosmos_graph_reversal.cjs"
            "scripts/cosmos_causal_narrative.cjs"
            "scripts/cosmos_graph_evolution.cjs"
            "scripts/cosmos_temporal_knowledge.cjs"
            "scripts/cosmos_first_party_identity.cjs"
            "scripts/backfill_history_from_articles.js"
            "package.json"
          )

          for file in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$file" ]; then
              echo "ERROR: Missing required file: $file"
              exit 1
            fi
            echo "OK: $file"
          done

      - name: Check JavaScript Syntax
        run: |
          set -e
          node --check scripts/generate_ai_news.js
          node --check scripts/resolve_entities.js
          node --check scripts/update_entity_lifecycle.js
          node --check scripts/normalize_map_signals.cjs
          node --check scripts/validate_intelligence.cjs
          node --check scripts/cosmos_state_delta.cjs
          node --check scripts/cosmos_impact_engine.cjs
          node --check scripts/cosmos_pattern_engine.cjs
          node --check scripts/cosmos_emergence_engine.cjs
          node --check scripts/cosmos_state_builder.cjs
          node --check scripts/cosmos_core.cjs
          node --check scripts/cosmos_attention.cjs
          node --check scripts/cosmos_consequence.cjs
          node --check scripts/cosmos_discovery.cjs
          node --check scripts/cosmos_acquisition.cjs
          node --check scripts/cosmos_acquisition_applicability.cjs
          node --check scripts/cosmos_decomposition.cjs
          node --check scripts/cosmos_candidate_validation.cjs
          node --check scripts/cosmos_evidence_strategy.cjs
          node --check scripts/cosmos_evidence_executor.cjs
          node --check scripts/cosmos_evidence_validator.cjs
          node --check scripts/cosmos_knowledge_admission.cjs
          node --check scripts/cosmos_graph_writer.cjs
          node --check scripts/cosmos_graph_integrity.cjs
          node --check scripts/cosmos_graph_reversal.cjs
          node --check scripts/cosmos_causal_narrative.cjs
          node --check scripts/cosmos_graph_evolution.cjs
          node --check scripts/cosmos_temporal_knowledge.cjs
          node --check scripts/cosmos_first_party_identity.cjs
          node --check scripts/backfill_history_from_articles.js
          echo "All Cosmos JavaScript files passed syntax validation."

      - name: Verify package.json
        shell: bash
        run: |
          node <<'NODE'
          const fs = require("fs");

          const pkg = JSON.parse(
            fs.readFileSync("package.json", "utf8")
          );

          if (!pkg.scripts || !pkg.scripts["generate:ai"]) {
            throw new Error(
              'package.json is missing scripts["generate:ai"].'
            );
          }

          if (!pkg.dependencies || !pkg.dependencies.openai) {
            throw new Error(
              'package.json is missing the "openai" dependency.'
            );
          }

          console.log("generate:ai:", pkg.scripts["generate:ai"]);
          console.log("OpenAI dependency:", pkg.dependencies.openai);
          console.log("package.json validation passed.");
          NODE

      - name: Validate Existing JSON Files
        shell: bash
        run: |
          node <<'NODE'
          const fs = require("fs");

          const required = [
            "briefs/daily-ai.json",
            "briefs/map-signals.json",
            "briefs/trends.json",
            "briefs/outlook.json",
            "knowledge/developments.json",
            "knowledge/entities.json",
            "knowledge/relationships.json",
            "knowledge/timeline-events.json",
            "knowledge/entity-lifecycle.json"
          ];

          let failed = false;

          for (const file of required) {
            if (!fs.existsSync(file)) {
              console.error(`ERROR: Missing ${file}`);
              failed = true;
              continue;
            }

            try {
              JSON.parse(fs.readFileSync(file, "utf8"));
              console.log(`OK JSON: ${file}`);
            } catch (error) {
              console.error(
                `ERROR: Invalid JSON in ${file}: ${error.message}`
              );
              failed = true;
            }
          }

          if (failed) process.exit(1);

          console.log(
            "All currently committed critical JSON files parse correctly."
          );
          NODE

      - name: Verify Current Knowledge Structure
        shell: bash
        run: |
          node <<'NODE'
          const fs = require("fs");

          function read(file) {
            return JSON.parse(fs.readFileSync(file, "utf8"));
          }

          const daily = read("briefs/daily-ai.json");
          const map = read("briefs/map-signals.json");
          const developments = read("knowledge/developments.json");
          const entities = read("knowledge/entities.json");
          const relationships = read("knowledge/relationships.json");
          const timeline = read("knowledge/timeline-events.json");
          const lifecycle = read("knowledge/entity-lifecycle.json");

          const errors = [];

          if (!Array.isArray(daily.items)) {
            errors.push("briefs/daily-ai.json: items[] missing");
          }

          if (!Array.isArray(map.signals)) {
            errors.push("briefs/map-signals.json: signals[] missing");
          }

          if (!Array.isArray(developments.developments)) {
            errors.push(
              "knowledge/developments.json: developments[] missing"
            );
          }

          if (!Array.isArray(entities.entities)) {
            errors.push("knowledge/entities.json: entities[] missing");
          }

          if (!Array.isArray(relationships.relationships)) {
            errors.push(
              "knowledge/relationships.json: relationships[] missing"
            );
          }

          if (!Array.isArray(timeline.events)) {
            errors.push(
              "knowledge/timeline-events.json: events[] missing"
            );
          }

          if (
            !lifecycle ||
            typeof lifecycle !== "object" ||
            !lifecycle.rankings
          ) {
            errors.push(
              "knowledge/entity-lifecycle.json: lifecycle structure missing"
            );
          }

          if (errors.length) {
            console.error("\nCompatibility errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("\nCurrent committed data structure:");
          console.log(`Daily items:      ${daily.items.length}`);
          console.log(`Map signals:      ${map.signals.length}`);
          console.log(`Developments:     ${developments.developments.length}`);
          console.log(`Entities:         ${entities.entities.length}`);
          console.log(`Relationships:    ${relationships.relationships.length}`);
          console.log(`Timeline events:  ${timeline.events.length}`);

          if (Array.isArray(lifecycle.entities)) {
            console.log(`Lifecycle registry: ${lifecycle.entities.length}`);
          } else {
            console.log(
              "Lifecycle registry: legacy format currently committed " +
              "(File 5 will create lifecycle.entities[] on the next production run)"
            );
          }

          console.log("\nCosmos source/data compatibility checks passed.");
          NODE

      - name: Test Cosmos Impact Engine In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_impact_engine.cjs "$TEST_DIR/scripts/"
          cp scripts/cosmos_pattern_engine.cjs "$TEST_DIR/scripts/"
          cp scripts/cosmos_emergence_engine.cjs "$TEST_DIR/scripts/"
          cp knowledge/relationships.json "$TEST_DIR/knowledge/"
          cp knowledge/developments.json "$TEST_DIR/knowledge/"

          if [ -f "knowledge/cosmos/state-current.json" ] && \
             [ -f "knowledge/cosmos/delta-current.json" ]; then
            cp knowledge/cosmos/state-current.json \
              "$TEST_DIR/knowledge/cosmos/state-current.json"
            cp knowledge/cosmos/delta-current.json \
              "$TEST_DIR/knowledge/cosmos/delta-current.json"
          else
            echo "Committed Cosmos state/delta not found."
            echo "Creating a minimal deterministic fixture for the impact-engine test."

            node - "$TEST_DIR" <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.argv[2];
          const relationships = JSON.parse(
            fs.readFileSync(
              path.join(root, "knowledge/relationships.json"),
              "utf8"
            )
          );

          const usable = (relationships.relationships || []).find((rel) => {
            const type = String(rel.relationship_type || "").toUpperCase();
            return (
              rel.from_entity_id &&
              rel.to_entity_id &&
              rel.from_entity_id !== rel.to_entity_id &&
              type !== "LOCATED_IN" &&
              Number(rel.strength || 0) >= 65 &&
              Number(rel.confidence || 0) >= 0.65
            );
          });

          if (!usable) {
            throw new Error(
              "No relationship is strong enough to build the preflight fixture."
            );
          }

          const ids = [usable.from_entity_id, usable.to_entity_id];

          const state = {
            schema_version: "preflight-fixture",
            generated_at: new Date().toISOString(),
            date_utc: "2099-01-01",
            entities: Object.fromEntries(
              ids.map((id, index) => [
                id,
                {
                  entity_id: id,
                  name: `Preflight Entity ${index + 1}`,
                  type: "Concept",
                  lifecycle_status: "active",
                  importance_score: 80,
                  momentum_score: null,
                  momentum_status: "insufficient_history",
                  last_seen_at: new Date().toISOString(),
                  relationship_degree: 1,
                  linked_development_count: 1
                }
              ])
            )
          };

          const delta = {
            schema_version: "preflight-fixture",
            generated_at: new Date().toISOString(),
            date_utc: "2099-01-01",
            cold_start: false,
            added_entities: [
              {
                entity_id: usable.from_entity_id,
                name: "Preflight Entity 1",
                type: "Concept"
              }
            ],
            removed_entities: [],
            changed_entities: []
          };

          fs.writeFileSync(
            path.join(root, "knowledge/cosmos/state-current.json"),
            JSON.stringify(state, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "knowledge/cosmos/delta-current.json"),
            JSON.stringify(delta, null, 2)
          );
          NODE
          fi

          (
            cd "$TEST_DIR"
            node scripts/cosmos_impact_engine.cjs
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const outputPath = path.join(
            root,
            "knowledge/cosmos/impact-current.json"
          );

          if (!fs.existsSync(outputPath)) {
            throw new Error(
              "Impact engine did not create knowledge/cosmos/impact-current.json"
            );
          }

          const impact = JSON.parse(
            fs.readFileSync(outputPath, "utf8")
          );

          const errors = [];

          if (impact.schema_version !== "0.2") {
            errors.push(
              `Unexpected impact schema_version: ${impact.schema_version}`
            );
          }

          if (!impact.seeds || !Array.isArray(impact.seeds.items)) {
            errors.push("impact-current.json: seeds.items[] missing");
          }

          if (!Array.isArray(impact.seeds?.observed_deltas)) {
            errors.push("impact-current.json: seeds.observed_deltas[] missing");
          }

          if (!Number.isInteger(impact.seeds?.observed_delta_count) ||
              impact.seeds.observed_delta_count < 0) {
            errors.push("impact-current.json: invalid seeds.observed_delta_count");
          }

          if (!Number.isInteger(impact.seeds?.evidence_indexed_entities) ||
              impact.seeds.evidence_indexed_entities < 0) {
            errors.push("impact-current.json: invalid seeds.evidence_indexed_entities");
          }

          for (const seed of impact.seeds?.items || []) {
            if (seed.propagation_eligible !== true) {
              errors.push(`Seed ${seed.entity_id || "(unknown)"} is not propagation eligible`);
            }
            if (!Array.isArray(seed.evidence_development_ids)) {
              errors.push(`Seed ${seed.entity_id || "(unknown)"} is missing evidence_development_ids[]`);
            }
            if (!["strong", "medium", "baseline"].includes(seed.significance)) {
              errors.push(`Seed ${seed.entity_id || "(unknown)"} has invalid significance`);
            }
          }

          for (const seed of impact.seeds?.observed_deltas || []) {
            if (seed.propagation_eligible !== false) {
              errors.push(`Observed delta ${seed.entity_id || "(unknown)"} should not propagate`);
            }
            if (seed.significance !== "contextual") {
              errors.push(`Observed delta ${seed.entity_id || "(unknown)"} should be contextual`);
            }
          }

          if (!impact.summary || typeof impact.summary !== "object") {
            errors.push("impact-current.json: summary missing");
          }

          if (!Array.isArray(impact.impacts)) {
            errors.push("impact-current.json: impacts[] missing");
          }

          if (
            !["ready", "no_significant_seed", "no_paths_above_threshold"].includes(
              impact.status
            )
          ) {
            errors.push(
              `impact-current.json: invalid status "${impact.status}"`
            );
          }

          for (const row of impact.impacts || []) {
            if (row.reasoning_mode !== "graph_propagation") {
              errors.push(
                `Impact ${row.impact_id || "(unknown)"} has invalid reasoning_mode`
              );
            }

            if (!Array.isArray(row.seed_evidence_development_ids)) {
              errors.push(
                `Impact ${row.impact_id || "(unknown)"} is missing seed evidence lineage`
              );
            }

            if (!["strong", "medium", "baseline"].includes(row.seed_significance)) {
              errors.push(
                `Impact ${row.impact_id || "(unknown)"} has invalid seed significance`
              );
            }

            if (
              !row.path ||
              !Array.isArray(row.path.entity_ids) ||
              !Array.isArray(row.path.steps) ||
              row.path.steps.length !== row.propagation_depth
            ) {
              errors.push(
                `Impact ${row.impact_id || "(unknown)"} has an invalid path`
              );
            }

            if (
              !Number.isInteger(row.propagation_depth) ||
              row.propagation_depth < 1 ||
              row.propagation_depth > 3
            ) {
              errors.push(
                `Impact ${row.impact_id || "(unknown)"} has invalid propagation depth`
              );
            }
          }

          if (errors.length) {
            console.error("\nImpact Engine preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("\nImpact Engine isolated test passed.");
          console.log(`Status:  ${impact.status}`);
          console.log(`Propagating seeds: ${impact.seeds.count}`);
          console.log(`Contextual deltas: ${impact.seeds.observed_delta_count}`);
          console.log(
            `Seeds with direct evidence: ${impact.summary.seeds_with_direct_development_evidence}`
          );
          console.log(`Impacts: ${impact.summary.impact_count}`);
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

          (
            cd "$TEST_DIR"
            node scripts/cosmos_pattern_engine.cjs
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const outputPath = path.join(
            root,
            "knowledge/cosmos/patterns-current.json"
          );

          if (!fs.existsSync(outputPath)) {
            throw new Error(
              "Pattern engine did not create knowledge/cosmos/patterns-current.json"
            );
          }

          const patterns = JSON.parse(
            fs.readFileSync(outputPath, "utf8")
          );

          const errors = [];

          if (patterns.schema_version !== "0.2") {
            errors.push(
              `Unexpected pattern schema_version: ${patterns.schema_version}`
            );
          }

          if (
            !["ready", "no_patterns_above_threshold"].includes(
              patterns.status
            )
          ) {
            errors.push(
              `patterns-current.json: invalid status "${patterns.status}"`
            );
          }

          if (!patterns.methodology ||
              patterns.methodology.reasoning_mode !== "deterministic_pattern_detection") {
            errors.push(
              "patterns-current.json: deterministic_pattern_detection methodology missing"
            );
          }

          const calibration = patterns.methodology?.cross_domain_calibration;

          if (!calibration || typeof calibration !== "object") {
            errors.push(
              "patterns-current.json: cross_domain_calibration methodology missing"
            );
          } else {
            if (Number(calibration.max_results) !== 30) {
              errors.push(
                `patterns-current.json: expected cross-domain max_results 30, got ${calibration.max_results}`
              );
            }

            if (Number(calibration.max_score) !== 96) {
              errors.push(
                `patterns-current.json: expected cross-domain max_score 96, got ${calibration.max_score}`
              );
            }

            if (!String(calibration.dynamic_gate || "").includes("meaningful delta")) {
              errors.push(
                "patterns-current.json: calibrated dynamic gate missing"
              );
            }
          }

          if (!patterns.summary || typeof patterns.summary !== "object") {
            errors.push("patterns-current.json: summary missing");
          }

          if (!Array.isArray(patterns.patterns)) {
            errors.push("patterns-current.json: patterns[] missing");
          }

          if (!patterns.diagnostics || typeof patterns.diagnostics !== "object") {
            errors.push("patterns-current.json: diagnostics missing");
          }

          const allowedFamilies = new Set([
            "impact_convergence",
            "evidence_reinforcement",
            "structural_acceleration",
            "cross_domain_coupling"
          ]);

          const allowedEvidenceQuality = new Set([
            "strong",
            "moderate",
            "weak",
            "unknown"
          ]);

          const allowedPersistence = new Set([
            "insufficient_history",
            "new",
            "recurring",
            "persistent"
          ]);

          let crossDomainCount = 0;

          for (const row of patterns.patterns || []) {
            if (!row.pattern_id) {
              errors.push("Pattern missing pattern_id");
            }

            if (!allowedFamilies.has(row.pattern_family)) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid pattern_family`
              );
            }

            if (!Number.isFinite(Number(row.pattern_score)) ||
                Number(row.pattern_score) < 0 ||
                Number(row.pattern_score) > 100) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid pattern_score`
              );
            }

            if (!Number.isFinite(Number(row.structural_strength_score)) ||
                Number(row.structural_strength_score) < 0 ||
                Number(row.structural_strength_score) > 100) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid structural_strength_score`
              );
            }

            if (!Number.isFinite(Number(row.evidence_quality_score)) ||
                Number(row.evidence_quality_score) < 0 ||
                Number(row.evidence_quality_score) > 100) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid evidence_quality_score`
              );
            }

            if (!allowedEvidenceQuality.has(row.evidence_quality_label)) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid evidence_quality_label`
              );
            }

            if (!row.evidence ||
                !Array.isArray(row.evidence.development_ids)) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} is missing evidence lineage`
              );
            }

            if (!row.persistence ||
                !allowedPersistence.has(row.persistence.status)) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid persistence status`
              );
            }

            if (!Array.isArray(row.supporting_entities) ||
                !Array.isArray(row.supporting_impact_ids) ||
                !Array.isArray(row.supporting_relationship_ids)) {
              errors.push(
                `Pattern ${row.pattern_id || "(unknown)"} has invalid support lineage`
              );
            }

            if (row.pattern_family === "cross_domain_coupling") {
              crossDomainCount += 1;

              if (Number(row.pattern_score) > 96) {
                errors.push(
                  `Pattern ${row.pattern_id || "(unknown)"} exceeds calibrated cross-domain score ceiling`
                );
              }

              const details = Array.isArray(row.signal_details)
                ? row.signal_details[0]
                : null;

              if (!details ||
                  !Number.isFinite(Number(details.strong_relationship_count)) ||
                  Number(details.strong_relationship_count) < 1) {
                errors.push(
                  `Pattern ${row.pattern_id || "(unknown)"} lacks strong resolved relationship support`
                );
              }

              const hasDynamicSupport =
                details?.meaningful_delta_support === true ||
                Number(details?.origin_impact_count || 0) > 0 ||
                Number(details?.affected_impact_count || 0) > 0;

              if (!hasDynamicSupport) {
                errors.push(
                  `Pattern ${row.pattern_id || "(unknown)"} lacks calibrated dynamic support`
                );
              }
            }
          }

          if (crossDomainCount > 30) {
            errors.push(
              `patterns-current.json: cross-domain pattern count ${crossDomainCount} exceeds calibrated cap 30`
            );
          }

          if (errors.length) {
            console.error("\nPattern Engine preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("\nPattern Engine isolated test passed.");
          console.log(`Status:   ${patterns.status}`);
          console.log(`Patterns: ${patterns.summary.pattern_count}`);
          console.log(
            `Convergence: ${patterns.summary.by_family?.impact_convergence || 0}`
          );
          console.log(
            `Reinforcement: ${patterns.summary.by_family?.evidence_reinforcement || 0}`
          );
          console.log(
            `Acceleration: ${patterns.summary.by_family?.structural_acceleration || 0}`
          );
          console.log(
            `Cross-domain: ${patterns.summary.by_family?.cross_domain_coupling || 0}`
          );
          console.log(
            "Pattern structural strength and evidence quality remain independently validated."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Emergence Engine In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_emergence_engine.cjs "$TEST_DIR/scripts/"
          cp knowledge/cosmos/patterns-current.json \
            "$TEST_DIR/knowledge/cosmos/patterns-current.json"

          if [ -f "knowledge/cosmos/impact-current.json" ]; then
            cp knowledge/cosmos/impact-current.json \
              "$TEST_DIR/knowledge/cosmos/impact-current.json"
          fi

          if [ -f "knowledge/cosmos/delta-current.json" ]; then
            cp knowledge/cosmos/delta-current.json \
              "$TEST_DIR/knowledge/cosmos/delta-current.json"
          fi

          if [ -f "knowledge/cosmos/state-current.json" ]; then
            cp knowledge/cosmos/state-current.json \
              "$TEST_DIR/knowledge/cosmos/state-current.json"
          fi

          (
            cd "$TEST_DIR"
            node scripts/cosmos_emergence_engine.cjs
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const outputPath = path.join(
            root,
            "knowledge/cosmos/emergence-current.json"
          );
          const patternPath = path.join(
            root,
            "knowledge/cosmos/patterns-current.json"
          );

          if (!fs.existsSync(outputPath)) {
            throw new Error(
              "Emergence engine did not create knowledge/cosmos/emergence-current.json"
            );
          }

          const emergence = JSON.parse(
            fs.readFileSync(outputPath, "utf8")
          );
          const patterns = JSON.parse(
            fs.readFileSync(patternPath, "utf8")
          );

          const patternById = new Map(
            (patterns.patterns || []).map(row => [row.pattern_id, row])
          );

          const errors = [];

          if (emergence.schema_version !== "0.1") {
            errors.push(
              `Unexpected emergence schema_version: ${emergence.schema_version}`
            );
          }

          if (
            !["ready", "no_emergence_above_threshold"].includes(
              emergence.status
            )
          ) {
            errors.push(
              `emergence-current.json: invalid status "${emergence.status}"`
            );
          }

          if (!emergence.methodology ||
              emergence.methodology.reasoning_mode !==
                "deterministic_higher_order_pattern_synthesis") {
            errors.push(
              "emergence-current.json: deterministic higher-order methodology missing"
            );
          }

          if (!emergence.summary || typeof emergence.summary !== "object") {
            errors.push("emergence-current.json: summary missing");
          }

          if (!emergence.diagnostics ||
              typeof emergence.diagnostics !== "object") {
            errors.push("emergence-current.json: diagnostics missing");
          }

          if (!Array.isArray(emergence.emergences)) {
            errors.push("emergence-current.json: emergences[] missing");
          }

          const allowedFamilies = new Set([
            "acceleration_convergence",
            "reinforced_coupling",
            "multi_family_emergence",
            "systemic_emergence",
            "multi_pattern_convergence"
          ]);

          const allowedEvidenceQuality = new Set([
            "strong",
            "moderate",
            "weak",
            "unknown"
          ]);

          const allowedPersistence = new Set([
            "insufficient_history",
            "new",
            "recurring",
            "persistent"
          ]);

          for (const row of emergence.emergences || []) {
            if (!row.emergence_id) {
              errors.push("Emergence missing emergence_id");
            }

            if (!allowedFamilies.has(row.emergence_family)) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid emergence_family`
              );
            }

            if (!Number.isFinite(Number(row.emergence_score)) ||
                Number(row.emergence_score) < 0 ||
                Number(row.emergence_score) > 98) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid emergence_score`
              );
            }

            if (!Array.isArray(row.supporting_pattern_ids) ||
                row.supporting_pattern_ids.length < 3) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} must have at least 3 supporting patterns`
              );
            }

            if (!Array.isArray(row.pattern_families) ||
                row.pattern_families.length < 2) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} must have at least 2 pattern families`
              );
            }

            const distinctFamilies = new Set(row.pattern_families || []);
            if (distinctFamilies.size !== (row.pattern_families || []).length) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has duplicate pattern families`
              );
            }

            for (const patternId of row.supporting_pattern_ids || []) {
              if (!patternById.has(patternId)) {
                errors.push(
                  `Emergence ${row.emergence_id || "(unknown)"} references unknown pattern ${patternId}`
                );
              }
            }

            const underlyingEvidenceScores =
              (row.supporting_pattern_ids || [])
                .map(id => patternById.get(id))
                .filter(Boolean)
                .map(p => Number(p.evidence_quality_score))
                .filter(Number.isFinite);

            const strongestUnderlyingEvidence = underlyingEvidenceScores.length
              ? Math.max(...underlyingEvidenceScores)
              : 0;

            if (Number(row.evidence_quality_score) >
                strongestUnderlyingEvidence + 1e-9) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} improperly upgrades evidence quality`
              );
            }

            if (!allowedEvidenceQuality.has(row.evidence_quality_label)) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid evidence_quality_label`
              );
            }

            if (!Array.isArray(row.shared_entities) ||
                row.shared_entities.length < 1) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} lacks shared entity support`
              );
            }

            if (!Array.isArray(row.supporting_impact_ids) ||
                !Array.isArray(row.supporting_relationship_ids) ||
                !Array.isArray(row.supporting_development_ids)) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid lineage arrays`
              );
            }

            if (!row.persistence ||
                !allowedPersistence.has(row.persistence.status)) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid persistence status`
              );
            }

            if (!row.interpretation ||
                row.interpretation.claim_class !==
                  "higher_order_structural_signal" ||
                row.interpretation.causality !== "not_established" ||
                row.interpretation.forecast !== false) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid interpretation safeguards`
              );
            }

            if (!row.signal_details ||
                Number(row.signal_details.pattern_count) < 3 ||
                Number(row.signal_details.family_count) < 2 ||
                Number(row.signal_details.shared_entity_count) < 1) {
              errors.push(
                `Emergence ${row.emergence_id || "(unknown)"} has invalid cluster diagnostics`
              );
            }
          }

          if (errors.length) {
            console.error("\nEmergence Engine preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("\nEmergence Engine isolated test passed.");
          console.log(`Status:      ${emergence.status}`);
          console.log(`Emergences: ${emergence.summary.emergence_count}`);
          console.log(
            `Systemic: ${emergence.summary.by_family?.systemic_emergence || 0}`
          );
          console.log(
            `Multi-family: ${emergence.summary.by_family?.multi_family_emergence || 0}`
          );
          console.log(
            `Acceleration + convergence: ${emergence.summary.by_family?.acceleration_convergence || 0}`
          );
          console.log(
            `Reinforced coupling: ${emergence.summary.by_family?.reinforced_coupling || 0}`
          );
          console.log(
            `Multi-pattern convergence: ${emergence.summary.by_family?.multi_pattern_convergence || 0}`
          );
          console.log(
            `Pattern graph edges: ${emergence.diagnostics.pattern_graph_edge_count}`
          );
          console.log(
            `Raw emergences: ${emergence.diagnostics.raw_emergence_count}`
          );
          console.log(
            "Evidence inheritance and higher-order safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos State Builder In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_state_builder.cjs "$TEST_DIR/scripts/"
          cp knowledge/developments.json "$TEST_DIR/knowledge/"
          cp knowledge/relationships.json "$TEST_DIR/knowledge/"

          for file in \
            state-current.json \
            delta-current.json \
            impact-current.json \
            patterns-current.json \
            emergence-current.json
          do
            if [ ! -f "knowledge/cosmos/$file" ]; then
              echo "ERROR: Missing committed Cosmos input: knowledge/cosmos/$file"
              exit 1
            fi
            cp "knowledge/cosmos/$file" "$TEST_DIR/knowledge/cosmos/$file"
          done

          (
            cd "$TEST_DIR"
            node scripts/cosmos_state_builder.cjs
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/cosmos-state-current.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos State Builder did not create knowledge/cosmos/cosmos-state-current.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.6") {
            errors.push(
              `Unexpected Cosmos State schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "ready") {
            errors.push(`Invalid Cosmos State status: ${payload.status}`);
          }

          if (
            payload.methodology?.reasoning_mode !==
            "deterministic_cosmos_state_synthesis"
          ) {
            errors.push(
              "Cosmos State deterministic synthesis methodology missing"
            );
          }

          if (payload.methodology?.no_new_claims !== true) {
            errors.push("Cosmos State must enforce no_new_claims=true");
          }

          const freshnessKeys = [
            "state",
            "delta",
            "impact",
            "patterns",
            "emergence",
            "developments",
            "relationships"
          ];

          for (const key of freshnessKeys) {
            if (!payload.source_freshness?.[key]) {
              errors.push(`Cosmos State source_freshness.${key} missing`);
            }
          }

          if (!Array.isArray(payload.now?.entities)) {
            errors.push("Cosmos State now.entities[] missing");
          }

          if (!Array.isArray(payload.now?.developments)) {
            errors.push("Cosmos State now.developments[] missing");
          }

          if (!Array.isArray(payload.graph?.relationships)) {
            errors.push("Cosmos State graph.relationships[] missing");
          }

          if (!Array.isArray(payload.impact?.signals)) {
            errors.push("Cosmos State impact.signals[] missing");
          }

          if (!Array.isArray(payload.patterns?.signals)) {
            errors.push("Cosmos State patterns.signals[] missing");
          }

          if (!Array.isArray(payload.emergence?.signals)) {
            errors.push("Cosmos State emergence.signals[] missing");
          }

          if (!Array.isArray(payload.attention?.signals)) {
            errors.push("Cosmos State attention.signals[] missing");
          }

          if (!Array.isArray(payload.attention?.clusters)) {
            errors.push("Cosmos State attention.clusters[] missing");
          }

          const attentionTypeCounts = {
            pattern: 0,
            emergence: 0,
            impact: 0
          };

          for (const row of payload.attention?.signals || []) {
            if (Object.prototype.hasOwnProperty.call(attentionTypeCounts, row.attention_type)) {
              attentionTypeCounts[row.attention_type] += 1;
            }
          }

          if ((payload.attention?.signals || []).length >= 20) {
            if (attentionTypeCounts.pattern > 8) {
              errors.push(
                `Cosmos State attention pattern quota exceeded: ${attentionTypeCounts.pattern}`
              );
            }
            if (attentionTypeCounts.emergence > 6) {
              errors.push(
                `Cosmos State attention emergence quota exceeded: ${attentionTypeCounts.emergence}`
              );
            }
            if (attentionTypeCounts.impact > 6) {
              errors.push(
                `Cosmos State attention impact quota exceeded: ${attentionTypeCounts.impact}`
              );
            }
          }

          for (const cluster of payload.attention?.clusters || []) {
            if (!cluster.cluster_id || !Array.isArray(cluster.member_attention_ids)) {
              errors.push("Cosmos State attention cluster missing identity/member references");
              continue;
            }

            if (cluster.member_attention_ids.length < 2) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} must contain at least 2 members`
              );
            }

            if (cluster.member_attention_ids.length > 7) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} exceeds v0.6 member cap: ${cluster.member_attention_ids.length}`
              );
            }

            if (
              !cluster.anchor_attention_id ||
              !cluster.anchor_type ||
              !cluster.anchor_ref_id ||
              !Array.isArray(cluster.anchor_entity_ids) ||
              cluster.anchor_entity_ids.length < 1
            ) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} missing v0.6 anchor identity fields`
              );
            }

            if (!["emergence", "pattern"].includes(cluster.anchor_type)) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} has invalid anchor_type ${cluster.anchor_type}`
              );
            }

            if (!Array.isArray(cluster.member_types) || cluster.member_types.length < 1) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} missing member_types[]`
              );
            }

            if (typeof cluster.cross_family !== "boolean") {
              errors.push(
                `Attention cluster ${cluster.cluster_id} missing cross_family boolean`
              );
            }

            if (
              !cluster.member_type_counts ||
              typeof cluster.member_type_counts !== "object"
            ) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} missing member_type_counts`
              );
            } else {
              if (Number(cluster.member_type_counts.impact || 0) < 1) {
                errors.push(
                  `Attention cluster ${cluster.cluster_id} must contain at least 1 Impact member in v0.6`
                );
              }

              if (Number(cluster.member_type_counts.emergence || 0) > 3) {
                errors.push(
                  `Attention cluster ${cluster.cluster_id} exceeds v0.6 Emergence cap`
                );
              }
            }

            const interpretation = String(cluster.interpretation || "").toLowerCase();

            if (
              !interpretation.includes("qualifies directly against the original anchor") ||
              !interpretation.includes("membership is not transitive") ||
              !interpretation.includes("anchor identity is preserved") ||
              !interpretation.includes("relevant impact signals are explicitly admitted") ||
              !interpretation.includes("near-duplicate emergence signals are suppressed") ||
              !interpretation.includes("does not establish new causality") ||
              !interpretation.includes("verification") ||
              !interpretation.includes("forecast")
            ) {
              errors.push(
                `Attention cluster ${cluster.cluster_id} missing v0.6 bounded-system safeguard`
              );
            }
          }

          if ((payload.attention?.clusters || []).length > 5) {
            errors.push(
              `Cosmos State must retain at most 5 distinct bounded systems; found ${(payload.attention?.clusters || []).length}`
            );
          }

          if (
            Number(payload.counts?.attention_cluster_members_max || 0) > 7
          ) {
            errors.push(
              `Cosmos State attention_cluster_members_max exceeds 7: ${payload.counts?.attention_cluster_members_max}`
            );
          }

          if (
            Number(payload.counts?.attention_distinct_system_target_max || 0) !== 5
          ) {
            errors.push(
              `Cosmos State distinct-system target max must be 5; found ${payload.counts?.attention_distinct_system_target_max}`
            );
          }

          if (
            Number(payload.counts?.attention_cross_family_clusters || 0) >
            Number(payload.counts?.attention_clusters || 0)
          ) {
            errors.push(
              "Cosmos State cross-family cluster count exceeds total cluster count"
            );
          }

          if (
            Number(payload.counts?.attention_clusters_with_impact || 0) !==
            Number(payload.counts?.attention_clusters || 0)
          ) {
            errors.push(
              `Cosmos State v0.6 requires every retained system to contain Impact; ` +
              `with-impact=${payload.counts?.attention_clusters_with_impact}, ` +
              `clusters=${payload.counts?.attention_clusters}`
            );
          }

          if (
            Number(payload.counts?.attention_cluster_emergence_members_max || 0) > 3
          ) {
            errors.push(
              `Cosmos State v0.6 emergence-member max exceeds 3: ` +
              `${payload.counts?.attention_cluster_emergence_members_max}`
            );
          }

          if (payload.safeguards?.forecast !== false) {
            errors.push("Cosmos State must keep forecast=false");
          }

          if (payload.safeguards?.evidence_quality_upgraded !== false) {
            errors.push(
              "Cosmos State must keep evidence_quality_upgraded=false"
            );
          }

          for (const row of payload.impact?.signals || []) {
            if (!row.impact_id) {
              errors.push("Cosmos State impact signal missing impact_id");
              continue;
            }

            if (!Array.isArray(row.entity_ids) || row.entity_ids.length < 2) {
              errors.push(
                `Cosmos State impact ${row.impact_id} missing propagated entity lineage`
              );
            }

            if (!row.path || !Array.isArray(row.path.entity_ids)) {
              errors.push(
                `Cosmos State impact ${row.impact_id} missing preserved path`
              );
            }

            if (!Array.isArray(row.supporting_development_ids)) {
              errors.push(
                `Cosmos State impact ${row.impact_id} missing development lineage`
              );
            }

            if (!Array.isArray(row.supporting_relationship_ids)) {
              errors.push(
                `Cosmos State impact ${row.impact_id} missing relationship lineage`
              );
            }

            if (!Array.isArray(row.evidence_modes)) {
              errors.push(
                `Cosmos State impact ${row.impact_id} missing evidence_modes[]`
              );
            }
          }

          const impactIds = new Set(
            (payload.impact?.signals || []).map(x => x.impact_id)
          );
          const patternIds = new Set(
            (payload.patterns?.signals || []).map(x => x.pattern_id)
          );
          const emergenceIds = new Set(
            (payload.emergence?.signals || []).map(x => x.emergence_id)
          );

          for (const row of payload.attention?.signals || []) {
            if (!row.attention_id || !row.attention_type || !row.ref_id) {
              errors.push("Attention signal missing identity fields");
              continue;
            }

            let exists = false;

            if (row.attention_type === "impact") {
              exists = impactIds.has(row.ref_id);
            } else if (row.attention_type === "pattern") {
              exists = patternIds.has(row.ref_id);
            } else if (row.attention_type === "emergence") {
              exists = emergenceIds.has(row.ref_id);
            } else {
              errors.push(
                `Attention ${row.attention_id} has invalid type ${row.attention_type}`
              );
              continue;
            }

            if (!exists) {
              errors.push(
                `Attention ${row.attention_id} references missing upstream ${row.attention_type} ${row.ref_id}`
              );
            }

            if (
              !Number.isFinite(Number(row.evidence_quality_score)) ||
              Number(row.evidence_quality_score) < 0 ||
              Number(row.evidence_quality_score) > 100
            ) {
              errors.push(
                `Attention ${row.attention_id} has invalid evidence quality`
              );
            }
          }

          const expectedHistory = path.join(
            root,
            "knowledge/cosmos/cosmos-state-history",
            `${payload.date_utc}.json`
          );

          if (!fs.existsSync(expectedHistory)) {
            errors.push(
              `Cosmos State history snapshot missing for ${payload.date_utc}`
            );
          }

          if (errors.length) {
            console.error("\nCosmos State preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("\nCosmos State isolated test passed.");
          console.log(`Date:          ${payload.date_utc}`);
          console.log(`Entities:      ${payload.counts.entities_available}`);
          console.log(`Relationships: ${payload.counts.relationships_available}`);
          console.log(`Developments:  ${payload.counts.developments_available}`);
          console.log(`Impacts:       ${payload.counts.impacts_available}`);
          console.log(`Patterns:      ${payload.counts.patterns_available}`);
          console.log(`Emergences:    ${payload.counts.emergences_available}`);
          console.log(`Attention:     ${payload.counts.attention_retained}`);
          console.log(`  Patterns:    ${payload.counts.attention_patterns ?? 0}`);
          console.log(`  Emergences:  ${payload.counts.attention_emergences ?? 0}`);
          console.log(`  Impacts:     ${payload.counts.attention_impacts ?? 0}`);
          console.log(`Clusters:      ${payload.counts.attention_clusters ?? 0}`);
          console.log(`  With Impact: ${payload.counts.attention_clusters_with_impact ?? 0}`);
          console.log(`  Max Emergence: ${payload.counts.attention_cluster_emergence_members_max ?? 0}`);
          console.log(
            "Freshness, Impact-aware composition, Emergence deduplication, anchor-preserving system diversity, evidence and no-new-claims safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Core v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_core_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"
            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/core-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Core did not create knowledge/cosmos/core-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Core schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "context_resolved") {
            errors.push(
              `Cosmos Core failed to resolve controlled context: ${payload.status}`
            );
          }

          if (payload.knowledge_state?.cosmos_state_schema_version !== "0.6") {
            errors.push(
              `Cosmos Core must consume frozen Cosmos State v0.6; got ${payload.knowledge_state?.cosmos_state_schema_version}`
            );
          }

          const starts = payload.resolution?.starting_points || [];
          const startNames = new Set(
            starts.map(row => String(row.name || "").toLowerCase())
          );

          if (starts.length < 2) {
            errors.push(
              `Cosmos Core controlled test expected at least 2 starting points; got ${starts.length}`
            );
          }

          if (!startNames.has("artificial intelligence")) {
            errors.push(
              "Cosmos Core did not resolve Artificial Intelligence starting point"
            );
          }

          if (!startNames.has("data centers")) {
            errors.push(
              "Cosmos Core did not resolve Data Centers starting point"
            );
          }

          const context = payload.selected_context || {};

          for (const key of [
            "systems",
            "attention",
            "patterns",
            "emergences",
            "impacts",
            "developments",
            "relationships",
            "entities"
          ]) {
            if (!Array.isArray(context[key])) {
              errors.push(`Cosmos Core selected_context.${key}[] missing`);
            }
          }

          if ((context.relationships || []).length < 1) {
            errors.push(
              "Cosmos Core controlled test produced no graph relationships"
            );
          }

          if ((context.impacts || []).length < 1) {
            errors.push(
              "Cosmos Core controlled test produced no relevant Impact context"
            );
          }

          if ((context.patterns || []).length < 1) {
            errors.push(
              "Cosmos Core controlled test produced no relevant Pattern context"
            );
          }

          if ((context.emergences || []).length < 1) {
            errors.push(
              "Cosmos Core controlled test produced no relevant Emergence context"
            );
          }

          if (
            Number(payload.navigation?.max_depth || 0) < 1 ||
            Number(payload.navigation?.max_depth || 0) > 3
          ) {
            errors.push(
              `Cosmos Core max_depth outside v0.1 bound: ${payload.navigation?.max_depth}`
            );
          }

          if (
            Number(payload.navigation?.max_branches || 0) < 1 ||
            Number(payload.navigation?.max_branches || 0) > 5
          ) {
            errors.push(
              `Cosmos Core max_branches outside v0.1 bound: ${payload.navigation?.max_branches}`
            );
          }

          if (payload.infinite_state?.terminal !== false) {
            errors.push(
              "Cosmos Core Infinite State must remain non-terminal"
            );
          }

          if (payload.infinite_state?.continuation_allowed !== true) {
            errors.push(
              "Cosmos Core must allow continuation"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.creates_new_causal_claims !== false) {
            errors.push(
              "Cosmos Core must not create new causal claims"
            );
          }

          if (safeguards.upgrades_evidence_quality !== false) {
            errors.push(
              "Cosmos Core must not upgrade evidence quality"
            );
          }

          if (safeguards.forecast_generated !== false) {
            errors.push(
              "Cosmos Core v0.1 must not generate forecasts"
            );
          }

          if (safeguards.sector_whitelist_present !== false) {
            errors.push(
              "Cosmos Core must remain domain-neutral with no sector whitelist"
            );
          }

          if (safeguards.traversal_bounded_for_response !== true) {
            errors.push(
              "Cosmos Core response traversal must remain bounded"
            );
          }

          if (safeguards.source_lineage_preserved !== true) {
            errors.push(
              "Cosmos Core must preserve source lineage"
            );
          }

          if (errors.length) {
            console.error("\nCosmos Core v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Core v0.1 isolated test passed.");
          console.log(`Status:          ${payload.status}`);
          console.log(`Starting points: ${starts.length}`);
          console.log(`Systems:         ${(context.systems || []).length}`);
          console.log(`Attention:       ${(context.attention || []).length}`);
          console.log(`Patterns:        ${(context.patterns || []).length}`);
          console.log(`Emergences:      ${(context.emergences || []).length}`);
          console.log(`Impacts:         ${(context.impacts || []).length}`);
          console.log(`Relationships:   ${(context.relationships || []).length}`);
          console.log(`Expanded nodes:  ${(context.entities || []).length}`);
          console.log(`Max depth:       ${payload.navigation.max_depth}`);
          console.log(`Max branches:    ${payload.navigation.max_branches}`);
          console.log(
            "Universal/domain-neutral navigation, Infinite State continuation, lineage and no-new-claims safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Attention v0.2 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_attention_v0.2_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"
            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const coreFile = path.join(root, "knowledge/cosmos/core-test-v0.1.json");
          const attentionFile = path.join(root, "knowledge/cosmos/attention-test-v0.2.json");

          if (!fs.existsSync(coreFile)) {
            throw new Error("Cosmos Core did not create the controlled core test output.");
          }

          if (!fs.existsSync(attentionFile)) {
            throw new Error(
              "Cosmos Attention did not create knowledge/cosmos/attention-test-v0.2.json"
            );
          }

          const core = JSON.parse(fs.readFileSync(coreFile, "utf8"));
          const payload = JSON.parse(fs.readFileSync(attentionFile, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.2") {
            errors.push(
              `Unexpected Cosmos Attention schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "attention_resolved") {
            errors.push(
              `Cosmos Attention failed to resolve attention: ${payload.status}`
            );
          }

          if (payload.source_core?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Attention must consume Cosmos Core schema 0.1; got ${payload.source_core?.schema_version}`
            );
          }

          const active = payload.active_context || {};
          const state = payload.attention_state || {};
          const budget = state.budget || {};
          const suppressed = state.suppressed_count || {};
          const coreContext = core.selected_context || {};

          for (const key of [
            "systems", "attention", "patterns", "emergences", "impacts",
            "developments", "relationships", "entities", "primary_pathways"
          ]) {
            if (!Array.isArray(active[key])) {
              errors.push(`Cosmos Attention active_context.${key}[] missing`);
            }
          }

          if ((active.primary_pathways || []).length < 1) {
            errors.push("Cosmos Attention controlled test produced no primary pathways");
          }

          if ((active.primary_pathways || []).length > Number(budget.primary_pathways || 0)) {
            errors.push("Cosmos Attention primary pathway budget exceeded");
          }

          const budgetChecks = [
            ["patterns", "patterns"], ["emergences", "emergences"],
            ["impacts", "impacts"], ["developments", "developments"],
            ["relationships", "relationships"], ["entities", "entities"],
            ["systems", "systems"], ["attention", "attention"]
          ];

          for (const [key, budgetKey] of budgetChecks) {
            if ((active[key] || []).length > Number(budget[budgetKey] || 0)) {
              errors.push(
                `Cosmos Attention active ${key} exceeds budget ${budget[budgetKey]}`
              );
            }
          }

          for (const key of [
            "patterns", "emergences", "impacts", "developments",
            "relationships", "entities", "systems", "attention"
          ]) {
            const discovered = Number(suppressed[key]?.discovered || 0);
            const activeCount = Number(suppressed[key]?.active || 0);
            const expandableCount =
              Number(suppressed[key]?.available_for_expansion || 0);

            if (discovered !== (coreContext[key] || []).length) {
              errors.push(
                `Cosmos Attention ${key} discovered count ${discovered} does not match Core ${(coreContext[key] || []).length}`
              );
            }

            if (activeCount + expandableCount !== discovered) {
              errors.push(
                `Cosmos Attention ${key} violates existence preservation: active ${activeCount} + expandable ${expandableCount} != discovered ${discovered}`
              );
            }
          }

          if (state.attention_mode !== "infinite_attention") {
            errors.push(
              `Cosmos Attention has invalid attention_mode: ${state.attention_mode}`
            );
          }

          if (!String(state.principle || "").toLowerCase().includes(
            "attention may suppress visibility"
          )) {
            errors.push("Cosmos Attention infinite-attention principle missing");
          }

          const expectedDimensions = [
            "question_relevance", "directness", "structural_strength",
            "confidence", "temporal_relevance", "emergence_novelty",
            "evidence_factor", "diversity", "relationship_semantics",
            "pathway_resolvability", "pathway_coverage"
          ];

          for (const dimension of expectedDimensions) {
            if (!(state.ranking_dimensions || []).includes(dimension)) {
              errors.push(
                `Cosmos Attention ranking dimension missing: ${dimension}`
              );
            }
          }

          const pathwayIntel = state.pathway_intelligence || {};

          if (pathwayIntel.intent_sensitive !== true) {
            errors.push(
              "Cosmos Attention v0.2 pathway intelligence must be intent-sensitive"
            );
          }

          if (pathwayIntel.unresolved_entities_excluded_from_primary_pathways !== true) {
            errors.push(
              "Cosmos Attention v0.2 must exclude unresolved entities from primary pathways"
            );
          }

          if (pathwayIntel.descriptive_relationships_downranked_for_impact_intent !== true) {
            errors.push(
              "Cosmos Attention v0.2 must down-rank descriptive relationships for impact intent"
            );
          }

          if (pathwayIntel.diversity_coverage_applied !== true) {
            errors.push(
              "Cosmos Attention v0.2 pathway diversity/coverage safeguard missing"
            );
          }

          for (const key of [
            "patterns", "emergences", "impacts", "relationships", "entities"
          ]) {
            for (const row of active[key] || []) {
              const attn = row._attention;
              if (!attn || !Number.isFinite(Number(attn.score))) {
                errors.push(
                  `Cosmos Attention active ${key} item is missing attention score`
                );
              }
              if (!Array.isArray(attn?.reasons)) {
                errors.push(
                  `Cosmos Attention active ${key} item is missing attention reasons`
                );
              }
            }
          }

          const primaryPathways = active.primary_pathways || [];
          const rawEntityPattern = /^ent_[a-z0-9]+$/i;

          for (const pathway of primaryPathways) {
            if (!pathway.pathway_id ||
                !pathway.from?.entity_id ||
                !pathway.to?.entity_id ||
                !pathway.relationship?.relationship_id) {
              errors.push(
                "Cosmos Attention primary pathway missing identity/lineage fields"
              );
            }

            const fromName = String(pathway.from?.name || "").trim();
            const toName = String(pathway.to?.name || "").trim();

            if (!fromName || !toName ||
                rawEntityPattern.test(fromName) ||
                rawEntityPattern.test(toName) ||
                fromName === pathway.from?.entity_id ||
                toName === pathway.to?.entity_id) {
              errors.push(
                `Cosmos Attention pathway ${pathway.pathway_id || "(unknown)"} exposes an unresolved entity`
              );
            }

            if (!Number.isFinite(Number(pathway.relationship?.semantic_adjustment))) {
              errors.push(
                `Cosmos Attention pathway ${pathway.pathway_id || "(unknown)"} missing semantic adjustment`
              );
            }

            if (!String(pathway.claim_rule || "").toLowerCase().includes(
              "does not establish new causality"
            )) {
              errors.push(
                `Cosmos Attention pathway ${pathway.pathway_id || "(unknown)"} missing no-new-causality safeguard`
              );
            }
          }

          const locatedInPrimary = primaryPathways.filter(
            pathway =>
              String(pathway.relationship?.type || "").toUpperCase() === "LOCATED_IN"
          );

          const consequencePrimary = primaryPathways.filter(pathway =>
            ["AFFECTS", "IMPACTS", "DRIVES", "REQUIRES", "DEPENDS_ON",
             "CONSTRAINS", "ENABLES", "CAUSES", "INCREASES", "DECREASES",
             "SUPPLIES", "SUPPORTS"].includes(
              String(pathway.relationship?.type || "").toUpperCase()
            )
          );

          if (locatedInPrimary.length > 0 && consequencePrimary.length > 0) {
            const bestLocated = Math.max(
              ...locatedInPrimary.map(x => Number(x.score || 0))
            );
            const bestConsequence = Math.max(
              ...consequencePrimary.map(x => Number(x.score || 0))
            );

            if (bestLocated >= bestConsequence) {
              errors.push(
                "Cosmos Attention v0.2 allows LOCATED_IN to outrank consequence pathways for an impact question"
              );
            }
          }

          if (payload.infinite_state?.terminal !== false) {
            errors.push("Cosmos Attention Infinite State must remain non-terminal");
          }
          if (payload.infinite_state?.continuation_allowed !== true) {
            errors.push("Cosmos Attention must preserve continuation");
          }
          if (payload.infinite_state?.attention_can_move !== true) {
            errors.push("Cosmos Attention must allow the attention center to move");
          }
          if (payload.infinite_state?.underlying_context_preserved !== true) {
            errors.push("Cosmos Attention must preserve underlying context");
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.creates_new_causal_claims !== false) {
            errors.push("Cosmos Attention must not create new causal claims");
          }
          if (safeguards.upgrades_evidence_quality !== false) {
            errors.push("Cosmos Attention must not upgrade evidence quality");
          }
          if (safeguards.forecast_generated !== false) {
            errors.push("Cosmos Attention v0.2 must not generate forecasts");
          }
          if (safeguards.sector_whitelist_present !== false) {
            errors.push("Cosmos Attention must remain domain-neutral");
          }
          if (safeguards.visibility_suppression_only !== true ||
              safeguards.existence_preserved !== true) {
            errors.push(
              "Cosmos Attention must suppress visibility only while preserving existence"
            );
          }
          if (safeguards.primary_pathways_require_resolved_entities !== true) {
            errors.push(
              "Cosmos Attention v0.2 resolved-entity pathway safeguard missing"
            );
          }
          if (safeguards.descriptive_relationships_are_intent_weighted !== true) {
            errors.push(
              "Cosmos Attention v0.2 intent-weighted relationship safeguard missing"
            );
          }
          if (safeguards.source_lineage_preserved !== true) {
            errors.push("Cosmos Attention must preserve source lineage");
          }

          if (errors.length) {
            console.error("\nCosmos Attention v0.2 preflight errors:");
            for (const error of errors) console.error(`- ${error}`);
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Attention v0.2 isolated test passed.");
          console.log(`Status:             ${payload.status}`);
          console.log(`Primary pathways:   ${(active.primary_pathways || []).length}`);
          console.log(
            `Patterns:           ${(coreContext.patterns || []).length} discovered -> ${(active.patterns || []).length} active`
          );
          console.log(
            `Emergences:         ${(coreContext.emergences || []).length} discovered -> ${(active.emergences || []).length} active`
          );
          console.log(
            `Impacts:            ${(coreContext.impacts || []).length} discovered -> ${(active.impacts || []).length} active`
          );
          console.log(
            `Relationships:      ${(coreContext.relationships || []).length} discovered -> ${(active.relationships || []).length} active`
          );
          console.log(
            `Entities:           ${(coreContext.entities || []).length} discovered -> ${(active.entities || []).length} active`
          );
          console.log(
            "Infinite Attention budgets, existence preservation, movable attention, diversity and no-new-claims safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Consequence v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_consequence_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/consequence-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Consequence did not create consequence-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Consequence schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "consequence_frontier_resolved") {
            errors.push(
              `Cosmos Consequence failed to resolve frontier: ${payload.status}`
            );
          }

          if (payload.source_attention?.schema_version !== "0.2") {
            errors.push(
              `Cosmos Consequence must consume Attention v0.2; got ${payload.source_attention?.schema_version}`
            );
          }

          if (!payload.origin_event ||
              Number(payload.origin_event.butterfly_distance) !== 0 ||
              !Array.isArray(payload.origin_event.entity_ids) ||
              payload.origin_event.entity_ids.length < 1) {
            errors.push("Cosmos Consequence origin event invalid");
          }

          const field = payload.consequence_field || {};
          const rows = field.consequences || [];

          if (!Array.isArray(rows)) {
            errors.push("consequence_field.consequences[] missing");
          }

          if (!Array.isArray(field.frontier_snapshots)) {
            errors.push("consequence_field.frontier_snapshots[] missing");
          }

          if (!Array.isArray(field.remaining_frontier_entity_ids)) {
            errors.push("remaining_frontier_entity_ids[] missing");
          }

          if (rows.length < 2) {
            errors.push(
              `Expected multiple controlled consequences; got ${rows.length}`
            );
          }

          const distances = rows
            .map(x => Number(x.butterfly_distance))
            .filter(Number.isFinite);

          if (!distances.includes(1)) {
            errors.push("No butterfly-distance-1 direct consequence found");
          }

          if (!distances.some(x => x >= 2)) {
            errors.push("No deeper butterfly ripple beyond distance 1 found");
          }

          for (const row of rows) {
            if (!row.consequence_id ||
                !row.from_entity_id ||
                !row.to_entity_id ||
                !row.relationship_id) {
              errors.push("Consequence missing identity/relationship fields");
            }

            if (!Number.isInteger(Number(row.butterfly_distance)) ||
                Number(row.butterfly_distance) < 1) {
              errors.push(
                `Invalid butterfly distance on ${row.consequence_id || "(unknown)"}`
              );
            }

            if (!Array.isArray(row.lineage_relationship_ids) ||
                row.lineage_relationship_ids.length < 1) {
              errors.push(
                `Missing relationship lineage on ${row.consequence_id || "(unknown)"}`
              );
            }

            if (!Array.isArray(row.lineage_entity_ids) ||
                row.lineage_entity_ids.length < 2) {
              errors.push(
                `Missing entity lineage on ${row.consequence_id || "(unknown)"}`
              );
            }

            if (!Array.isArray(row.evidence_development_ids)) {
              errors.push(
                `Missing evidence lineage on ${row.consequence_id || "(unknown)"}`
              );
            }

            if (row.propagation_direction === "reverse_context" &&
                row.claim_class === "graph_supported_consequence") {
              errors.push(
                `Reverse traversal improperly treated as consequence on ${row.consequence_id || "(unknown)"}`
              );
            }

            if (row.continuation_possible !== true) {
              errors.push(
                `Consequence ${row.consequence_id || "(unknown)"} is not continuable`
              );
            }
          }

          if (!Array.isArray(payload.affected_actors)) {
            errors.push("affected_actors[] missing");
          }

          if (!payload.alternatives ||
              !Array.isArray(payload.alternatives.items)) {
            errors.push("alternatives.items[] missing");
          }

          for (const alt of payload.alternatives?.items || []) {
            if (alt.qualification_status !== "requires_validation") {
              errors.push(
                `Alternative ${alt.alternative_id || "(unknown)"} improperly validated`
              );
            }
          }

          if (!payload.knowledge_gaps ||
              !Array.isArray(payload.knowledge_gaps.items)) {
            errors.push("knowledge_gaps.items[] missing");
          }

          const horizonKeys = [
            "immediate_or_short",
            "short_to_mid",
            "mid",
            "mid_to_long",
            "long_to_far"
          ];

          for (const key of horizonKeys) {
            if (!payload.horizon_map?.[key] ||
                !Array.isArray(payload.horizon_map[key].consequence_ids)) {
              errors.push(`horizon_map.${key} invalid`);
            }
          }

          if (!payload.evidence_lineage ||
              !Array.isArray(payload.evidence_lineage.development_ids) ||
              !Array.isArray(payload.evidence_lineage.relationship_ids) ||
              payload.evidence_lineage.source_lineage_preserved !== true) {
            errors.push("Cosmos Consequence evidence lineage invalid");
          }

          const butterfly = payload.butterfly_state || {};

          if (Number(butterfly.origin_distance) !== 0) {
            errors.push("Butterfly origin distance must equal 0");
          }

          if (butterfly.runtime_distance_is_computational_budget_only !== true) {
            errors.push("Runtime distance must be computational-budget-only");
          }

          if (butterfly.conceptual_distance_limit !== null) {
            errors.push("Conceptual butterfly-distance limit must remain null");
          }

          if (butterfly.continuation_possible !== true ||
              butterfly.frontier_is_terminal !== false ||
              butterfly.consequence_can_become_new_origin !== true) {
            errors.push("Butterfly continuation/new-origin invariants invalid");
          }

          if (butterfly.convergence_allowed !== true ||
              butterfly.divergence_allowed !== true ||
              butterfly.feedback_loops_allowed !== true) {
            errors.push(
              "Convergence/divergence/feedback-loop capability missing"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.creates_new_causal_claims !== false ||
              safeguards.upgrades_evidence_quality !== false ||
              safeguards.numeric_forecast_generated !== false ||
              safeguards.unidentified_material_is_not_assumed !== true ||
              safeguards.reverse_relationships_are_not_treated_as_forward_causality !== true ||
              safeguards.alternatives_require_validation !== true ||
              safeguards.runtime_budget_is_not_conceptual_limit !== true ||
              safeguards.source_lineage_preserved !== true) {
            errors.push("Cosmos Consequence safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Consequence v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Consequence v0.1 isolated test passed.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Origin:            ${payload.origin_event.statement}`);
          console.log(`Consequences:      ${rows.length}`);
          console.log(`Max distance seen: ${distances.length ? Math.max(...distances) : 0}`);
          console.log(`Affected actors:   ${(payload.affected_actors || []).length}`);
          console.log(`Alternatives:      ${(payload.alternatives?.items || []).length}`);
          console.log(`Knowledge gaps:    ${(payload.knowledge_gaps?.items || []).length}`);
          console.log(
            "Butterfly distance, lineage, continuation and non-terminal frontier safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Discovery v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_discovery_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/discovery-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Discovery did not create knowledge/cosmos/discovery-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Discovery schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "discovery_targets_resolved") {
            errors.push(
              `Cosmos Discovery failed to resolve targets: ${payload.status}`
            );
          }

          if (payload.source_consequence?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Discovery must consume Consequence v0.1; got ${payload.source_consequence?.schema_version}`
            );
          }

          const targets =
            payload.discovery_targets?.items ||
            payload.discovery_targets ||
            [];

          if (!Array.isArray(targets) || targets.length < 1) {
            errors.push("Cosmos Discovery produced no discovery_targets");
          }

          const queue = payload.next_acquisition_queue || [];

          if (!Array.isArray(queue) || queue.length < 1) {
            errors.push("Cosmos Discovery next_acquisition_queue[] missing or empty");
          }

          const allowedActions = new Set([
            "resolve_entity",
            "validate_direction",
            "strengthen_evidence",
            "identify_suppliers",
            "identify_buyers",
            "identify_substitutes",
            "identify_capacity",
            "identify_geography",
            "identify_logistics",
            "identify_people",
            "identify_companies",
            "identify_components",
            "identify_materials",
            "identify_market_exposure",
            "extend_frontier"
          ]);

          for (const target of targets) {
            const action =
              target.discovery_action ||
              target.action ||
              target.target_type;

            if (action && !allowedActions.has(action)) {
              errors.push(
                `Discovery target ${target.discovery_target_id || target.target_id || "(unknown)"} has invalid action ${action}`
              );
            }

            if (typeof target.reopens_frontier_if_resolved !== "boolean") {
              errors.push(
                `Discovery target ${target.discovery_target_id || target.target_id || "(unknown)"} missing reopens_frontier_if_resolved boolean`
              );
            }
          }

          for (let i = 0; i < queue.length; i += 1) {
            const row = queue[i];

            if (i > 0) {
              const prev = queue[i - 1];
              const prevRank = Number(prev.queue_rank ?? i);
              const rank = Number(row.queue_rank ?? i + 1);

              if (Number.isFinite(prevRank) &&
                  Number.isFinite(rank) &&
                  rank <= prevRank) {
                errors.push("Cosmos Discovery acquisition queue is not ranked");
                break;
              }
            }
          }

          const targetTypes = new Set(
            targets.map(target =>
              target.discovery_action ||
              target.action ||
              target.target_type
            ).filter(Boolean)
          );

          if (targetTypes.size < 1) {
            errors.push(
              "Cosmos Discovery controlled test produced no classified discovery target types"
            );
          }

          const reopenableTargets = targets.filter(
            target => target.reopens_frontier_if_resolved === true
          );

          if (reopenableTargets.length < 1) {
            errors.push(
              "Cosmos Discovery controlled test produced no frontier-reopening targets"
            );
          }

          if (!targetTypes.has("extend_frontier") &&
              payload.curiosity_state?.continuation_possible === true &&
              payload.curiosity_state?.current_frontier_is_terminal === false &&
              (payload.source_consequence?.status === "consequence_frontier_resolved")) {
            // Not every deterministic run must need extend_frontier: a non-empty
            // remaining frontier can itself keep propagation open.
            const hasReopenable = reopenableTargets.length > 0;
            if (!hasReopenable) {
              errors.push(
                "Cosmos Discovery continuation is open but no extend_frontier or other reopenable target exists"
              );
            }
          }

          const curiosity = payload.curiosity_state || {};

          if (curiosity.conceptual_distance_limit !== undefined &&
              curiosity.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Discovery conceptual distance must remain unlimited/null"
            );
          }

          if (curiosity.continuation_possible !== true) {
            errors.push(
              "Cosmos Discovery must preserve consequence continuation"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false) {
            errors.push("Cosmos Discovery v0.1 must not execute external search");
          }

          if (safeguards.calls_openai_or_external_api !== false) {
            errors.push("Cosmos Discovery v0.1 must not use OpenAI/API generation");
          }

          if (safeguards.invents_missing_entities !== false) {
            errors.push("Cosmos Discovery must not invent missing entities");
          }

          if (safeguards.upgrades_evidence_quality !== false) {
            errors.push("Cosmos Discovery must not upgrade evidence quality");
          }

          if (safeguards.converts_unknown_to_fact !== false) {
            errors.push("Cosmos Discovery must not convert unknowns into facts");
          }

          if (safeguards.acquisition_targets_are_requests_for_knowledge_not_claims !== true) {
            errors.push(
              "Cosmos Discovery acquisition targets must remain requests for knowledge, not claims"
            );
          }

          if (safeguards.source_lineage_preserved !== true) {
            errors.push("Cosmos Discovery must preserve source lineage");
          }

          if (safeguards.consequence_frontier_may_be_reopened !== true) {
            errors.push("Cosmos Discovery must preserve frontier reopening");
          }

          if (errors.length) {
            console.error("\nCosmos Discovery v0.1 preflight errors:");
            for (const error of errors) console.error(`- ${error}`);
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Discovery v0.1 isolated test passed.");
          console.log(`Status:              ${payload.status}`);
          console.log(`Discovery targets:   ${targets.length}`);
          console.log(`Acquisition queue:   ${queue.length}`);
          console.log(
            `Reopenable targets:  ${targets.filter(x => x.reopens_frontier_if_resolved === true).length}`
          );
          console.log(
            "Knowledge-gap targeting, frontier reopening, ranked acquisition and no-invention safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Acquisition v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_acquisition_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/acquisition-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Acquisition did not create knowledge/cosmos/acquisition-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Acquisition schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "acquisition_plans_resolved") {
            errors.push(
              `Cosmos Acquisition failed to resolve plans: ${payload.status}`
            );
          }

          if (payload.source_discovery?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Acquisition must consume Discovery v0.1; got ${payload.source_discovery?.schema_version}`
            );
          }

          const plans = payload.acquisition_plans || [];
          const executorQueue = payload.future_executor_queue || [];

          if (!Array.isArray(plans) || plans.length < 1) {
            errors.push("Cosmos Acquisition produced no acquisition_plans[]");
          }

          if (!Array.isArray(executorQueue) || executorQueue.length < 1) {
            errors.push("Cosmos Acquisition future_executor_queue[] missing or empty");
          }

          if (plans.length !== executorQueue.length) {
            errors.push(
              `Cosmos Acquisition plan/executor queue mismatch: ${plans.length} vs ${executorQueue.length}`
            );
          }

          const planIds = new Set();

          for (const plan of plans) {
            if (!plan.acquisition_plan_id) {
              errors.push("Acquisition plan missing acquisition_plan_id");
              continue;
            }

            if (planIds.has(plan.acquisition_plan_id)) {
              errors.push(
                `Duplicate acquisition_plan_id ${plan.acquisition_plan_id}`
              );
            }
            planIds.add(plan.acquisition_plan_id);

            if (!plan.discovery_target_id) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} missing discovery_target_id`
              );
            }

            if (!plan.target_type || !plan.statement) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} missing target_type/statement`
              );
            }

            if (!plan.lineage || typeof plan.lineage !== "object") {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} missing lineage`
              );
            } else {
              for (const key of [
                "entity_ids",
                "consequence_ids",
                "relationship_ids",
                "originating_gap_ids"
              ]) {
                if (!Array.isArray(plan.lineage[key])) {
                  errors.push(
                    `Acquisition plan ${plan.acquisition_plan_id} lineage.${key}[] missing`
                  );
                }
              }
            }

            if (!Array.isArray(plan.query_templates) ||
                plan.query_templates.length < 1) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} has no query_templates`
              );
            }

            if (!Array.isArray(plan.source_strategy) ||
                plan.source_strategy.length < 1) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} has no source_strategy`
              );
            } else {
              let previousAuthority = Infinity;

              for (const source of plan.source_strategy) {
                if (!source.source_type ||
                    !Number.isFinite(Number(source.authority_score))) {
                  errors.push(
                    `Acquisition plan ${plan.acquisition_plan_id} has invalid source strategy`
                  );
                  continue;
                }

                const score = Number(source.authority_score);

                if (score > previousAuthority + 1e-9) {
                  errors.push(
                    `Acquisition plan ${plan.acquisition_plan_id} source strategy is not authority-ranked`
                  );
                }

                previousAuthority = score;

                if (source.acquisition_status !== "planned" ||
                    source.execution_adapter !== "unassigned") {
                  errors.push(
                    `Acquisition plan ${plan.acquisition_plan_id} prematurely executes a source`
                  );
                }
              }
            }

            const contract = plan.evidence_contract || {};

            if (!contract.objective ||
                !Array.isArray(contract.required_fields) ||
                !Array.isArray(contract.validation_rules) ||
                !Array.isArray(contract.accepted_dispositions)) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} evidence contract invalid`
              );
            }

            if (contract.evidence_is_not_knowledge_until_validated !== true ||
                contract.knowledge_admission_status !== "not_evaluated") {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} bypasses evidence validation`
              );
            }

            if (!plan.frontier_effect ||
                typeof plan.frontier_effect.reopens_frontier_if_resolved !== "boolean") {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} frontier effect invalid`
              );
            }

            if (plan.frontier_effect?.reopens_frontier_if_resolved === true &&
                plan.frontier_effect?.consequence_frontier_action !==
                  "eligible_for_repropagation_after_validation") {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} may reopen frontier before validation`
              );
            }

            if (plan.execution?.mode !== "planned_only" ||
                plan.execution?.network_execution_performed !== false ||
                plan.execution?.executor_status !== "not_connected" ||
                Number(plan.execution?.result_count) !== 0) {
              errors.push(
                `Acquisition plan ${plan.acquisition_plan_id} execution boundary invalid`
              );
            }
          }

          for (let i = 0; i < executorQueue.length; i += 1) {
            const row = executorQueue[i];

            if (Number(row.execution_rank) !== i + 1) {
              errors.push(
                `Future executor queue rank invalid at index ${i}`
              );
            }

            if (!planIds.has(row.acquisition_plan_id)) {
              errors.push(
                `Future executor queue references unknown plan ${row.acquisition_plan_id}`
              );
            }

            if (row.execution_status !== "not_connected") {
              errors.push(
                `Future executor queue ${row.acquisition_plan_id} must remain not_connected`
              );
            }

            if (!Array.isArray(row.query_templates) ||
                !Array.isArray(row.source_strategy)) {
              errors.push(
                `Future executor queue ${row.acquisition_plan_id} missing planned query/source strategy`
              );
            }
          }

          const state = payload.acquisition_state || {};

          if (Number(state.plan_count) !== plans.length) {
            errors.push(
              `Cosmos Acquisition state.plan_count mismatch: ${state.plan_count} vs ${plans.length}`
            );
          }

          if (state.execution_mode !== "planned_only" ||
              state.external_execution_connected !== false) {
            errors.push(
              "Cosmos Acquisition v0.1 must remain planning-only"
            );
          }

          if (state.evidence_validation_required_before_graph_admission !== true) {
            errors.push(
              "Cosmos Acquisition must require validation before graph admission"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Acquisition conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Acquisition must preserve Cosmos continuation"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.invents_entities !== false ||
              safeguards.upgrades_evidence_quality !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.converts_acquisition_plan_to_fact !== false ||
              safeguards.evidence_requires_validation !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.discovery_lineage_preserved !== true ||
              safeguards.frontier_reopens_only_after_validation !== true) {
            errors.push("Cosmos Acquisition safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Acquisition v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Acquisition v0.1 isolated test passed.");
          console.log(`Status:             ${payload.status}`);
          console.log(`Acquisition plans:  ${plans.length}`);
          console.log(`Executor queue:      ${executorQueue.length}`);
          console.log(
            `Reopening plans:     ${plans.filter(x => x.frontier_effect?.reopens_frontier_if_resolved === true).length}`
          );
          console.log(
            "Planning-only execution boundary, ranked source strategy, evidence contracts, lineage and validation-before-knowledge safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Acquisition Applicability v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_applicability_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/applicability-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Applicability did not create applicability-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Applicability schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "acquisition_applicability_resolved") {
            errors.push(
              `Cosmos Applicability failed: ${payload.status}`
            );
          }

          if (payload.source_acquisition?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Applicability must consume Acquisition v0.1; got ${payload.source_acquisition?.schema_version}`
            );
          }

          const state = payload.applicability_state || {};
          const evaluations = payload.evaluations || [];
          const executableQueue = payload.executable_queue || [];
          const decompositionQueue = payload.decomposition_queue || [];

          if (!Array.isArray(evaluations) || evaluations.length < 1) {
            errors.push("Cosmos Applicability evaluations[] missing or empty");
          }

          if (!Array.isArray(executableQueue)) {
            errors.push("Cosmos Applicability executable_queue[] missing");
          }

          if (!Array.isArray(decompositionQueue)) {
            errors.push("Cosmos Applicability decomposition_queue[] missing");
          }

          if (Number(state.plan_count) !== evaluations.length) {
            errors.push(
              `Cosmos Applicability plan_count mismatch: ${state.plan_count} vs ${evaluations.length}`
            );
          }

          const applicable = evaluations.filter(x => x.applicability_status === "applicable");
          const decompose = evaluations.filter(
            x => x.applicability_status === "decomposition_required"
          );

          if (Number(state.applicable_count) !== applicable.length) {
            errors.push("Cosmos Applicability applicable_count mismatch");
          }

          if (Number(state.decomposition_required_count) !== decompose.length) {
            errors.push(
              "Cosmos Applicability decomposition_required_count mismatch"
            );
          }

          if (Number(state.executable_queue_count) !== executableQueue.length) {
            errors.push(
              "Cosmos Applicability executable_queue_count mismatch"
            );
          }

          if (state.external_execution_connected !== false) {
            errors.push(
              "Cosmos Applicability v0.1 must not connect external execution"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Applicability conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Applicability must preserve continuation"
            );
          }

          for (const row of evaluations) {
            if (!row.acquisition_plan_id ||
                !row.discovery_target_id ||
                !row.target_type ||
                !row.original_statement ||
                !row.subject) {
              errors.push(
                "Cosmos Applicability evaluation missing identity/context"
              );
            }

            if (!["applicable", "decomposition_required"].includes(
              row.applicability_status
            )) {
              errors.push(
                `Cosmos Applicability invalid status ${row.applicability_status}`
              );
            }

            if (typeof row.executable !== "boolean") {
              errors.push(
                `Cosmos Applicability evaluation ${row.acquisition_plan_id || "(unknown)"} missing executable boolean`
              );
            }

            if (row.applicability_status === "decomposition_required") {
              if (row.executable !== false) {
                errors.push(
                  `Decomposition-required plan ${row.acquisition_plan_id} must be blocked`
                );
              }

              if (!row.decomposition_question ||
                  !Array.isArray(row.decomposition_hints) ||
                  row.decomposition_hints.length < 1) {
                errors.push(
                  `Decomposition-required plan ${row.acquisition_plan_id} lacks decomposition guidance`
                );
              }
            }

            if (row.applicability_status === "applicable" &&
                row.executable !== true) {
              errors.push(
                `Applicable plan ${row.acquisition_plan_id} is not executable`
              );
            }

            if (row.original_plan_preserved !== true) {
              errors.push(
                `Applicability evaluation ${row.acquisition_plan_id} did not preserve source plan`
              );
            }
          }

          const evalById = new Map(
            evaluations.map(x => [x.acquisition_plan_id, x])
          );

          for (let i = 0; i < executableQueue.length; i += 1) {
            const row = executableQueue[i];

            if (Number(row.execution_rank) !== i + 1) {
              errors.push(
                `Executable queue rank invalid at index ${i}`
              );
            }

            const evaluation = evalById.get(row.acquisition_plan_id);

            if (!evaluation || evaluation.executable !== true) {
              errors.push(
                `Executable queue contains blocked/unknown plan ${row.acquisition_plan_id}`
              );
            }

            if (row.applicability_status !== "applicable") {
              errors.push(
                `Executable queue plan ${row.acquisition_plan_id} not marked applicable`
              );
            }
          }

          for (let i = 0; i < decompositionQueue.length; i += 1) {
            const row = decompositionQueue[i];

            if (Number(row.decomposition_rank) !== i + 1) {
              errors.push(
                `Decomposition queue rank invalid at index ${i}`
              );
            }

            const evaluation = evalById.get(row.acquisition_plan_id);

            if (!evaluation ||
                evaluation.applicability_status !== "decomposition_required") {
              errors.push(
                `Decomposition queue contains non-decomposition plan ${row.acquisition_plan_id}`
              );
            }

            if (row.status !== "not_resolved") {
              errors.push(
                `Decomposition queue plan ${row.acquisition_plan_id} must remain unresolved`
              );
            }
          }

          const aiPhysicalBlocks = decompose.filter(row =>
            /artificial intelligence/i.test(row.subject) &&
            ["identify_components", "identify_materials", "identify_capacity"]
              .includes(row.target_type)
          );

          if (aiPhysicalBlocks.length < 1) {
            errors.push(
              "Controlled Applicability test did not block a physically specific Artificial Intelligence acquisition plan"
            );
          }

          const transformerExecutable = applicable.filter(row =>
            /transformers/i.test(row.subject) &&
            ["identify_components", "identify_materials", "identify_capacity"]
              .includes(row.target_type)
          );

          if (transformerExecutable.length < 1) {
            errors.push(
              "Controlled Applicability test did not preserve an applicable Transformer physical acquisition plan"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.invents_entities !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.mutates_source_acquisition_plans !== false ||
              safeguards.executes_decomposition_hints_as_facts !== false ||
              safeguards.abstract_targets_blocked_from_physical_execution !== true ||
              safeguards.source_lineage_preserved !== true) {
            errors.push("Cosmos Applicability safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Acquisition Applicability v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Acquisition Applicability v0.1 isolated test passed.");
          console.log(`Status:                 ${payload.status}`);
          console.log(`Plans evaluated:        ${evaluations.length}`);
          console.log(`Applicable:             ${applicable.length}`);
          console.log(`Decomposition required: ${decompose.length}`);
          console.log(`Executable queue:       ${executableQueue.length}`);
          console.log(
            "Abstract physical-target blocking, executable filtering, decomposition guidance, lineage and no-execution safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Decomposition v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp scripts/cosmos_decomposition.cjs \
            "$TEST_DIR/scripts/cosmos_decomposition.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_decomposition_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json

            node scripts/cosmos_decomposition.cjs \
              --input knowledge/cosmos/applicability-test-v0.1.json \
              --out knowledge/cosmos/decomposition-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/decomposition-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Decomposition did not create decomposition-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Decomposition schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "decomposition_candidates_resolved") {
            errors.push(
              `Cosmos Decomposition failed: ${payload.status}`
            );
          }

          if (payload.source_applicability?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Decomposition must consume Applicability v0.1; got ${payload.source_applicability?.schema_version}`
            );
          }

          const state = payload.decomposition_state || {};
          const decompositions = payload.decompositions || [];
          const validationQueue = payload.candidate_validation_queue || [];

          if (!Array.isArray(decompositions) || decompositions.length < 1) {
            errors.push("Cosmos Decomposition decompositions[] missing or empty");
          }

          if (!Array.isArray(validationQueue) || validationQueue.length < 1) {
            errors.push(
              "Cosmos Decomposition candidate_validation_queue[] missing or empty"
            );
          }

          if (Number(state.blocked_plan_count) !== decompositions.length) {
            errors.push(
              `Cosmos Decomposition blocked_plan_count mismatch: ${state.blocked_plan_count} vs ${decompositions.length}`
            );
          }

          const allCandidates = decompositions.flatMap(
            row => Array.isArray(row.candidates) ? row.candidates : []
          );

          if (Number(state.candidate_count) !== allCandidates.length) {
            errors.push(
              `Cosmos Decomposition candidate_count mismatch: ${state.candidate_count} vs ${allCandidates.length}`
            );
          }

          if (Number(state.validated_candidate_count) !== 0) {
            errors.push(
              "Cosmos Decomposition v0.1 must not auto-validate candidates"
            );
          }

          if (Number(state.executable_candidate_count) !== 0) {
            errors.push(
              "Cosmos Decomposition v0.1 must not make candidates executable"
            );
          }

          if (state.external_execution_connected !== false) {
            errors.push(
              "Cosmos Decomposition v0.1 must not connect external execution"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Decomposition conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Decomposition must preserve continuation"
            );
          }

          for (const decomposition of decompositions) {
            if (!decomposition.decomposition_id ||
                !decomposition.acquisition_plan_id ||
                !decomposition.discovery_target_id ||
                !decomposition.abstract_subject ||
                !decomposition.original_target_type) {
              errors.push(
                "Cosmos Decomposition item missing identity/lineage context"
              );
            }

            if (![
              "provisional_candidates_resolved",
              "insufficient_decomposition_context"
            ].includes(decomposition.status)) {
              errors.push(
                `Cosmos Decomposition invalid status ${decomposition.status}`
              );
            }

            if (!Array.isArray(decomposition.candidates)) {
              errors.push(
                `Cosmos Decomposition ${decomposition.decomposition_id || "(unknown)"} candidates[] missing`
              );
              continue;
            }

            const contract = decomposition.resolution_contract || {};

            if (contract.candidates_are_facts !== false ||
                contract.candidates_are_executable !== false ||
                contract.validation_required !== true ||
                contract.external_evidence_required !== true ||
                contract.parent_plan_remains_blocked !== true) {
              errors.push(
                `Cosmos Decomposition ${decomposition.decomposition_id || "(unknown)"} resolution contract invalid`
              );
            }

            for (const candidate of decomposition.candidates) {
              if (!candidate.decomposition_candidate_id ||
                  !candidate.parent_acquisition_plan_id ||
                  !candidate.parent_discovery_target_id ||
                  !candidate.abstract_subject ||
                  !candidate.candidate_label ||
                  !candidate.candidate_class) {
                errors.push(
                  "Cosmos Decomposition candidate missing identity/context"
                );
              }

              if (candidate.epistemic_status !== "provisional_candidate" ||
                  candidate.validated !== false ||
                  candidate.executable !== false) {
                errors.push(
                  `Candidate ${candidate.decomposition_candidate_id || "(unknown)"} improperly promoted`
                );
              }

              if (!Array.isArray(candidate.proposed_investigation_types) ||
                  candidate.proposed_investigation_types.length < 1) {
                errors.push(
                  `Candidate ${candidate.decomposition_candidate_id || "(unknown)"} lacks proposed investigation types`
                );
              }

              if (!Array.isArray(candidate.validation_requirements) ||
                  candidate.validation_requirements.length < 1) {
                errors.push(
                  `Candidate ${candidate.decomposition_candidate_id || "(unknown)"} lacks validation requirements`
                );
              }

              if (!candidate.lineage ||
                  !candidate.lineage.acquisition_plan_id ||
                  !candidate.lineage.discovery_target_id) {
                errors.push(
                  `Candidate ${candidate.decomposition_candidate_id || "(unknown)"} missing lineage`
                );
              }
            }
          }

          const candidateIds = new Set(
            allCandidates.map(x => x.decomposition_candidate_id)
          );

          if (candidateIds.size !== allCandidates.length) {
            errors.push("Cosmos Decomposition candidate IDs are not unique");
          }

          if (validationQueue.length !== allCandidates.length) {
            errors.push(
              `Cosmos Decomposition validation queue mismatch: ${validationQueue.length} vs ${allCandidates.length}`
            );
          }

          for (let i = 0; i < validationQueue.length; i += 1) {
            const row = validationQueue[i];

            if (Number(row.validation_rank) !== i + 1) {
              errors.push(
                `Candidate validation queue rank invalid at index ${i}`
              );
            }

            if (!candidateIds.has(row.decomposition_candidate_id)) {
              errors.push(
                `Candidate validation queue references unknown candidate ${row.decomposition_candidate_id}`
              );
            }

            if (row.validation_status !== "not_started" ||
                row.execution_status !== "blocked_pending_validation") {
              errors.push(
                `Candidate ${row.decomposition_candidate_id} crossed validation/execution boundary`
              );
            }

            if (!Array.isArray(row.proposed_investigation_types) ||
                !Array.isArray(row.validation_requirements)) {
              errors.push(
                `Candidate validation queue ${row.decomposition_candidate_id} missing investigation/validation fields`
              );
            }
          }

          const aiCandidates = allCandidates.filter(
            row => /artificial intelligence/i.test(row.abstract_subject)
          );

          if (aiCandidates.length < 1) {
            errors.push(
              "Controlled Decomposition test produced no Artificial Intelligence candidates"
            );
          }

          const expectedHints = [
            "AI compute infrastructure",
            "semiconductors and accelerators",
            "data centers",
            "electricity demand",
            "cooling systems",
            "networking infrastructure",
            "software/model infrastructure"
          ];

          const aiLabels = new Set(
            aiCandidates.map(x => String(x.candidate_label || ""))
          );

          for (const expected of expectedHints) {
            if (!aiLabels.has(expected)) {
              errors.push(
                `Controlled Decomposition test missing expected AI candidate: ${expected}`
              );
            }
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.promotes_candidates_to_facts !== false ||
              safeguards.executes_candidates !== false ||
              safeguards.validates_candidates !== false ||
              safeguards.parent_acquisition_plans_remain_blocked !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Cosmos Decomposition safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Decomposition v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Decomposition v0.1 isolated test passed.");
          console.log(`Status:              ${payload.status}`);
          console.log(`Blocked plans:       ${state.blocked_plan_count}`);
          console.log(`Decompositions:      ${decompositions.length}`);
          console.log(`Candidates:          ${allCandidates.length}`);
          console.log(`Validation queue:    ${validationQueue.length}`);
          console.log(`Validated:           ${state.validated_candidate_count}`);
          console.log(`Executable:          ${state.executable_candidate_count}`);
          console.log(
            "Provisional-candidate boundaries, lineage, validation queue and no-promotion safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Candidate Validation Planner v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp scripts/cosmos_decomposition.cjs \
            "$TEST_DIR/scripts/cosmos_decomposition.cjs"
          cp scripts/cosmos_candidate_validation.cjs \
            "$TEST_DIR/scripts/cosmos_candidate_validation.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_candidate_validation_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json

            node scripts/cosmos_decomposition.cjs \
              --input knowledge/cosmos/applicability-test-v0.1.json \
              --out knowledge/cosmos/decomposition-test-v0.1.json

            node scripts/cosmos_candidate_validation.cjs \
              --input knowledge/cosmos/decomposition-test-v0.1.json \
              --out knowledge/cosmos/candidate-validation-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/candidate-validation-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Candidate Validation Planner did not create candidate-validation-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Candidate Validation schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "candidate_validation_targets_resolved") {
            errors.push(
              `Candidate Validation Planner failed: ${payload.status}`
            );
          }

          if (payload.source_decomposition?.schema_version !== "0.1") {
            errors.push(
              `Candidate Validation Planner must consume Decomposition v0.1; got ${payload.source_decomposition?.schema_version}`
            );
          }

          const state = payload.validation_state || {};
          const targets = payload.validation_targets || [];
          const queue = payload.future_evidence_queue || [];

          if (!Array.isArray(targets) || targets.length < 1) {
            errors.push("Candidate Validation Planner produced no validation_targets[]");
          }

          if (!Array.isArray(queue) || queue.length < 1) {
            errors.push("Candidate Validation Planner future_evidence_queue[] missing or empty");
          }

          if (queue.length !== targets.length) {
            errors.push(
              `Candidate Validation target/evidence queue mismatch: ${targets.length} vs ${queue.length}`
            );
          }

          if (Number(state.raw_candidate_count) !== 21) {
            errors.push(
              `Controlled Candidate Validation test expected 21 raw candidates; got ${state.raw_candidate_count}`
            );
          }

          if (Number(state.unique_validation_target_count) !== 7) {
            errors.push(
              `Controlled Candidate Validation test expected 7 unique validation targets; got ${state.unique_validation_target_count}`
            );
          }

          if (Number(state.duplicate_candidate_count) !== 14) {
            errors.push(
              `Controlled Candidate Validation test expected 14 duplicate candidates; got ${state.duplicate_candidate_count}`
            );
          }

          if (Number(state.validated_target_count) !== 0 ||
              Number(state.executable_target_count) !== 0) {
            errors.push(
              "Candidate Validation Planner v0.1 must not validate or execute targets"
            );
          }

          if (state.external_execution_connected !== false) {
            errors.push(
              "Candidate Validation Planner v0.1 must not connect external execution"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Candidate Validation conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Candidate Validation must preserve Cosmos continuation"
            );
          }

          const targetIds = new Set();

          for (const target of targets) {
            if (!target.validation_target_id ||
                !target.abstract_subject ||
                !target.candidate_label ||
                !target.candidate_class) {
              errors.push(
                "Candidate Validation target missing identity/context"
              );
              continue;
            }

            if (targetIds.has(target.validation_target_id)) {
              errors.push(
                `Duplicate validation_target_id ${target.validation_target_id}`
              );
            }
            targetIds.add(target.validation_target_id);

            if (target.epistemic_status !== "provisional_candidate" ||
                target.validation_status !== "not_started" ||
                target.executable !== false ||
                target.knowledge_status !== "not_admitted") {
              errors.push(
                `Validation target ${target.validation_target_id} crossed epistemic/execution boundary`
              );
            }

            if (!Array.isArray(target.source_candidate_ids) ||
                target.source_candidate_ids.length < 1 ||
                !Array.isArray(target.parent_acquisition_plan_ids) ||
                target.parent_acquisition_plan_ids.length < 1 ||
                !Array.isArray(target.parent_discovery_target_ids) ||
                target.parent_discovery_target_ids.length < 1) {
              errors.push(
                `Validation target ${target.validation_target_id} missing merged lineage`
              );
            }

            if (!Array.isArray(target.original_target_types) ||
                target.original_target_types.length < 1 ||
                !Array.isArray(target.proposed_investigation_types) ||
                target.proposed_investigation_types.length < 1) {
              errors.push(
                `Validation target ${target.validation_target_id} missing investigation intents`
              );
            }

            if (!target.validation_objective ||
                !Array.isArray(target.validation_requirements) ||
                target.validation_requirements.length < 1) {
              errors.push(
                `Validation target ${target.validation_target_id} missing validation contract`
              );
            }

            const contract = target.evidence_contract || {};

            if (Number(contract.minimum_independent_sources) < 1 ||
                contract.preferred_source_characteristic !==
                  "primary_or_authoritative_where_available" ||
                contract.contradiction_capture_required !== true ||
                contract.provenance_required !== true ||
                contract.source_date_required !== true ||
                contract.relationship_direction_required_where_applicable !== true ||
                !Array.isArray(contract.disposition_options) ||
                contract.disposition_options.length < 4) {
              errors.push(
                `Validation target ${target.validation_target_id} evidence contract invalid`
              );
            }

            if (!target.post_validation_fanout ||
                !Array.isArray(target.post_validation_fanout.if_supported) ||
                !Array.isArray(target.post_validation_fanout.if_partially_supported) ||
                !Array.isArray(target.post_validation_fanout.if_contradicted) ||
                !Array.isArray(target.post_validation_fanout.if_insufficient_evidence)) {
              errors.push(
                `Validation target ${target.validation_target_id} post-validation fanout invalid`
              );
            }

            if (!target.lineage ||
                !Array.isArray(target.lineage.source_candidate_ids) ||
                !Array.isArray(target.lineage.acquisition_plan_ids) ||
                !Array.isArray(target.lineage.discovery_target_ids) ||
                !Array.isArray(target.lineage.source_lineage)) {
              errors.push(
                `Validation target ${target.validation_target_id} lineage block invalid`
              );
            }
          }

          const expectedLabels = [
            "AI compute infrastructure",
            "semiconductors and accelerators",
            "data centers",
            "electricity demand",
            "cooling systems",
            "networking infrastructure",
            "software/model infrastructure"
          ];

          const labels = new Set(
            targets.map(x => String(x.candidate_label || ""))
          );

          for (const expected of expectedLabels) {
            if (!labels.has(expected)) {
              errors.push(
                `Candidate Validation Planner missing expected unique target: ${expected}`
              );
            }
          }

          const dataCenters = targets.find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            errors.push(
              "Candidate Validation Planner did not resolve unique data centers target"
            );
          } else {
            const types = new Set(dataCenters.original_target_types || []);

            for (const expectedType of [
              "identify_components",
              "identify_materials",
              "identify_capacity"
            ]) {
              if (!types.has(expectedType)) {
                errors.push(
                  `Data centers validation target lost parent intent ${expectedType}`
                );
              }
            }

            if (Number(dataCenters.merged_from_candidate_count) !== 3) {
              errors.push(
                `Data centers validation target should merge 3 candidates; got ${dataCenters.merged_from_candidate_count}`
              );
            }
          }

          for (let i = 0; i < queue.length; i += 1) {
            const row = queue[i];

            if (Number(row.evidence_rank) !== i + 1) {
              errors.push(
                `Future evidence queue rank invalid at index ${i}`
              );
            }

            if (!targetIds.has(row.validation_target_id)) {
              errors.push(
                `Future evidence queue references unknown validation target ${row.validation_target_id}`
              );
            }

            if (row.execution_status !== "not_connected" ||
                row.validation_status !== "not_started") {
              errors.push(
                `Future evidence queue ${row.validation_target_id} crossed execution/validation boundary`
              );
            }

            if (!row.validation_objective ||
                !Array.isArray(row.validation_requirements) ||
                !row.evidence_contract ||
                !Array.isArray(row.proposed_investigation_types)) {
              errors.push(
                `Future evidence queue ${row.validation_target_id} missing evidence-ready planning fields`
              );
            }
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.validates_candidates !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.promotes_candidates_to_facts !== false ||
              safeguards.duplicate_semantic_candidates_consolidated !== true ||
              safeguards.parent_lineage_preserved !== true ||
              safeguards.investigation_intents_preserved !== true ||
              safeguards.evidence_required_before_validation !== true ||
              safeguards.validation_required_before_execution !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Candidate Validation Planner safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Candidate Validation Planner v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Candidate Validation Planner v0.1 isolated test passed.");
          console.log(`Status:                ${payload.status}`);
          console.log(`Raw candidates:        ${state.raw_candidate_count}`);
          console.log(`Unique targets:        ${state.unique_validation_target_count}`);
          console.log(`Duplicates collapsed:  ${state.duplicate_candidate_count}`);
          console.log(`Future evidence queue: ${queue.length}`);
          console.log(`Validated:             ${state.validated_target_count}`);
          console.log(`Executable:            ${state.executable_target_count}`);
          console.log(
            "Semantic consolidation, parent-lineage preservation, evidence contracts and no-validation/no-execution safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Evidence Strategy v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp scripts/cosmos_decomposition.cjs \
            "$TEST_DIR/scripts/cosmos_decomposition.cjs"
          cp scripts/cosmos_candidate_validation.cjs \
            "$TEST_DIR/scripts/cosmos_candidate_validation.cjs"
          cp scripts/cosmos_evidence_strategy.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_strategy.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_evidence_strategy_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json

            node scripts/cosmos_decomposition.cjs \
              --input knowledge/cosmos/applicability-test-v0.1.json \
              --out knowledge/cosmos/decomposition-test-v0.1.json

            node scripts/cosmos_candidate_validation.cjs \
              --input knowledge/cosmos/decomposition-test-v0.1.json \
              --out knowledge/cosmos/candidate-validation-test-v0.1.json

            node scripts/cosmos_evidence_strategy.cjs \
              --input knowledge/cosmos/candidate-validation-test-v0.1.json \
              --out knowledge/cosmos/evidence-strategy-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/evidence-strategy-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Evidence Strategy did not create evidence-strategy-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Evidence Strategy schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "evidence_strategy_resolved") {
            errors.push(
              `Cosmos Evidence Strategy failed: ${payload.status}`
            );
          }

          if (payload.source_candidate_validation?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Evidence Strategy must consume Candidate Validation v0.1; got ${payload.source_candidate_validation?.schema_version}`
            );
          }

          const state = payload.evidence_state || {};
          const tasks = payload.evidence_tasks || [];
          const queue = payload.executor_queue || [];

          if (!Array.isArray(tasks) || tasks.length < 1) {
            errors.push("Cosmos Evidence Strategy evidence_tasks[] missing or empty");
          }

          if (!Array.isArray(queue) || queue.length < 1) {
            errors.push("Cosmos Evidence Strategy executor_queue[] missing or empty");
          }

          if (tasks.length !== queue.length) {
            errors.push(
              `Evidence Strategy task/executor queue mismatch: ${tasks.length} vs ${queue.length}`
            );
          }

          if (Number(state.validation_target_count) !== 7) {
            errors.push(
              `Controlled Evidence Strategy test expected 7 validation targets; got ${state.validation_target_count}`
            );
          }

          if (Number(state.evidence_task_count) !== 7) {
            errors.push(
              `Controlled Evidence Strategy test expected 7 evidence tasks; got ${state.evidence_task_count}`
            );
          }

          if (Number(state.evidence_collected_count) !== 0 ||
              Number(state.validation_decision_count) !== 0 ||
              Number(state.knowledge_admission_count) !== 0) {
            errors.push(
              "Cosmos Evidence Strategy v0.1 must not collect evidence, validate, or admit knowledge"
            );
          }

          if (state.external_execution_connected !== false) {
            errors.push(
              "Cosmos Evidence Strategy v0.1 must not connect external execution"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Evidence Strategy conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Evidence Strategy must preserve Cosmos continuation"
            );
          }

          const taskIds = new Set();
          const validationTargetIds = new Set();

          for (const task of tasks) {
            if (!task.evidence_task_id ||
                !task.validation_target_id ||
                !task.abstract_subject ||
                !task.candidate_label ||
                !task.candidate_class) {
              errors.push(
                "Cosmos Evidence Strategy task missing identity/context"
              );
              continue;
            }

            if (taskIds.has(task.evidence_task_id)) {
              errors.push(
                `Duplicate evidence_task_id ${task.evidence_task_id}`
              );
            }
            taskIds.add(task.evidence_task_id);
            validationTargetIds.add(task.validation_target_id);

            if (!task.validation_objective ||
                !Array.isArray(task.evidence_questions) ||
                task.evidence_questions.length < 1 ||
                !Array.isArray(task.query_intents) ||
                task.query_intents.length < 1) {
              errors.push(
                `Evidence task ${task.evidence_task_id} missing evidence questions/query intents`
              );
            }

            if (!Array.isArray(task.source_strategy) ||
                task.source_strategy.length < 1) {
              errors.push(
                `Evidence task ${task.evidence_task_id} has no source_strategy`
              );
            } else {
              let previousAuthority = Infinity;

              for (const source of task.source_strategy) {
                const score = Number(source.authority_score);

                if (!source.source_type ||
                    !Number.isFinite(score) ||
                    !["primary", "secondary", "tertiary"].includes(source.priority)) {
                  errors.push(
                    `Evidence task ${task.evidence_task_id} has invalid source strategy`
                  );
                  continue;
                }

                if (score > previousAuthority + 1e-9) {
                  errors.push(
                    `Evidence task ${task.evidence_task_id} source strategy is not authority-ranked`
                  );
                }

                previousAuthority = score;

                if (source.acquisition_status !== "planned" ||
                    source.execution_adapter !== "unassigned") {
                  errors.push(
                    `Evidence task ${task.evidence_task_id} prematurely executes a source`
                  );
                }
              }
            }

            const sufficiency = task.sufficiency_contract || {};

            if (Number(sufficiency.minimum_total_sources) !== 2 ||
                Number(sufficiency.minimum_independent_sources) !== 2 ||
                sufficiency.direct_primary_source_preferred !== true ||
                sufficiency.contradiction_search_required !== true ||
                sufficiency.provenance_required !== true ||
                sufficiency.source_date_required !== true ||
                sufficiency.source_identity_required !== true ||
                sufficiency.extracted_fact_required !== true ||
                sufficiency.relationship_direction_required_where_applicable !== true ||
                sufficiency.geography_scope_required_where_applicable !== true ||
                sufficiency.temporal_scope_required_where_applicable !== true ||
                !sufficiency.single_source_exception) {
              errors.push(
                `Evidence task ${task.evidence_task_id} sufficiency contract invalid`
              );
            }

            const schema = task.evidence_record_schema || {};

            if (!Array.isArray(schema.required_fields) ||
                schema.required_fields.length < 10 ||
                !Array.isArray(schema.allowed_dispositions) ||
                !schema.allowed_dispositions.includes("supports") ||
                !schema.allowed_dispositions.includes("contradicts") ||
                !schema.allowed_dispositions.includes("insufficient")) {
              errors.push(
                `Evidence task ${task.evidence_task_id} evidence record schema invalid`
              );
            }

            const boundary = task.validation_boundary || {};

            if (boundary.evidence_collected !== false ||
                boundary.validation_decision_made !== false ||
                boundary.candidate_promoted !== false ||
                boundary.executable !== false ||
                boundary.knowledge_admitted !== false) {
              errors.push(
                `Evidence task ${task.evidence_task_id} crossed validation/knowledge boundary`
              );
            }

            if (!task.downstream_context ||
                !Array.isArray(task.downstream_context.original_target_types) ||
                !Array.isArray(task.downstream_context.proposed_investigation_types) ||
                !Array.isArray(task.downstream_context.if_supported_fanout) ||
                !Array.isArray(task.downstream_context.if_partially_supported_fanout)) {
              errors.push(
                `Evidence task ${task.evidence_task_id} downstream context invalid`
              );
            }

            if (!task.lineage ||
                !Array.isArray(task.lineage.source_candidate_ids) ||
                !Array.isArray(task.lineage.acquisition_plan_ids) ||
                !Array.isArray(task.lineage.discovery_target_ids)) {
              errors.push(
                `Evidence task ${task.evidence_task_id} lineage invalid`
              );
            }

            if (task.execution?.mode !== "planned_only" ||
                task.execution?.external_search_performed !== false ||
                task.execution?.executor_status !== "not_connected" ||
                Number(task.execution?.result_count) !== 0) {
              errors.push(
                `Evidence task ${task.evidence_task_id} execution boundary invalid`
              );
            }
          }

          if (taskIds.size !== tasks.length) {
            errors.push("Cosmos Evidence Strategy task IDs are not unique");
          }

          if (validationTargetIds.size !== tasks.length) {
            errors.push(
              "Cosmos Evidence Strategy must produce one task per unique validation target"
            );
          }

          const expectedLabels = [
            "AI compute infrastructure",
            "semiconductors and accelerators",
            "data centers",
            "electricity demand",
            "cooling systems",
            "networking infrastructure",
            "software/model infrastructure"
          ];

          const labels = new Set(
            tasks.map(x => String(x.candidate_label || ""))
          );

          for (const expected of expectedLabels) {
            if (!labels.has(expected)) {
              errors.push(
                `Cosmos Evidence Strategy missing expected target: ${expected}`
              );
            }
          }

          const dataCenters = tasks.find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            errors.push(
              "Cosmos Evidence Strategy did not produce data centers evidence task"
            );
          } else {
            if (!dataCenters.source_strategy.some(
              x => x.source_type === "official_project_or_operator_document" &&
                   Number(x.authority_score) === 100
            )) {
              errors.push(
                "Data centers evidence task missing highest-authority operator/project source"
              );
            }

            for (const expectedType of [
              "identify_components",
              "identify_materials",
              "identify_capacity"
            ]) {
              if (!(dataCenters.downstream_context?.original_target_types || [])
                    .includes(expectedType)) {
                errors.push(
                  `Data centers evidence task lost downstream intent ${expectedType}`
                );
              }
            }
          }

          for (let i = 0; i < queue.length; i += 1) {
            const row = queue[i];

            if (Number(row.execution_rank) !== i + 1) {
              errors.push(
                `Evidence executor queue rank invalid at index ${i}`
              );
            }

            if (!taskIds.has(row.evidence_task_id)) {
              errors.push(
                `Evidence executor queue references unknown task ${row.evidence_task_id}`
              );
            }

            if (row.execution_status !== "not_connected" ||
                row.validation_status !== "not_started") {
              errors.push(
                `Evidence executor queue ${row.evidence_task_id} crossed execution/validation boundary`
              );
            }

            if (!Array.isArray(row.query_intents) ||
                !Array.isArray(row.source_strategy) ||
                !row.sufficiency_contract) {
              errors.push(
                `Evidence executor queue ${row.evidence_task_id} missing executor planning fields`
              );
            }
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.collects_evidence !== false ||
              safeguards.validates_candidates !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.promotes_candidates_to_facts !== false ||
              safeguards.source_authority_ranked !== true ||
              safeguards.contradiction_search_required !== true ||
              safeguards.independent_corroboration_planned !== true ||
              safeguards.validation_required_before_execution !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Cosmos Evidence Strategy safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Evidence Strategy v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Evidence Strategy v0.1 isolated test passed.");
          console.log(`Status:              ${payload.status}`);
          console.log(`Validation targets:  ${state.validation_target_count}`);
          console.log(`Evidence tasks:      ${state.evidence_task_count}`);
          console.log(`Executor queue:      ${queue.length}`);
          console.log(`Evidence collected:  ${state.evidence_collected_count}`);
          console.log(`Validation decisions:${state.validation_decision_count}`);
          console.log(`Knowledge admitted:  ${state.knowledge_admission_count}`);
          console.log(
            "Authority ranking, 2-source corroboration planning, contradiction search, lineage and no-execution safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Evidence Executor v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp scripts/cosmos_decomposition.cjs \
            "$TEST_DIR/scripts/cosmos_decomposition.cjs"
          cp scripts/cosmos_candidate_validation.cjs \
            "$TEST_DIR/scripts/cosmos_candidate_validation.cjs"
          cp scripts/cosmos_evidence_strategy.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_strategy.cjs"
          cp scripts/cosmos_evidence_executor.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_executor.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_evidence_executor_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json

            node scripts/cosmos_decomposition.cjs \
              --input knowledge/cosmos/applicability-test-v0.1.json \
              --out knowledge/cosmos/decomposition-test-v0.1.json

            node scripts/cosmos_candidate_validation.cjs \
              --input knowledge/cosmos/decomposition-test-v0.1.json \
              --out knowledge/cosmos/candidate-validation-test-v0.1.json

            node scripts/cosmos_evidence_strategy.cjs \
              --input knowledge/cosmos/candidate-validation-test-v0.1.json \
              --out knowledge/cosmos/evidence-strategy-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const strategyFile = path.join(
            root,
            "knowledge/cosmos/evidence-strategy-test-v0.1.json"
          );
          const strategy = JSON.parse(fs.readFileSync(strategyFile, "utf8"));

          const dataCenters = (strategy.evidence_tasks || []).find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            throw new Error(
              "Controlled Evidence Executor fixture could not locate the data centers evidence task."
            );
          }

          const adapter = {
            schema_version: "0.1-fixture",
            generated_at: new Date().toISOString(),
            status: "adapter_results_ready",
            adapter_name: "cosmos_preflight_mock_adapter",
            adapter_mode: "deterministic_fixture",
            results: [
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://operator/ai-data-center-1",
                source_type: "official_project_or_operator_document",
                source_title: "Operator AI Infrastructure Requirement",
                source_publisher_or_owner: "Fixture Operator A",
                source_date_or_event_date: "2026-01-15",
                retrieved_at: new Date().toISOString(),
                extracted_fact: "The operator identifies data-center infrastructure as a direct platform for AI compute workloads.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 100,
                independence_group: "operator-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" official",
                source_rank: 1
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://government/ai-data-center-2",
                source_type: "regulatory_or_government_document",
                source_title: "Government AI Compute Infrastructure Assessment",
                source_publisher_or_owner: "Fixture Government Agency",
                source_date_or_event_date: "2026-02-20",
                retrieved_at: new Date().toISOString(),
                extracted_fact: "The assessment describes data centers as infrastructure supporting large-scale AI training and inference.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 96,
                independence_group: "government-a",
                geography_scope: "United States",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" infrastructure",
                source_rank: 2
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://research/ai-edge-counterexample",
                source_type: "industry_body_or_research_institution",
                source_title: "Edge AI Deployment Note",
                source_publisher_or_owner: "Fixture Research Institution",
                source_date_or_event_date: "2026-03-10",
                retrieved_at: new Date().toISOString(),
                extracted_fact: "Some AI inference workloads can execute on distributed edge devices without relying on centralized data centers.",
                supports_or_contradicts: "contradicts",
                directness: "direct",
                authority_score: 82,
                independence_group: "research-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" limitation",
                source_rank: 3
              },
              {
                evidence_task_id: "unknown_fixture_task",
                source_url_or_identifier: "fixture://orphan/result",
                source_title: "Unmapped Adapter Result",
                extracted_fact: "This result intentionally exercises orphan-result preservation.",
                supports_or_contradicts: "context_only",
                directness: "contextual",
                authority_score: 50,
                independence_group: "orphan-fixture"
              }
            ]
          };

          fs.writeFileSync(
            path.join(root, "adapter-results-v0.1.json"),
            JSON.stringify(adapter, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"
            node scripts/cosmos_evidence_executor.cjs \
              --strategy knowledge/cosmos/evidence-strategy-test-v0.1.json \
              --adapter-results adapter-results-v0.1.json \
              --out knowledge/cosmos/evidence-executor-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/evidence-executor-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Evidence Executor did not create evidence-executor-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Evidence Executor schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "evidence_execution_normalized") {
            errors.push(
              `Cosmos Evidence Executor failed: ${payload.status}`
            );
          }

          if (payload.source_evidence_strategy?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Evidence Executor must consume Evidence Strategy v0.1; got ${payload.source_evidence_strategy?.schema_version}`
            );
          }

          if (payload.source_adapter?.adapter_name !==
              "cosmos_preflight_mock_adapter" ||
              payload.source_adapter?.adapter_mode !==
              "deterministic_fixture") {
            errors.push(
              "Cosmos Evidence Executor did not preserve adapter identity/mode"
            );
          }

          const state = payload.execution_state || {};
          const records = payload.evidence_records || [];
          const taskExecution = payload.task_execution || [];
          const orphans = payload.orphan_results || [];
          const validationQueue = payload.validation_queue || [];

          if (Number(state.evidence_task_count) !== 7) {
            errors.push(
              `Controlled Evidence Executor expected 7 evidence tasks; got ${state.evidence_task_count}`
            );
          }

          if (Number(state.adapter_result_count) !== 4) {
            errors.push(
              `Controlled Evidence Executor expected 4 adapter results; got ${state.adapter_result_count}`
            );
          }

          if (Number(state.normalized_evidence_record_count) !== 3) {
            errors.push(
              `Controlled Evidence Executor expected 3 normalized evidence records; got ${state.normalized_evidence_record_count}`
            );
          }

          if (Number(state.orphan_result_count) !== 1) {
            errors.push(
              `Controlled Evidence Executor expected 1 orphan result; got ${state.orphan_result_count}`
            );
          }

          if (Number(state.validation_decision_count) !== 0 ||
              Number(state.knowledge_admission_count) !== 0) {
            errors.push(
              "Cosmos Evidence Executor v0.1 must not make validation or knowledge-admission decisions"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Evidence Executor conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Evidence Executor must preserve Cosmos continuation"
            );
          }

          if (!Array.isArray(records) || records.length !== 3) {
            errors.push(
              "Cosmos Evidence Executor normalized evidence_records[] invalid"
            );
          }

          const recordIds = new Set();

          for (const record of records) {
            if (!record.evidence_record_id ||
                !record.evidence_task_id ||
                !record.validation_target_id) {
              errors.push(
                "Cosmos Evidence Executor record missing identity/lineage"
              );
              continue;
            }

            if (recordIds.has(record.evidence_record_id)) {
              errors.push(
                `Duplicate evidence_record_id ${record.evidence_record_id}`
              );
            }
            recordIds.add(record.evidence_record_id);

            if (!record.source_url_or_identifier ||
                !record.source_title ||
                !record.extracted_fact ||
                !record.independence_group) {
              errors.push(
                `Evidence record ${record.evidence_record_id} missing source/evidence fields`
              );
            }

            if (![
              "supports",
              "partially_supports",
              "contradicts",
              "context_only",
              "insufficient"
            ].includes(record.supports_or_contradicts)) {
              errors.push(
                `Evidence record ${record.evidence_record_id} has invalid disposition`
              );
            }

            if (record.epistemic_status !== "collected_evidence" ||
                record.validation_status !== "not_evaluated" ||
                record.knowledge_status !== "not_admitted") {
              errors.push(
                `Evidence record ${record.evidence_record_id} crossed validation/knowledge boundary`
              );
            }

            if (record.record_quality?.has_source_identity !== true ||
                record.record_quality?.has_extracted_fact !== true ||
                record.record_quality?.has_source_date !== true ||
                record.record_quality?.has_publisher !== true ||
                record.record_quality?.has_independence_group !== true) {
              errors.push(
                `Evidence record ${record.evidence_record_id} record-quality flags invalid`
              );
            }
          }

          const contradictionRecords = records.filter(
            r => r.supports_or_contradicts === "contradicts"
          );

          if (contradictionRecords.length !== 1) {
            errors.push(
              `Controlled Evidence Executor expected exactly 1 contradictory evidence record; got ${contradictionRecords.length}`
            );
          }

          const dataCentersExecution = taskExecution.find(
            row => Number(row.collected_record_count) === 3
          );

          if (!dataCentersExecution) {
            errors.push(
              "Cosmos Evidence Executor did not resolve the data-centers task execution summary"
            );
          } else {
            if (Number(dataCentersExecution.supporting_record_count) !== 2 ||
                Number(dataCentersExecution.contradiction_record_count) !== 1 ||
                Number(dataCentersExecution.direct_high_authority_record_count) !== 2 ||
                Number(dataCentersExecution.independent_source_group_count) !== 3) {
              errors.push(
                "Cosmos Evidence Executor data-centers evidence counts invalid"
              );
            }

            const sufficiency =
              dataCentersExecution.sufficiency_observation || {};

            if (sufficiency.total_source_threshold_met !== true ||
                sufficiency.independent_source_threshold_met !== true ||
                sufficiency.direct_primary_preference_met !== true ||
                sufficiency.contradiction_search_observed !== true) {
              errors.push(
                "Cosmos Evidence Executor sufficiency observations invalid"
              );
            }

            if (dataCentersExecution.execution_status !== "evidence_collected" ||
                dataCentersExecution.validation_status !== "not_started" ||
                dataCentersExecution.knowledge_admission_status !== "not_started") {
              errors.push(
                "Cosmos Evidence Executor task crossed validation/knowledge boundary"
              );
            }
          }

          if (!Array.isArray(taskExecution) ||
              taskExecution.length !== 7) {
            errors.push(
              `Cosmos Evidence Executor expected 7 task_execution rows; got ${taskExecution.length}`
            );
          }

          const emptyTasks = taskExecution.filter(
            row => Number(row.collected_record_count) === 0
          );

          if (emptyTasks.length !== 6 ||
              emptyTasks.some(
                row => row.execution_status !== "no_evidence_returned"
              )) {
            errors.push(
              "Cosmos Evidence Executor no-evidence task handling invalid"
            );
          }

          if (!Array.isArray(validationQueue) ||
              validationQueue.length !== 7) {
            errors.push(
              "Cosmos Evidence Executor validation_queue[] invalid"
            );
          }

          for (let i = 0; i < validationQueue.length; i += 1) {
            const row = validationQueue[i];

            if (Number(row.validation_rank) !== i + 1) {
              errors.push(
                `Evidence Executor validation queue rank invalid at index ${i}`
              );
            }

            if (!Array.isArray(row.evidence_record_ids)) {
              errors.push(
                `Evidence Executor validation queue ${row.evidence_task_id} missing evidence_record_ids[]`
              );
            }

            if (row.validation_status !== "not_started" ||
                row.knowledge_admission_status !== "not_started") {
              errors.push(
                `Evidence Executor validation queue ${row.evidence_task_id} crossed validation boundary`
              );
            }
          }

          if (!Array.isArray(orphans) ||
              orphans.length !== 1 ||
              orphans[0].reason !== "unknown_evidence_task_id") {
            errors.push(
              "Cosmos Evidence Executor orphan-result preservation invalid"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.adapter_results_are_treated_as_unvalidated_input !== true ||
              safeguards.invents_evidence !== false ||
              safeguards.validates_candidates !== false ||
              safeguards.promotes_candidates_to_facts !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.preserves_contradictory_evidence !== true ||
              safeguards.preserves_orphan_adapter_results !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.validation_required_before_knowledge_admission !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Cosmos Evidence Executor safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Evidence Executor v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Evidence Executor v0.1 isolated test passed.");
          console.log(`Status:                 ${payload.status}`);
          console.log(`Evidence tasks:         ${state.evidence_task_count}`);
          console.log(`Adapter results:        ${state.adapter_result_count}`);
          console.log(`Normalized records:     ${state.normalized_evidence_record_count}`);
          console.log(`Contradictions retained:${contradictionRecords.length}`);
          console.log(`Orphan results retained:${state.orphan_result_count}`);
          console.log(`Validation decisions:   ${state.validation_decision_count}`);
          console.log(`Knowledge admitted:     ${state.knowledge_admission_count}`);
          console.log(
            "Evidence normalization, independent-source counting, contradiction preservation, orphan preservation and validation-before-knowledge safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Evidence Validator v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_core.cjs "$TEST_DIR/scripts/cosmos_core.cjs"
          cp scripts/cosmos_attention.cjs "$TEST_DIR/scripts/cosmos_attention.cjs"
          cp scripts/cosmos_consequence.cjs "$TEST_DIR/scripts/cosmos_consequence.cjs"
          cp scripts/cosmos_discovery.cjs "$TEST_DIR/scripts/cosmos_discovery.cjs"
          cp scripts/cosmos_acquisition.cjs "$TEST_DIR/scripts/cosmos_acquisition.cjs"
          cp scripts/cosmos_acquisition_applicability.cjs \
            "$TEST_DIR/scripts/cosmos_acquisition_applicability.cjs"
          cp scripts/cosmos_decomposition.cjs \
            "$TEST_DIR/scripts/cosmos_decomposition.cjs"
          cp scripts/cosmos_candidate_validation.cjs \
            "$TEST_DIR/scripts/cosmos_candidate_validation.cjs"
          cp scripts/cosmos_evidence_strategy.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_strategy.cjs"
          cp scripts/cosmos_evidence_executor.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_executor.cjs"
          cp scripts/cosmos_evidence_validator.cjs \
            "$TEST_DIR/scripts/cosmos_evidence_validator.cjs"
          cp knowledge/cosmos/cosmos-state-current.json \
            "$TEST_DIR/knowledge/cosmos/cosmos-state-current.json"

          cat > "$TEST_DIR/core-input.json" <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "cosmos_evidence_validator_v0.1_preflight"
            }
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_core.cjs \
              --input core-input.json \
              --out knowledge/cosmos/core-test-v0.1.json

            node scripts/cosmos_attention.cjs \
              --input knowledge/cosmos/core-test-v0.1.json \
              --out knowledge/cosmos/attention-test-v0.2.json

            node scripts/cosmos_consequence.cjs \
              --input knowledge/cosmos/attention-test-v0.2.json \
              --out knowledge/cosmos/consequence-test-v0.1.json

            node scripts/cosmos_discovery.cjs \
              --input knowledge/cosmos/consequence-test-v0.1.json \
              --out knowledge/cosmos/discovery-test-v0.1.json

            node scripts/cosmos_acquisition.cjs \
              --input knowledge/cosmos/discovery-test-v0.1.json \
              --out knowledge/cosmos/acquisition-test-v0.1.json

            node scripts/cosmos_acquisition_applicability.cjs \
              --input knowledge/cosmos/acquisition-test-v0.1.json \
              --out knowledge/cosmos/applicability-test-v0.1.json

            node scripts/cosmos_decomposition.cjs \
              --input knowledge/cosmos/applicability-test-v0.1.json \
              --out knowledge/cosmos/decomposition-test-v0.1.json

            node scripts/cosmos_candidate_validation.cjs \
              --input knowledge/cosmos/decomposition-test-v0.1.json \
              --out knowledge/cosmos/candidate-validation-test-v0.1.json

            node scripts/cosmos_evidence_strategy.cjs \
              --input knowledge/cosmos/candidate-validation-test-v0.1.json \
              --out knowledge/cosmos/evidence-strategy-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const strategy = JSON.parse(
            fs.readFileSync(
              path.join(
                root,
                "knowledge/cosmos/evidence-strategy-test-v0.1.json"
              ),
              "utf8"
            )
          );

          const dataCenters = (strategy.evidence_tasks || []).find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            throw new Error(
              "Controlled Evidence Validator fixture could not locate the data centers evidence task."
            );
          }

          const now = new Date().toISOString();

          const adapter = {
            schema_version: "0.1-fixture",
            generated_at: now,
            status: "adapter_results_ready",
            adapter_name: "cosmos_preflight_mock_adapter",
            adapter_mode: "deterministic_fixture",
            results: [
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://operator/ai-data-center-1",
                source_type: "official_project_or_operator_document",
                source_title: "Operator AI Infrastructure Requirement",
                source_publisher_or_owner: "Fixture Operator A",
                source_date_or_event_date: "2026-01-15",
                retrieved_at: now,
                extracted_fact: "The operator identifies data-center infrastructure as a direct platform for AI compute workloads.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 100,
                independence_group: "operator-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" official",
                source_rank: 1
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://government/ai-data-center-2",
                source_type: "regulatory_or_government_document",
                source_title: "Government AI Compute Infrastructure Assessment",
                source_publisher_or_owner: "Fixture Government Agency",
                source_date_or_event_date: "2026-02-20",
                retrieved_at: now,
                extracted_fact: "The assessment describes data centers as infrastructure supporting large-scale AI training and inference.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 96,
                independence_group: "government-a",
                geography_scope: "United States",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" infrastructure",
                source_rank: 2
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://research/ai-edge-counterexample",
                source_type: "industry_body_or_research_institution",
                source_title: "Edge AI Deployment Note",
                source_publisher_or_owner: "Fixture Research Institution",
                source_date_or_event_date: "2026-03-10",
                retrieved_at: now,
                extracted_fact: "Some AI inference workloads can execute on distributed edge devices without relying on centralized data centers.",
                supports_or_contradicts: "contradicts",
                directness: "direct",
                authority_score: 82,
                independence_group: "research-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" limitation",
                source_rank: 3
              }
            ]
          };

          fs.writeFileSync(
            path.join(root, "adapter-results-v0.1.json"),
            JSON.stringify(adapter, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"

            node scripts/cosmos_evidence_executor.cjs \
              --strategy knowledge/cosmos/evidence-strategy-test-v0.1.json \
              --adapter-results adapter-results-v0.1.json \
              --out knowledge/cosmos/evidence-executor-test-v0.1.json

            node scripts/cosmos_evidence_validator.cjs \
              --input knowledge/cosmos/evidence-executor-test-v0.1.json \
              --out knowledge/cosmos/evidence-validator-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/evidence-validator-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Evidence Validator did not create evidence-validator-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Evidence Validator schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "evidence_validation_resolved") {
            errors.push(
              `Cosmos Evidence Validator failed: ${payload.status}`
            );
          }

          if (payload.source_evidence_executor?.schema_version !== "0.1") {
            errors.push(
              `Cosmos Evidence Validator must consume Evidence Executor v0.1; got ${payload.source_evidence_executor?.schema_version}`
            );
          }

          const state = payload.validation_state || {};
          const validations = payload.validations || [];
          const admissionQueue = payload.knowledge_admission_queue || [];

          if (!Array.isArray(validations) || validations.length !== 7) {
            errors.push(
              `Controlled Evidence Validator expected 7 validations; got ${validations.length}`
            );
          }

          if (Number(state.validation_target_count) !== 7 ||
              Number(state.completed_validation_count) !== 7) {
            errors.push(
              "Cosmos Evidence Validator validation counts invalid"
            );
          }

          if (Number(state.knowledge_admission_count) !== 0) {
            errors.push(
              "Cosmos Evidence Validator v0.1 must not admit knowledge"
            );
          }

          if (state.conceptual_distance_limit !== null) {
            errors.push(
              "Cosmos Evidence Validator conceptual distance must remain unlimited/null"
            );
          }

          if (state.continuation_possible !== true) {
            errors.push(
              "Cosmos Evidence Validator must preserve Cosmos continuation"
            );
          }

          const allowed = new Set([
            "supported",
            "partially_supported",
            "contradicted",
            "unresolved",
            "insufficient_evidence"
          ]);

          for (const row of validations) {
            if (!row.evidence_validation_id ||
                !row.evidence_task_id ||
                !row.validation_target_id ||
                !allowed.has(row.disposition)) {
              errors.push(
                "Cosmos Evidence Validator row missing identity/disposition"
              );
              continue;
            }

            if (!Number.isFinite(Number(row.confidence_score)) ||
                Number(row.confidence_score) < 0 ||
                Number(row.confidence_score) > 100 ||
                !["very_high","high","moderate","low","very_low"]
                  .includes(row.confidence_band)) {
              errors.push(
                `Evidence validation ${row.evidence_validation_id} confidence invalid`
              );
            }

            if (!Array.isArray(row.evidence_record_ids) ||
                !Array.isArray(row.record_assessments)) {
              errors.push(
                `Evidence validation ${row.evidence_validation_id} evidence lineage invalid`
              );
            }

            if (row.validation_status !== "completed" ||
                row.knowledge_admission_status !== "not_started") {
              errors.push(
                `Evidence validation ${row.evidence_validation_id} crossed knowledge-admission boundary`
              );
            }

            if (!String(row.epistemic_rule || "").includes(
              "only Knowledge Admission"
            )) {
              errors.push(
                `Evidence validation ${row.evidence_validation_id} epistemic safeguard missing`
              );
            }
          }

          const dataCenters = validations.find(
            row => Number(row.evidence_record_count) === 3
          );

          if (!dataCenters) {
            errors.push(
              "Cosmos Evidence Validator did not resolve the data-centers validation"
            );
          } else {
            if (dataCenters.disposition !== "partially_supported") {
              errors.push(
                `Controlled Data Centers validation expected partially_supported; got ${dataCenters.disposition}`
              );
            }

            if (Number(dataCenters.evidence_balance?.independent_source_groups) !== 3 ||
                Number(dataCenters.evidence_balance?.direct_high_authority_records) !== 2) {
              errors.push(
                "Controlled Data Centers evidence-balance counts invalid"
              );
            }

            if (!(Number(dataCenters.evidence_balance?.weighted_support) >
                  Number(dataCenters.evidence_balance?.weighted_contradiction))) {
              errors.push(
                "Controlled Data Centers weighted support should exceed weighted contradiction"
              );
            }

            if (dataCenters.sufficiency_observation?.total_source_threshold_met !== true ||
                dataCenters.sufficiency_observation?.independent_source_threshold_met !== true ||
                dataCenters.sufficiency_observation?.direct_primary_preference_met !== true ||
                dataCenters.sufficiency_observation?.contradiction_search_observed !== true) {
              errors.push(
                "Controlled Data Centers sufficiency observations invalid"
              );
            }

            if (dataCenters.executable_for_knowledge_admission !== true) {
              errors.push(
                "Partially supported Data Centers validation should be eligible for Knowledge Admission review"
              );
            }
          }

          const insufficient = validations.filter(
            row => row.disposition === "insufficient_evidence"
          );

          if (insufficient.length !== 6) {
            errors.push(
              `Controlled Evidence Validator expected 6 insufficient-evidence targets; got ${insufficient.length}`
            );
          }

          if (insufficient.some(
            row => row.executable_for_knowledge_admission !== false
          )) {
            errors.push(
              "Insufficient-evidence targets must not be eligible for Knowledge Admission"
            );
          }

          if (!Array.isArray(admissionQueue) ||
              admissionQueue.length !== 1) {
            errors.push(
              `Controlled Evidence Validator expected exactly 1 Knowledge Admission queue item; got ${admissionQueue.length}`
            );
          } else {
            const row = admissionQueue[0];

            if (row.disposition !== "partially_supported" ||
                row.admission_status !== "not_started" ||
                !Array.isArray(row.evidence_record_ids) ||
                row.evidence_record_ids.length !== 3) {
              errors.push(
                "Controlled Knowledge Admission queue item invalid"
              );
            }
          }

          if (Number(state.by_disposition?.partially_supported || 0) !== 1 ||
              Number(state.by_disposition?.insufficient_evidence || 0) !== 6) {
            errors.push(
              "Cosmos Evidence Validator disposition summary invalid"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.invents_evidence !== false ||
              safeguards.preserves_contradictory_evidence !== true ||
              safeguards.validation_uses_authority_directness_independence_and_completeness !== true ||
              safeguards.validation_does_not_equal_knowledge_admission !== true ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.promotes_candidates_to_facts !== false ||
              safeguards.unresolved_or_insufficient_targets_not_admission_eligible !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Cosmos Evidence Validator safeguards invalid");
          }

          if (errors.length) {
            console.error("\nCosmos Evidence Validator v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Evidence Validator v0.1 isolated test passed.");
          console.log(`Status:                 ${payload.status}`);
          console.log(`Validations:            ${validations.length}`);
          console.log(`Partially supported:    ${state.by_disposition?.partially_supported || 0}`);
          console.log(`Insufficient evidence:  ${state.by_disposition?.insufficient_evidence || 0}`);
          console.log(`Admission queue:        ${admissionQueue.length}`);
          console.log(`Knowledge admitted:     ${state.knowledge_admission_count}`);
          console.log(
            "Authority/directness/independence weighting, contradiction-aware validation, insufficiency handling and validation-before-admission safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Knowledge Admission v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_knowledge_admission.cjs \
            "$TEST_DIR/scripts/cosmos_knowledge_admission.cjs"

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const partiallySupported = {
            evidence_validation_id: "evidence_validation_5ca25c15",
            evidence_task_id: "evidence_task_115f3b3e",
            validation_target_id: "validation_3642f09d",
            evidence_record_ids: [
              "evidence_d0143201",
              "evidence_6cccfc61",
              "evidence_65ed10af"
            ],
            evidence_record_count: 3,
            disposition: "partially_supported",
            confidence_score: 76,
            confidence_band: "high",
            rationale:
              "Supporting evidence outweighs contradiction, but contradiction remains material and prevents an unqualified supported conclusion.",
            evidence_balance: {
              weighted_support: 196,
              weighted_contradiction: 82,
              independent_source_groups: 3,
              direct_high_authority_records: 2
            },
            sufficiency_observation: {
              total_source_threshold_met: true,
              independent_source_threshold_met: true,
              direct_primary_preference_met: true,
              contradiction_search_observed: true
            },
            record_assessments: [],
            validation_status: "completed",
            knowledge_admission_status: "not_started",
            executable_for_knowledge_admission: true,
            epistemic_rule:
              "Validation may classify evidence, but only Knowledge Admission may create or update persistent Cosmos knowledge."
          };

          const insufficientIds = [
            ["evidence_validation_3bf0d71e", "evidence_task_a6c1f318", "validation_787b6815"],
            ["evidence_validation_64afcb5a", "evidence_task_3e80975d", "validation_83169e60"],
            ["evidence_validation_932cf23f", "evidence_task_64771149", "validation_4600b8de"],
            ["evidence_validation_e319e364", "evidence_task_2f80881f", "validation_224b2a14"],
            ["evidence_validation_eb71dfb6", "evidence_task_e1e94d3d", "validation_e81fcda8"],
            ["evidence_validation_60545a0e", "evidence_task_39c6b18f", "validation_04b40a48"]
          ];

          const insufficient = insufficientIds.map(([validationId, taskId, targetId]) => ({
            evidence_validation_id: validationId,
            evidence_task_id: taskId,
            validation_target_id: targetId,
            evidence_record_ids: [],
            evidence_record_count: 0,
            disposition: "insufficient_evidence",
            confidence_score: 0,
            confidence_band: "very_low",
            rationale: "No evidence records were collected for this target.",
            evidence_balance: {
              weighted_support: 0,
              weighted_contradiction: 0,
              independent_source_groups: 0,
              direct_high_authority_records: 0
            },
            sufficiency_observation: {
              total_source_threshold_met: false,
              independent_source_threshold_met: false,
              direct_primary_preference_met: false,
              contradiction_search_observed: false
            },
            record_assessments: [],
            validation_status: "completed",
            knowledge_admission_status: "not_started",
            executable_for_knowledge_admission: false,
            epistemic_rule:
              "Validation may classify evidence, but only Knowledge Admission may create or update persistent Cosmos knowledge."
          }));

          const fixture = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "evidence_validation_resolved",
            source_evidence_executor: {
              schema_version: "0.1",
              generated_at: new Date().toISOString(),
              status: "evidence_execution_normalized"
            },
            validation_state: {
              validation_target_count: 7,
              completed_validation_count: 7,
              knowledge_admission_count: 0,
              by_disposition: {
                insufficient_evidence: 6,
                partially_supported: 1
              },
              conceptual_distance_limit: null,
              continuation_possible: true,
              principle:
                "Cosmos may classify what the collected evidence justifies, but validation is not permanent knowledge admission."
            },
            validations: [
              insufficient[0],
              insufficient[1],
              partiallySupported,
              insufficient[2],
              insufficient[3],
              insufficient[4],
              insufficient[5]
            ],
            knowledge_admission_queue: [
              {
                admission_rank: 1,
                evidence_validation_id: partiallySupported.evidence_validation_id,
                evidence_task_id: partiallySupported.evidence_task_id,
                validation_target_id: partiallySupported.validation_target_id,
                disposition: "partially_supported",
                confidence_score: 76,
                confidence_band: "high",
                evidence_record_ids: partiallySupported.evidence_record_ids,
                admission_status: "not_started"
              }
            ]
          };

          fs.writeFileSync(
            path.join(root, "evidence-validator-fixture-v0.1.json"),
            JSON.stringify(fixture, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"
            node scripts/cosmos_knowledge_admission.cjs \
              --input evidence-validator-fixture-v0.1.json \
              --out knowledge/cosmos/knowledge-admission-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/knowledge-admission-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Knowledge Admission did not create knowledge-admission-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Knowledge Admission schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "knowledge_admission_resolved") {
            errors.push(
              `Cosmos Knowledge Admission failed: ${payload.status}`
            );
          }

          if (payload.source_evidence_validator?.schema_version !== "0.1" ||
              payload.source_evidence_validator?.status !== "evidence_validation_resolved") {
            errors.push(
              "Cosmos Knowledge Admission must consume Evidence Validator v0.1"
            );
          }

          const state = payload.admission_state || {};
          const admissions = payload.admissions || [];
          const graphQueue = payload.graph_write_queue || [];

          if (Number(state.validation_count) !== 7 ||
              Number(state.reviewed_count) !== 7 ||
              Number(state.validator_queue_count) !== 1) {
            errors.push(
              `Knowledge Admission counts invalid: validations=${state.validation_count}, reviewed=${state.reviewed_count}, validator_queue=${state.validator_queue_count}`
            );
          }

          if (Number(state.graph_write_eligible_count) !== 1) {
            errors.push(
              `Controlled Knowledge Admission expected exactly 1 graph-write-eligible item; got ${state.graph_write_eligible_count}`
            );
          }

          if (Number(state.graph_write_count) !== 0) {
            errors.push(
              "Knowledge Admission v0.1 must not perform graph writes"
            );
          }

          if (state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            errors.push(
              "Knowledge Admission did not preserve unlimited continuation"
            );
          }

          if (!Array.isArray(admissions) || admissions.length !== 7) {
            errors.push(
              `Controlled Knowledge Admission expected 7 admissions; got ${admissions.length}`
            );
          }

          const qualified = admissions.filter(
            row => row.decision === "admit_with_qualification"
          );

          const rejected = admissions.filter(
            row => row.decision === "reject_for_now"
          );

          if (qualified.length !== 1) {
            errors.push(
              `Expected exactly 1 admit_with_qualification; got ${qualified.length}`
            );
          }

          if (rejected.length !== 6) {
            errors.push(
              `Expected exactly 6 reject_for_now decisions; got ${rejected.length}`
            );
          }

          if (Number(state.by_decision?.admit_with_qualification || 0) !== 1 ||
              Number(state.by_decision?.reject_for_now || 0) !== 6) {
            errors.push(
              "Knowledge Admission decision summary invalid"
            );
          }

          if (qualified.length === 1) {
            const row = qualified[0];

            if (row.validation_disposition !== "partially_supported" ||
                Number(row.confidence_score) !== 76 ||
                row.confidence_band !== "high") {
              errors.push(
                "Qualified admission lost Evidence Validator disposition/confidence"
              );
            }

            if (row.admitted_claim_class !==
                  "validated_qualified_relationship" ||
                row.persistence_level !==
                  "persistent_qualified_knowledge") {
              errors.push(
                "Qualified admission claim/persistence class invalid"
              );
            }

            if (row.qualification?.contradiction_present !== true ||
                row.qualification?.must_preserve_contradiction !== true ||
                row.qualification?.confidence_must_be_preserved !== true ||
                row.qualification?.source_lineage_must_be_preserved !== true ||
                row.qualification?.temporal_scope_must_be_preserved !== true ||
                row.qualification?.geography_scope_must_be_preserved !== true) {
              errors.push(
                "Qualified admission failed contradiction/scope preservation contract"
              );
            }

            if (!Array.isArray(row.evidence_record_ids) ||
                row.evidence_record_ids.length !== 3) {
              errors.push(
                "Qualified admission did not preserve all evidence-record lineage"
              );
            }

            if (row.knowledge_write_contract?.direct_graph_write_performed !== false ||
                row.knowledge_write_contract?.requires_separate_graph_writer !== true ||
                row.knowledge_write_contract?.preserve_validation_disposition !== true ||
                row.knowledge_write_contract?.preserve_confidence !== true ||
                row.knowledge_write_contract?.preserve_evidence_lineage !== true ||
                row.knowledge_write_contract?.preserve_contradiction !== true ||
                row.knowledge_write_contract?.reversible !== true) {
              errors.push(
                "Qualified admission graph-write contract invalid"
              );
            }

            if (row.graph_write_eligibility !== true ||
                row.graph_write_status !== "not_started") {
              errors.push(
                "Qualified admission graph-write boundary invalid"
              );
            }
          }

          for (const row of rejected) {
            if (row.validation_disposition !== "insufficient_evidence" ||
                row.persistence_level !== "none" ||
                row.graph_write_eligibility !== false ||
                row.graph_write_status !== "not_started") {
              errors.push(
                `Insufficient-evidence admission ${row.knowledge_admission_id || "(unknown)"} improperly became graph eligible`
              );
            }
          }

          if (!Array.isArray(graphQueue) || graphQueue.length !== 1) {
            errors.push(
              `Controlled Knowledge Admission expected 1 graph_write_queue item; got ${graphQueue.length}`
            );
          } else {
            const row = graphQueue[0];

            if (row.graph_write_rank !== 1 ||
                row.admitted_claim_class !== "validated_qualified_relationship" ||
                row.validation_disposition !== "partially_supported" ||
                Number(row.confidence_score) !== 76 ||
                row.graph_write_status !== "not_started" ||
                !Array.isArray(row.evidence_record_ids) ||
                row.evidence_record_ids.length !== 3) {
              errors.push(
                "Knowledge Admission graph_write_queue item invalid"
              );
            }
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.admission_is_not_graph_mutation !== true ||
              safeguards.insufficient_or_unresolved_not_admitted !== true ||
              safeguards.partially_supported_requires_qualification !== true ||
              safeguards.contradictory_evidence_preserved !== true ||
              safeguards.confidence_preserved !== true ||
              safeguards.source_lineage_preserved !== true ||
              safeguards.graph_writes_are_reversible !== true ||
              safeguards.separate_graph_writer_required !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push(
              "Cosmos Knowledge Admission safeguards invalid"
            );
          }

          if (errors.length) {
            console.error("\nCosmos Knowledge Admission v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Knowledge Admission v0.1 isolated test passed.");
          console.log(`Status:                    ${payload.status}`);
          console.log(`Validations reviewed:      ${state.reviewed_count}`);
          console.log(`Admit with qualification:  ${state.by_decision?.admit_with_qualification || 0}`);
          console.log(`Reject for now:            ${state.by_decision?.reject_for_now || 0}`);
          console.log(`Graph-write eligible:      ${state.graph_write_eligible_count}`);
          console.log(`Graph writes performed:    ${state.graph_write_count}`);
          console.log(
            "Qualified admission, contradiction/confidence/lineage preservation, insufficient-evidence exclusion, reversible graph-write contract and separate-writer safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Graph Writer v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_graph_writer.cjs \
            "$TEST_DIR/scripts/cosmos_graph_writer.cjs"

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const qualified = {
            knowledge_admission_id: "knowledge_admission_graph_writer_fixture_qualified",
            evidence_validation_id: "evidence_validation_5ca25c15",
            evidence_task_id: "evidence_task_115f3b3e",
            validation_target_id: "validation_3642f09d",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_d0143201",
              "evidence_6cccfc61",
              "evidence_65ed10af"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const rejected = {
            knowledge_admission_id: "knowledge_admission_graph_writer_fixture_rejected",
            evidence_validation_id: "evidence_validation_rejected_fixture",
            evidence_task_id: "evidence_task_rejected_fixture",
            validation_target_id: "validation_rejected_fixture",
            admitted_claim_class: null,
            validation_disposition: "insufficient_evidence",
            decision: "reject_for_now",
            confidence_score: 0,
            confidence_band: "very_low",
            persistence_level: "none",
            evidence_record_ids: [],
            qualification: {
              contradiction_present: false,
              must_preserve_contradiction: false
            },
            graph_write_eligibility: false,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              validation_count: 2,
              reviewed_count: 2,
              graph_write_eligible_count: 1,
              graph_write_count: 0,
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified, rejected],
            graph_write_queue: [
              {
                ...qualified,
                graph_write_rank: 1
              },
              {
                ...rejected,
                graph_write_rank: 2
              }
            ]
          };

          const emptyGraph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          fs.writeFileSync(
            path.join(root, "knowledge-admission-graph-writer-fixture-v0.1.json"),
            JSON.stringify(admission, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "graph-before-v0.1.json"),
            JSON.stringify(emptyGraph, null, 2)
          );
          NODE

          BEFORE_HASH="$(sha256sum "$TEST_DIR/graph-before-v0.1.json" | awk '{print $1}')"

          (
            cd "$TEST_DIR"
            node scripts/cosmos_graph_writer.cjs \
              --admission knowledge-admission-graph-writer-fixture-v0.1.json \
              --graph graph-before-v0.1.json \
              --out knowledge/cosmos/graph-after-first-v0.1.json \
              --report knowledge/cosmos/graph-write-first-report-v0.1.json
          )

          AFTER_INPUT_HASH="$(sha256sum "$TEST_DIR/graph-before-v0.1.json" | awk '{print $1}')"

          if [ "$BEFORE_HASH" != "$AFTER_INPUT_HASH" ]; then
            echo "ERROR: Cosmos Graph Writer mutated the input graph in place."
            exit 1
          fi

          (
            cd "$TEST_DIR"
            node scripts/cosmos_graph_writer.cjs \
              --admission knowledge-admission-graph-writer-fixture-v0.1.json \
              --graph knowledge/cosmos/graph-after-first-v0.1.json \
              --out knowledge/cosmos/graph-after-second-v0.1.json \
              --report knowledge/cosmos/graph-write-second-report-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const read = (name) => JSON.parse(
            fs.readFileSync(path.join(root, "knowledge/cosmos", name), "utf8")
          );

          const firstGraph = read("graph-after-first-v0.1.json");
          const secondGraph = read("graph-after-second-v0.1.json");
          const firstReport = read("graph-write-first-report-v0.1.json");
          const secondReport = read("graph-write-second-report-v0.1.json");
          const errors = [];

          if (firstGraph.status !== "cosmos_graph_snapshot" ||
              secondGraph.status !== "cosmos_graph_snapshot") {
            errors.push("Graph Writer did not produce Cosmos graph snapshots");
          }

          if (firstReport.status !== "graph_write_resolved" ||
              secondReport.status !== "graph_write_resolved") {
            errors.push("Graph Writer reports did not resolve successfully");
          }

          const firstState = firstReport.graph_write_state || {};
          const secondState = secondReport.graph_write_state || {};

          if (Number(firstState.written_count) !== 1 ||
              Number(firstState.idempotent_skip_count) !== 0 ||
              Number(firstState.error_count) !== 1) {
            errors.push(
              `First graph write invalid: written=${firstState.written_count}, skipped=${firstState.idempotent_skip_count}, errors=${firstState.error_count}`
            );
          }

          if (Number(secondState.written_count) !== 0 ||
              Number(secondState.idempotent_skip_count) !== 1 ||
              Number(secondState.error_count) !== 1) {
            errors.push(
              `Second graph write failed idempotency: written=${secondState.written_count}, skipped=${secondState.idempotent_skip_count}, errors=${secondState.error_count}`
            );
          }

          if (firstGraph.knowledge_records.length !== 1 ||
              secondGraph.knowledge_records.length !== 1) {
            errors.push(
              `Idempotency failed: first_records=${firstGraph.knowledge_records.length}, second_records=${secondGraph.knowledge_records.length}`
            );
          }

          if (firstGraph.audit_log.length !== 1 ||
              secondGraph.audit_log.length !== 1) {
            errors.push(
              `Idempotency failed for audit log: first=${firstGraph.audit_log.length}, second=${secondGraph.audit_log.length}`
            );
          }

          const firstRecord = firstGraph.knowledge_records[0];
          const secondRecord = secondGraph.knowledge_records[0];

          if (!firstRecord || !secondRecord ||
              firstRecord.graph_record_id !== secondRecord.graph_record_id ||
              firstRecord.knowledge_admission_id !== secondRecord.knowledge_admission_id) {
            errors.push("Second run did not preserve the original graph record identity");
          }

          if (firstRecord?.claim_class !== "validated_qualified_relationship" ||
              firstRecord?.validation_disposition !== "partially_supported" ||
              Number(firstRecord?.confidence_score) !== 76 ||
              firstRecord?.confidence_band !== "high") {
            errors.push("Graph record lost claim class, disposition or confidence");
          }

          if (firstRecord?.qualification?.contradiction_present !== true ||
              firstRecord?.qualification?.must_preserve_contradiction !== true ||
              firstRecord?.qualification?.confidence_preserved !== true ||
              firstRecord?.qualification?.source_lineage_preserved !== true ||
              firstRecord?.qualification?.temporal_scope_preserved !== true ||
              firstRecord?.qualification?.geography_scope_preserved !== true) {
            errors.push("Graph record lost qualification or preservation metadata");
          }

          if (!Array.isArray(firstRecord?.evidence_record_ids) ||
              firstRecord.evidence_record_ids.length !== 3) {
            errors.push("Graph record did not preserve complete evidence lineage");
          }

          if (firstRecord?.reversibility?.reversible !== true ||
              firstRecord?.reversibility?.reversal_operation !== "deactivate_graph_record" ||
              firstRecord?.reversibility?.reversal_key !== firstRecord?.knowledge_admission_id) {
            errors.push("Graph record reversibility contract invalid");
          }

          const rejectedId = "knowledge_admission_graph_writer_fixture_rejected";
          if (firstGraph.knowledge_records.some(
                row => row.knowledge_admission_id === rejectedId
              ) ||
              secondGraph.knowledge_records.some(
                row => row.knowledge_admission_id === rejectedId
              )) {
            errors.push("Non-eligible admission was materialized into the graph");
          }

          if (!firstReport.errors.some(
                row => row.knowledge_admission_id === rejectedId &&
                       row.reason === "admission_not_graph_write_eligible"
              ) ||
              !secondReport.errors.some(
                row => row.knowledge_admission_id === rejectedId &&
                       row.reason === "admission_not_graph_write_eligible"
              )) {
            errors.push("Graph Writer did not explicitly refuse the non-eligible admission");
          }

          if (!secondReport.skipped.some(
                row => row.knowledge_admission_id === firstRecord?.knowledge_admission_id &&
                       row.reason === "already_materialized_idempotent_skip"
              )) {
            errors.push("Second run did not record the expected idempotent skip");
          }

          const safeguards = firstReport.safeguards || {};
          if (safeguards.mutates_input_graph_in_place !== false ||
              safeguards.writes_only_explicit_output_snapshot !== true ||
              safeguards.requires_graph_write_eligibility !== true ||
              safeguards.preserves_qualification !== true ||
              safeguards.preserves_contradiction !== true ||
              safeguards.preserves_confidence !== true ||
              safeguards.preserves_evidence_lineage !== true ||
              safeguards.idempotent_by_knowledge_admission_id !== true ||
              safeguards.audit_log_required !== true ||
              safeguards.graph_records_reversible !== true ||
              safeguards.rejected_admissions_not_written !== true ||
              safeguards.unlimited_conceptual_continuation_preserved !== true) {
            errors.push("Cosmos Graph Writer safeguards invalid");
          }

          if (firstState.conceptual_distance_limit !== null ||
              firstState.continuation_possible !== true ||
              secondState.conceptual_distance_limit !== null ||
              secondState.continuation_possible !== true) {
            errors.push("Graph Writer did not preserve unlimited conceptual continuation");
          }

          if (errors.length) {
            console.error("\nCosmos Graph Writer v0.1 preflight errors:");
            for (const error of errors) console.error(`- ${error}`);
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Graph Writer v0.1 isolated test passed.");
          console.log(`First run writes:          ${firstState.written_count}`);
          console.log(`First run refused:         ${firstState.error_count}`);
          console.log(`Second run writes:         ${secondState.written_count}`);
          console.log(`Second run idempotent skip:${secondState.idempotent_skip_count}`);
          console.log(`Stable graph records:      ${secondGraph.knowledge_records.length}`);
          console.log(`Stable audit records:      ${secondGraph.audit_log.length}`);
          console.log(
            "Eligibility enforcement, qualification/confidence/lineage preservation, reversibility, snapshot isolation and second-run idempotency safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE


      - name: Test Cosmos Graph Integrity v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_graph_writer.cjs \
            "$TEST_DIR/scripts/cosmos_graph_writer.cjs"
          cp scripts/cosmos_graph_integrity.cjs \
            "$TEST_DIR/scripts/cosmos_graph_integrity.cjs"

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const qualified = {
            knowledge_admission_id: "knowledge_admission_integrity_fixture_qualified",
            evidence_validation_id: "evidence_validation_integrity_fixture",
            evidence_task_id: "evidence_task_integrity_fixture",
            validation_target_id: "validation_integrity_fixture",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_integrity_1",
              "evidence_integrity_2",
              "evidence_integrity_3"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified],
            graph_write_queue: [
              { ...qualified, graph_write_rank: 1 }
            ]
          };

          const graph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          fs.writeFileSync(
            path.join(root, "knowledge-admission-fixture.json"),
            JSON.stringify(admission, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "empty-graph-fixture.json"),
            JSON.stringify(graph, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_writer.cjs \
              --admission knowledge-admission-fixture.json \
              --graph empty-graph-fixture.json \
              --out knowledge/cosmos/valid-graph-v0.1.json \
              --report knowledge/cosmos/graph-writer-report-v0.1.json

            node scripts/cosmos_graph_integrity.cjs \
              --graph knowledge/cosmos/valid-graph-v0.1.json \
              --out knowledge/cosmos/graph-integrity-valid-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const graph = JSON.parse(
            fs.readFileSync(
              path.join(root, "knowledge/cosmos/valid-graph-v0.1.json"),
              "utf8"
            )
          );
          const report = JSON.parse(
            fs.readFileSync(
              path.join(root, "knowledge/cosmos/graph-integrity-valid-v0.1.json"),
              "utf8"
            )
          );

          const errors = [];

          if (report.schema_version !== "0.1") {
            errors.push(
              `Unexpected Graph Integrity schema_version: ${report.schema_version}`
            );
          }

          if (report.status !== "graph_integrity_passed") {
            errors.push(
              `Valid controlled graph should pass integrity; got ${report.status}`
            );
          }

          if (Number(report.integrity_state?.error_count) !== 0) {
            errors.push(
              `Valid graph integrity expected 0 errors; got ${report.integrity_state?.error_count}`
            );
          }

          if (report.integrity_state?.production_write_safe !== true) {
            errors.push(
              "Valid graph should be production_write_safe=true"
            );
          }

          if (Number(report.graph_state?.knowledge_record_count) !== 1 ||
              Number(report.graph_state?.active_record_count) !== 1 ||
              Number(report.graph_state?.audit_record_count) !== 1 ||
              Number(report.graph_state?.unique_admission_count) !== 1 ||
              Number(report.graph_state?.unique_validation_target_count) !== 1) {
            errors.push(
              "Valid graph state counts are incorrect"
            );
          }

          if (!Array.isArray(graph.knowledge_records) ||
              graph.knowledge_records.length !== 1 ||
              graph.knowledge_records[0].qualification?.must_preserve_contradiction !== true) {
            errors.push(
              "Graph Writer fixture did not preserve qualified contradiction"
            );
          }

          const safeguards = report.safeguards || {};

          if (safeguards.read_only !== true ||
              safeguards.mutates_graph !== false ||
              safeguards.auto_repairs_graph !== false ||
              safeguards.checks_duplicate_materialization !== true ||
              safeguards.checks_broken_lineage !== true ||
              safeguards.checks_qualification_loss !== true ||
              safeguards.checks_confidence_integrity !== true ||
              safeguards.checks_audit_integrity !== true ||
              safeguards.checks_reversibility !== true ||
              safeguards.checks_active_conflicts !== true ||
              safeguards.checks_temporal_ordering !== true) {
            errors.push(
              "Graph Integrity safeguards invalid on valid graph"
            );
          }

          if (errors.length) {
            console.error("\nCosmos Graph Integrity valid-graph errors:");
            for (const error of errors) console.error(`- ${error}`);
            process.exit(1);
          }

          // Create an intentionally corrupted copy.
          const corrupted = JSON.parse(JSON.stringify(graph));
          corrupted.knowledge_records[0].qualification.must_preserve_contradiction = false;

          fs.writeFileSync(
            path.join(root, "knowledge/cosmos/corrupted-graph-v0.1.json"),
            JSON.stringify(corrupted, null, 2)
          );

          console.log("");
          console.log("Valid graph integrity test passed.");
          console.log("Creating intentional qualification-loss corruption for negative test.");
          NODE

          set +e
          (
            cd "$TEST_DIR"
            node scripts/cosmos_graph_integrity.cjs \
              --graph knowledge/cosmos/corrupted-graph-v0.1.json \
              --out knowledge/cosmos/graph-integrity-corrupt-v0.1.json
          )
          CORRUPT_EXIT=$?
          set -e

          if [ "$CORRUPT_EXIT" -eq 0 ]; then
            echo "ERROR: Corrupted graph unexpectedly passed Graph Integrity."
            exit 1
          fi

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const report = JSON.parse(
            fs.readFileSync(
              path.join(root, "knowledge/cosmos/graph-integrity-corrupt-v0.1.json"),
              "utf8"
            )
          );

          const codes = new Set((report.issues || []).map(x => x.code));

          if (report.status !== "graph_integrity_failed") {
            throw new Error(
              `Corrupted graph must fail integrity; got ${report.status}`
            );
          }

          if (Number(report.integrity_state?.error_count) < 1) {
            throw new Error(
              "Corrupted graph integrity report must contain at least one error"
            );
          }

          if (report.integrity_state?.production_write_safe !== false) {
            throw new Error(
              "Corrupted graph must have production_write_safe=false"
            );
          }

          if (!codes.has("QUALIFICATION_LOSS")) {
            throw new Error(
              `Expected QUALIFICATION_LOSS; got ${[...codes].join(", ")}`
            );
          }

          console.log("");
          console.log("Cosmos Graph Integrity v0.1 isolated test passed.");
          console.log("Valid graph:             PASS");
          console.log(`Valid errors:            0`);
          console.log("Intentional corruption:  REJECTED");
          console.log(`Corrupt errors:          ${report.integrity_state.error_count}`);
          console.log("Detected:                QUALIFICATION_LOSS");
          console.log("Production graph writes: NONE");
          console.log(
            "Positive integrity and intentional-failure detection safeguards passed."
          );
          NODE

      - name: Test Cosmos Graph Reversal v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_graph_writer.cjs \
            "$TEST_DIR/scripts/cosmos_graph_writer.cjs"
          cp scripts/cosmos_graph_integrity.cjs \
            "$TEST_DIR/scripts/cosmos_graph_integrity.cjs"
          cp scripts/cosmos_graph_reversal.cjs \
            "$TEST_DIR/scripts/cosmos_graph_reversal.cjs"

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const qualified = {
            knowledge_admission_id: "knowledge_admission_reversal_fixture",
            evidence_validation_id: "evidence_validation_reversal_fixture",
            evidence_task_id: "evidence_task_reversal_fixture",
            validation_target_id: "validation_reversal_fixture",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_reversal_1",
              "evidence_reversal_2",
              "evidence_reversal_3"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified],
            graph_write_queue: [
              { ...qualified, graph_write_rank: 1 }
            ]
          };

          const graph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          const request = {
            schema_version: "0.1",
            requested_at: new Date().toISOString(),
            knowledge_admission_id:
              "knowledge_admission_reversal_fixture",
            reason:
              "controlled_preflight_reversal_test",
            requested_by:
              "cosmos_preflight"
          };

          fs.writeFileSync(
            path.join(root, "knowledge-admission-fixture.json"),
            JSON.stringify(admission, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "empty-graph-fixture.json"),
            JSON.stringify(graph, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "reversal-request-fixture.json"),
            JSON.stringify(request, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_writer.cjs \
              --admission knowledge-admission-fixture.json \
              --graph empty-graph-fixture.json \
              --out knowledge/cosmos/pre-reversal-graph-v0.1.json \
              --report knowledge/cosmos/pre-reversal-writer-report-v0.1.json

            node scripts/cosmos_graph_integrity.cjs \
              --graph knowledge/cosmos/pre-reversal-graph-v0.1.json \
              --out knowledge/cosmos/pre-reversal-integrity-v0.1.json
          )

          PRE_HASH="$(
            sha256sum "$TEST_DIR/knowledge/cosmos/pre-reversal-graph-v0.1.json" |
            awk '{print $1}'
          )"

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_reversal.cjs \
              --graph knowledge/cosmos/pre-reversal-graph-v0.1.json \
              --request reversal-request-fixture.json \
              --out knowledge/cosmos/post-reversal-graph-v0.1.json \
              --report knowledge/cosmos/graph-reversal-report-v0.1.json
          )

          POST_INPUT_HASH="$(
            sha256sum "$TEST_DIR/knowledge/cosmos/pre-reversal-graph-v0.1.json" |
            awk '{print $1}'
          )"

          if [ "$PRE_HASH" != "$POST_INPUT_HASH" ]; then
            echo "ERROR: Graph Reversal mutated its input graph."
            exit 1
          fi

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_integrity.cjs \
              --graph knowledge/cosmos/post-reversal-graph-v0.1.json \
              --out knowledge/cosmos/post-reversal-integrity-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          function read(relative) {
            return JSON.parse(
              fs.readFileSync(path.join(root, relative), "utf8")
            );
          }

          const before = read(
            "knowledge/cosmos/pre-reversal-graph-v0.1.json"
          );
          const beforeIntegrity = read(
            "knowledge/cosmos/pre-reversal-integrity-v0.1.json"
          );
          const after = read(
            "knowledge/cosmos/post-reversal-graph-v0.1.json"
          );
          const report = read(
            "knowledge/cosmos/graph-reversal-report-v0.1.json"
          );
          const afterIntegrity = read(
            "knowledge/cosmos/post-reversal-integrity-v0.1.json"
          );

          const errors = [];

          if (beforeIntegrity.status !== "graph_integrity_passed" ||
              Number(beforeIntegrity.integrity_state?.error_count) !== 0) {
            errors.push(
              "Pre-reversal graph must pass Graph Integrity"
            );
          }

          if (report.schema_version !== "0.1" ||
              report.status !== "graph_reversal_resolved") {
            errors.push(
              `Unexpected Graph Reversal result: ${report.schema_version} / ${report.status}`
            );
          }

          if (Number(report.reversal_state?.requested_count) !== 1 ||
              Number(report.reversal_state?.reversed_count) !== 1 ||
              Number(report.reversal_state?.idempotent_skip_count) !== 0 ||
              Number(report.reversal_state?.error_count) !== 0 ||
              Number(report.reversal_state?.active_graph_record_count) !== 0 ||
              Number(report.reversal_state?.inactive_graph_record_count) !== 1) {
            errors.push(
              "First reversal state counts invalid"
            );
          }

          if (!Array.isArray(before.knowledge_records) ||
              before.knowledge_records.length !== 1 ||
              !Array.isArray(after.knowledge_records) ||
              after.knowledge_records.length !== 1) {
            errors.push(
              "Graph Reversal must preserve exactly one historical graph record"
            );
          } else {
            const prior = before.knowledge_records[0];
            const current = after.knowledge_records[0];

            if (prior.graph_record_id !== current.graph_record_id ||
                prior.knowledge_admission_id !== current.knowledge_admission_id ||
                prior.validation_target_id !== current.validation_target_id) {
              errors.push(
                "Graph Reversal changed graph-record identity/lineage"
              );
            }

            if (current.active !== false ||
                !current.deactivated_at ||
                current.deactivation?.operation !==
                  "deactivate_graph_record" ||
                current.deactivation?.reversible_history_preserved !== true) {
              errors.push(
                "Graph Reversal did not safely deactivate the record"
              );
            }

            if (Number(current.confidence_score) !== 76 ||
                current.validation_disposition !==
                  "partially_supported" ||
                current.qualification?.contradiction_present !== true ||
                current.qualification?.must_preserve_contradiction !== true ||
                !Array.isArray(current.evidence_record_ids) ||
                current.evidence_record_ids.length !== 3) {
              errors.push(
                "Graph Reversal lost confidence, qualification, contradiction or evidence lineage"
              );
            }
          }

          if (!Array.isArray(before.audit_log) ||
              before.audit_log.length !== 1 ||
              !Array.isArray(after.audit_log) ||
              after.audit_log.length !== 2) {
            errors.push(
              "Graph Reversal must preserve creation audit and append exactly one reversal audit"
            );
          } else {
            const creationAudit = before.audit_log[0];
            const preservedCreation = after.audit_log.find(
              row => row.audit_id === creationAudit.audit_id
            );
            const reversalAudit = after.audit_log.find(
              row => row.operation === "deactivate_graph_record"
            );

            if (!preservedCreation) {
              errors.push(
                "Original creation audit was not preserved"
              );
            }

            if (!reversalAudit ||
                reversalAudit.prior_active_state !== true ||
                reversalAudit.resulting_active_state !== false ||
                reversalAudit.preserves_original_record !== true ||
                reversalAudit.preserves_creation_audit !== true ||
                reversalAudit.reversible !== true ||
                reversalAudit.reversal_operation !==
                  "deactivate_graph_record" ||
                !Array.isArray(reversalAudit.evidence_record_ids) ||
                reversalAudit.evidence_record_ids.length !== 3 ||
                Number(reversalAudit.confidence_score) !== 76) {
              errors.push(
                "Reversal audit contract invalid"
              );
            }
          }

          if (afterIntegrity.status !== "graph_integrity_passed" ||
              Number(afterIntegrity.integrity_state?.error_count) !== 0 ||
              afterIntegrity.integrity_state?.production_write_safe !== true ||
              Number(afterIntegrity.graph_state?.active_record_count) !== 0 ||
              Number(afterIntegrity.graph_state?.inactive_record_count) !== 1 ||
              Number(afterIntegrity.graph_state?.audit_record_count) !== 2) {
            errors.push(
              "Post-reversal Graph Integrity did not pass with 0 active / 1 inactive / 2 audits"
            );
          }

          const safeguards = report.safeguards || {};

          if (safeguards.mutates_input_graph_in_place !== false ||
              safeguards.deletes_graph_records !== false ||
              safeguards.preserves_historical_record !== true ||
              safeguards.preserves_creation_audit !== true ||
              safeguards.appends_reversal_audit !== true ||
              safeguards.requires_explicit_target !== true ||
              safeguards.respects_reversibility_contract !== true ||
              safeguards.idempotent_for_inactive_record !== true ||
              safeguards.writes_only_explicit_output_snapshot !== true) {
            errors.push(
              "Graph Reversal safeguards invalid"
            );
          }

          if (errors.length) {
            console.error("\nCosmos Graph Reversal v0.1 preflight errors:");
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("First Cosmos Graph Reversal v0.1 test passed.");
          console.log("Before: 1 active / 0 inactive / 1 audit");
          console.log("After:  0 active / 1 inactive / 2 audits");
          console.log("Post-reversal Graph Integrity: PASS");
          NODE

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_reversal.cjs \
              --graph knowledge/cosmos/post-reversal-graph-v0.1.json \
              --request reversal-request-fixture.json \
              --out knowledge/cosmos/second-reversal-graph-v0.1.json \
              --report knowledge/cosmos/second-reversal-report-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          function read(relative) {
            return JSON.parse(
              fs.readFileSync(path.join(root, relative), "utf8")
            );
          }

          const once = read(
            "knowledge/cosmos/post-reversal-graph-v0.1.json"
          );
          const twice = read(
            "knowledge/cosmos/second-reversal-graph-v0.1.json"
          );
          const report = read(
            "knowledge/cosmos/second-reversal-report-v0.1.json"
          );

          if (report.status !== "graph_reversal_resolved" ||
              Number(report.reversal_state?.reversed_count) !== 0 ||
              Number(report.reversal_state?.idempotent_skip_count) !== 1 ||
              Number(report.reversal_state?.error_count) !== 0) {
            throw new Error(
              "Second reversal did not resolve as an idempotent skip"
            );
          }

          if (twice.knowledge_records?.length !== 1 ||
              twice.audit_log?.length !== 2 ||
              twice.knowledge_records?.[0]?.active !== false) {
            throw new Error(
              "Second reversal changed already-reversed graph history"
            );
          }

          if (JSON.stringify(once.knowledge_records) !==
              JSON.stringify(twice.knowledge_records) ||
              JSON.stringify(once.audit_log) !==
              JSON.stringify(twice.audit_log)) {
            throw new Error(
              "Second reversal was not structurally idempotent"
            );
          }

          console.log("");
          console.log("Second reversal idempotency test passed.");
          console.log("Second-run reversals: 0");
          console.log("Idempotent skips:     1");
          console.log("Additional audits:    0");
          NODE

          cat > "$TEST_DIR/unknown-reversal-request.json" <<'JSON'
          {
            "schema_version": "0.1",
            "knowledge_admission_id": "knowledge_admission_does_not_exist",
            "reason": "intentional_unknown_target_test",
            "requested_by": "cosmos_preflight"
          }
          JSON

          set +e
          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_reversal.cjs \
              --graph knowledge/cosmos/post-reversal-graph-v0.1.json \
              --request unknown-reversal-request.json \
              --out knowledge/cosmos/unknown-reversal-graph-v0.1.json \
              --report knowledge/cosmos/unknown-reversal-report-v0.1.json
          )
          UNKNOWN_EXIT=$?
          set -e

          if [ "$UNKNOWN_EXIT" -eq 0 ]; then
            echo "ERROR: Unknown reversal target unexpectedly succeeded."
            exit 1
          fi

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const graph = JSON.parse(
            fs.readFileSync(
              path.join(
                root,
                "knowledge/cosmos/unknown-reversal-graph-v0.1.json"
              ),
              "utf8"
            )
          );

          const original = JSON.parse(
            fs.readFileSync(
              path.join(
                root,
                "knowledge/cosmos/post-reversal-graph-v0.1.json"
              ),
              "utf8"
            )
          );

          const report = JSON.parse(
            fs.readFileSync(
              path.join(
                root,
                "knowledge/cosmos/unknown-reversal-report-v0.1.json"
              ),
              "utf8"
            )
          );

          if (report.status !== "graph_reversal_rejected" ||
              Number(report.reversal_state?.reversed_count) !== 0 ||
              Number(report.reversal_state?.error_count) !== 1 ||
              report.errors?.[0]?.code !== "reversal_target_not_found") {
            throw new Error(
              "Unknown target rejection contract invalid"
            );
          }

          if (JSON.stringify(graph.knowledge_records) !==
              JSON.stringify(original.knowledge_records) ||
              JSON.stringify(graph.audit_log) !==
              JSON.stringify(original.audit_log)) {
            throw new Error(
              "Rejected unknown reversal modified graph history"
            );
          }

          console.log("");
          console.log("Unknown-target negative reversal test passed.");
          console.log("Unknown target: REJECTED");
          console.log("Graph changes:  0");
          console.log("Repository files were not modified by isolated tests.");
          NODE

      - name: Test Cosmos Causal Narrative v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_causal_narrative.cjs \
            "$TEST_DIR/scripts/cosmos_causal_narrative.cjs"

          cat > "$TEST_DIR/causal-input-v0.1.json" <<'JSON'
          {
            "focal_signal": {
              "node_id": "substation_pressure",
              "label": "Substation capacity pressure",
              "type": "signal",
              "role": "focal_signal"
            },
            "nodes": [
              {
                "node_id": "ai_compute",
                "label": "AI compute demand growth",
                "type": "driver",
                "role": "upstream_driver"
              },
              {
                "node_id": "data_center_expansion",
                "label": "Data-center capacity expansion",
                "type": "driver",
                "role": "upstream_driver"
              },
              {
                "node_id": "substation_pressure",
                "label": "Substation capacity pressure",
                "type": "signal",
                "role": "focal_signal"
              },
              {
                "node_id": "transformer_demand",
                "label": "Transformer capacity demand",
                "type": "consequence",
                "role": "direct_effect"
              },
              {
                "node_id": "oem_capacity_pressure",
                "label": "OEM manufacturing capacity pressure",
                "type": "consequence",
                "role": "second_order_effect"
              },
              {
                "node_id": "lead_time_pressure",
                "label": "Transformer lead-time pressure",
                "type": "consequence",
                "role": "third_order_effect"
              },
              {
                "node_id": "project_schedule_pressure",
                "label": "Project schedule pressure",
                "type": "consequence",
                "role": "emerging_effect"
              },
              {
                "node_id": "investment_relocation",
                "label": "Potential geographic investment relocation",
                "type": "scenario",
                "role": "emerging_effect"
              }
            ],
            "edges": [
              {
                "edge_id": "edge_ai_to_dc",
                "from_node_id": "ai_compute",
                "to_node_id": "data_center_expansion",
                "relationship": "drives",
                "evidence_status": "supported",
                "confidence_score": 92,
                "source_record_ids": [
                  "evidence_ai_dc_1",
                  "evidence_ai_dc_2"
                ],
                "time_horizon": "current_to_short",
                "geography_scope": "global",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_dc_to_substation",
                "from_node_id": "data_center_expansion",
                "to_node_id": "substation_pressure",
                "relationship": "increases",
                "evidence_status": "supported",
                "confidence_score": 88,
                "source_record_ids": [
                  "evidence_dc_grid_1",
                  "evidence_dc_grid_2"
                ],
                "time_horizon": "short",
                "geography_scope": "United States",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_substation_to_transformer",
                "from_node_id": "substation_pressure",
                "to_node_id": "transformer_demand",
                "relationship": "increases",
                "evidence_status": "supported",
                "confidence_score": 84,
                "source_record_ids": [
                  "evidence_substation_transformer_1"
                ],
                "time_horizon": "short",
                "geography_scope": "United States",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_transformer_to_oem",
                "from_node_id": "transformer_demand",
                "to_node_id": "oem_capacity_pressure",
                "relationship": "constrains",
                "evidence_status": "inferred",
                "confidence_score": 78,
                "source_record_ids": [
                  "evidence_oem_capacity_1"
                ],
                "time_horizon": "short_to_mid",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_oem_to_lead",
                "from_node_id": "oem_capacity_pressure",
                "to_node_id": "lead_time_pressure",
                "relationship": "increases",
                "evidence_status": "scenario",
                "confidence_score": 74,
                "source_record_ids": [
                  "evidence_lead_time_1"
                ],
                "time_horizon": "mid",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_lead_to_schedule",
                "from_node_id": "lead_time_pressure",
                "to_node_id": "project_schedule_pressure",
                "relationship": "increases",
                "evidence_status": "scenario",
                "confidence_score": 70,
                "source_record_ids": [
                  "evidence_schedule_1"
                ],
                "time_horizon": "mid_to_long",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_schedule_to_relocation",
                "from_node_id": "project_schedule_pressure",
                "to_node_id": "investment_relocation",
                "relationship": "may_influence",
                "evidence_status": "speculative",
                "confidence_score": 62,
                "source_record_ids": [
                  "evidence_location_scenario_1"
                ],
                "time_horizon": "long",
                "geography_scope": "multi-region",
                "temporal_scope": "forward"
              }
            ],
            "activation_events": [
              {
                "activation_id": "activation_1",
                "label": "Recent data-center project announcements increased",
                "kind": "new_evidence",
                "evidence_status": "supported",
                "confidence_score": 88,
                "source_record_ids": [
                  "activation_evidence_1"
                ],
                "occurred_at": "2026-08-27T12:00:00Z",
                "relevance_score": 95
              },
              {
                "activation_id": "activation_2",
                "label": "Utility load forecasts strengthened",
                "kind": "strengthened_evidence",
                "evidence_status": "supported",
                "confidence_score": 84,
                "source_record_ids": [
                  "activation_evidence_2"
                ],
                "occurred_at": "2026-08-27T10:00:00Z",
                "relevance_score": 90
              },
              {
                "activation_id": "activation_3",
                "label": "Transformer lead-time pressure remained unresolved",
                "kind": "related_signal",
                "evidence_status": "supported",
                "confidence_score": 80,
                "source_record_ids": [
                  "activation_evidence_3"
                ],
                "occurred_at": "2026-08-26T18:00:00Z",
                "relevance_score": 82
              }
            ],
            "max_causal_depth": 5
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_causal_narrative.cjs \
              --input causal-input-v0.1.json \
              --out knowledge/cosmos/causal-narrative-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/causal-narrative-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Causal Narrative did not create causal-narrative-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Causal Narrative schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "causal_narrative_resolved") {
            errors.push(
              `Cosmos Causal Narrative failed: ${payload.status}`
            );
          }

          if (payload.focal_signal?.node_id !== "substation_pressure" ||
              payload.focal_signal?.label !== "Substation capacity pressure") {
            errors.push(
              "Cosmos Causal Narrative focal signal invalid"
            );
          }

          const whyNow = payload.why_now || {};

          if (whyNow.explanation_status !==
                "activation_evidence_available" ||
              Number(whyNow.activation_event_count) !== 3 ||
              !Array.isArray(whyNow.strongest_activation_events) ||
              whyNow.strongest_activation_events.length !== 3) {
            errors.push(
              "Cosmos Causal Narrative why_now activation block invalid"
            );
          }

          if (!String(whyNow.rule || "").includes(
                "not the long-run causal origin"
              )) {
            errors.push(
              "Cosmos Causal Narrative did not separate Why Now from root causality"
            );
          }

          const upstream = payload.upstream_drivers || [];

          if (!Array.isArray(upstream) || upstream.length !== 2) {
            errors.push(
              `Controlled Causal Narrative expected 2 upstream drivers; got ${upstream.length}`
            );
          } else {
            const depths = new Set(
              upstream.map(x => Number(x.causal_depth))
            );

            if (!depths.has(1) || !depths.has(2)) {
              errors.push(
                "Controlled upstream drivers did not preserve causal depth 1 and 2"
              );
            }

            const labels = new Set(
              upstream.map(x => String(x.from_label || ""))
            );

            if (!labels.has("Data-center capacity expansion") ||
                !labels.has("AI compute demand growth")) {
              errors.push(
                "Controlled upstream driver labels invalid"
              );
            }
          }

          const story = payload.downstream_story || {};
          const direct = story.direct_effects || [];
          const second = story.second_order_effects || [];
          const third = story.third_order_effects || [];
          const emerging = story.emerging_effects || [];

          if (direct.length !== 1 ||
              second.length !== 1 ||
              third.length !== 1 ||
              emerging.length !== 2) {
            errors.push(
              `Controlled downstream grouping invalid: direct=${direct.length}, second=${second.length}, third=${third.length}, emerging=${emerging.length}`
            );
          }

          if (direct[0]?.to_label !== "Transformer capacity demand") {
            errors.push(
              "Direct effect should be Transformer capacity demand"
            );
          }

          if (second[0]?.to_label !==
                "OEM manufacturing capacity pressure") {
            errors.push(
              "Second-order effect should be OEM manufacturing capacity pressure"
            );
          }

          if (third[0]?.to_label !==
                "Transformer lead-time pressure") {
            errors.push(
              "Third-order effect should be Transformer lead-time pressure"
            );
          }

          const butterfly = payload.butterfly_effect || {};
          const downstream = butterfly.all_downstream_edges || [];
          const candidates = butterfly.potential_new_signals || [];

          if (!Array.isArray(downstream) || downstream.length !== 5) {
            errors.push(
              `Controlled butterfly effect expected 5 downstream edges; got ${downstream.length}`
            );
          }

          const expectedDepths = [1,2,3,4,5];

          for (const depth of expectedDepths) {
            if (!downstream.some(
                  row => Number(row.causal_depth) === depth
                )) {
              errors.push(
                `Butterfly effect missing causal depth ${depth}`
              );
            }
          }

          // Confidence must never increase beyond the original edge confidence
          // and should not increase as the path gets farther away.
          let priorEffective = Infinity;

          for (const row of [...downstream].sort(
            (a,b) => Number(a.causal_depth)-Number(b.causal_depth)
          )) {
            const effective =
              Number(row.effective_confidence_score);
            const original =
              Number(row.original_confidence_score);

            if (!Number.isFinite(effective) ||
                !Number.isFinite(original) ||
                effective > original + 1e-9) {
              errors.push(
                `Causal confidence inflation on ${row.narrative_edge_id || "(unknown)"}`
              );
            }

            if (effective > priorEffective + 1e-9) {
              errors.push(
                `Effective confidence increased with causal distance at depth ${row.causal_depth}`
              );
            }

            priorEffective = effective;

            if (!Array.isArray(row.source_record_ids) ||
                row.source_record_ids.length < 1) {
              errors.push(
                `Causal edge ${row.narrative_edge_id || "(unknown)"} lost evidence lineage`
              );
            }

            if (!row.time_horizon ||
                !row.evidence_status ||
                !row.effective_confidence_band) {
              errors.push(
                `Causal edge ${row.narrative_edge_id || "(unknown)"} lost narrative metadata`
              );
            }
          }

          if (!Array.isArray(candidates) || candidates.length < 1) {
            errors.push(
              "Cosmos Causal Narrative produced no potential_new_signals"
            );
          }

          for (const candidate of candidates) {
            if (candidate.status !== "candidate_only" ||
                candidate.promotion_to_signal !== false) {
              errors.push(
                `Potential new signal ${candidate.candidate_signal_id || "(unknown)"} was improperly promoted`
              );
            }

            if (Number(candidate.causal_depth) < 2) {
              errors.push(
                `Potential new signal ${candidate.candidate_signal_id || "(unknown)"} is too close to focal signal`
              );
            }
          }

          const contract = payload.narrative_contract || {};

          if (contract.machine_readable_only !== true ||
              contract.article_prose_generated !== false ||
              contract.causal_direction_preserved !== true ||
              contract.causal_depth_preserved !== true ||
              contract.time_horizon_preserved !== true ||
              contract.evidence_status_preserved !== true ||
              contract.evidence_lineage_preserved !== true ||
              contract.confidence_degrades_with_distance !== true ||
              contract.why_now_separated_from_root_cause !== true ||
              contract.possibility_never_silently_promoted_to_fact !== true) {
            errors.push(
              "Cosmos Causal Narrative narrative contract invalid"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.writes_to_knowledge_graph !== false ||
              safeguards.mutates_input_graph !== false ||
              safeguards.creates_unverified_facts !== false ||
              safeguards.preserves_source_lineage !== true ||
              safeguards.preserves_epistemic_status !== true ||
              safeguards.causal_distance_penalizes_confidence !== true ||
              safeguards.activation_logic_separate_from_causality !== true ||
              safeguards.candidate_signals_not_auto_promoted !== true) {
            errors.push(
              "Cosmos Causal Narrative safeguards invalid"
            );
          }

          if (errors.length) {
            console.error(
              "\nCosmos Causal Narrative v0.1 preflight errors:"
            );
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Causal Narrative v0.1 isolated test passed.");
          console.log(`Status:              ${payload.status}`);
          console.log(`Why-now events:      ${whyNow.activation_event_count}`);
          console.log(`Upstream drivers:    ${upstream.length}`);
          console.log(`Direct effects:      ${direct.length}`);
          console.log(`Second-order:        ${second.length}`);
          console.log(`Third-order:         ${third.length}`);
          console.log(`Emerging effects:    ${emerging.length}`);
          console.log(`Butterfly edges:     ${downstream.length}`);
          console.log(`Candidate signals:   ${candidates.length}`);
          console.log(
            "Why-now separation, causal ordering, confidence decay, evidence lineage and candidate-only butterfly safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Graph Evolution v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_graph_evolution.cjs \
            "$TEST_DIR/scripts/cosmos_graph_evolution.cjs"

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;

          const graph = {
            schema_version: "0.1",
            generated_at: "2026-08-26T12:00:00Z",
            status: "cosmos_graph_snapshot",
            knowledge_records: [
              {
                graph_record_id: "graph_existing_same",
                knowledge_admission_id: "knowledge_admission_existing_same",
                validation_target_id: "validation_same",
                claim_class: "validated_qualified_relationship",
                validation_disposition: "partially_supported",
                confidence_score: 70,
                confidence_band: "high",
                epistemic_status: "persistent_qualified_knowledge",
                evidence_record_ids: [
                  "evidence_old_1"
                ],
                qualification: {
                  contradiction_present: false
                },
                admitted_at: "2026-08-20T12:00:00Z",
                active: true
              },
              {
                graph_record_id: "graph_existing_conflict",
                knowledge_admission_id: "knowledge_admission_existing_conflict",
                validation_target_id: "validation_conflict",
                claim_class: "validated_relationship",
                validation_disposition: "supported",
                confidence_score: 82,
                confidence_band: "high",
                epistemic_status: "persistent_knowledge",
                evidence_record_ids: [
                  "evidence_conflict_old"
                ],
                qualification: {
                  contradiction_present: false
                },
                admitted_at: "2026-08-21T12:00:00Z",
                active: true
              },
              {
                graph_record_id: "graph_existing_supersede",
                knowledge_admission_id: "knowledge_admission_existing_supersede",
                validation_target_id: "validation_supersede",
                claim_class: "validated_relationship_v1",
                validation_disposition: "supported",
                confidence_score: 68,
                confidence_band: "moderate",
                epistemic_status: "persistent_knowledge",
                evidence_record_ids: [
                  "evidence_supersede_old"
                ],
                qualification: {
                  contradiction_present: false
                },
                admitted_at: "2026-08-19T12:00:00Z",
                active: true
              }
            ],
            audit_log: [
              {
                audit_id: "audit_existing_same",
                operation: "create_graph_record",
                graph_record_id: "graph_existing_same",
                knowledge_admission_id: "knowledge_admission_existing_same"
              },
              {
                audit_id: "audit_existing_conflict",
                operation: "create_graph_record",
                graph_record_id: "graph_existing_conflict",
                knowledge_admission_id: "knowledge_admission_existing_conflict"
              },
              {
                audit_id: "audit_existing_supersede",
                operation: "create_graph_record",
                graph_record_id: "graph_existing_supersede",
                knowledge_admission_id: "knowledge_admission_existing_supersede"
              }
            ]
          };

          const admissions = [
            {
              knowledge_admission_id: "knowledge_admission_reinforce",
              evidence_validation_id: "evidence_validation_reinforce",
              validation_target_id: "validation_same",
              admitted_claim_class: "validated_qualified_relationship",
              validation_disposition: "partially_supported",
              decision: "admit_with_qualification",
              confidence_score: 82,
              confidence_band: "high",
              persistence_level: "persistent_qualified_knowledge",
              evidence_record_ids: [
                "evidence_old_1",
                "evidence_new_2"
              ],
              qualification: {
                contradiction_present: false
              },
              graph_write_eligibility: true,
              graph_write_status: "not_started"
            },
            {
              knowledge_admission_id: "knowledge_admission_qualify",
              evidence_validation_id: "evidence_validation_qualify",
              validation_target_id: "validation_same",
              admitted_claim_class: "validated_qualified_relationship",
              validation_disposition: "partially_supported",
              decision: "admit_with_qualification",
              confidence_score: 72,
              confidence_band: "high",
              persistence_level: "persistent_qualified_knowledge",
              evidence_record_ids: [
                "evidence_old_1",
                "evidence_contradiction_new"
              ],
              qualification: {
                contradiction_present: true
              },
              graph_write_eligibility: true,
              graph_write_status: "not_started"
            },
            {
              knowledge_admission_id: "knowledge_admission_conflict",
              evidence_validation_id: "evidence_validation_conflict",
              validation_target_id: "validation_conflict",
              admitted_claim_class: "validated_relationship",
              validation_disposition: "contradicted",
              decision: "admit_with_qualification",
              confidence_score: 79,
              confidence_band: "high",
              persistence_level: "persistent_qualified_knowledge",
              evidence_record_ids: [
                "evidence_conflict_new"
              ],
              qualification: {
                contradiction_present: true
              },
              graph_write_eligibility: true,
              graph_write_status: "not_started"
            },
            {
              knowledge_admission_id: "knowledge_admission_supersede",
              evidence_validation_id: "evidence_validation_supersede",
              validation_target_id: "validation_supersede",
              admitted_claim_class: "validated_relationship_v2",
              validation_disposition: "supported",
              decision: "admit",
              confidence_score: 86,
              confidence_band: "very_high",
              persistence_level: "persistent_knowledge",
              evidence_record_ids: [
                "evidence_supersede_new_1",
                "evidence_supersede_new_2"
              ],
              qualification: {
                contradiction_present: false
              },
              graph_write_eligibility: true,
              graph_write_status: "not_started"
            },
            {
              knowledge_admission_id: "knowledge_admission_create",
              evidence_validation_id: "evidence_validation_create",
              validation_target_id: "validation_new",
              admitted_claim_class: "validated_relationship",
              validation_disposition: "supported",
              decision: "admit",
              confidence_score: 90,
              confidence_band: "very_high",
              persistence_level: "persistent_knowledge",
              evidence_record_ids: [
                "evidence_create_1",
                "evidence_create_2"
              ],
              qualification: {
                contradiction_present: false
              },
              graph_write_eligibility: true,
              graph_write_status: "not_started"
            }
          ];

          const admission = {
            schema_version: "0.1",
            generated_at: "2026-08-27T12:00:00Z",
            status: "knowledge_admission_resolved",
            admissions,
            graph_write_queue: admissions.map((row, index) => ({
              ...row,
              graph_write_rank: index + 1
            }))
          };

          fs.writeFileSync(
            path.join(root, "graph-fixture-v0.1.json"),
            JSON.stringify(graph, null, 2)
          );

          fs.writeFileSync(
            path.join(root, "admission-fixture-v0.1.json"),
            JSON.stringify(admission, null, 2)
          );
          NODE

          (
            cd "$TEST_DIR"

            node scripts/cosmos_graph_evolution.cjs \
              --graph graph-fixture-v0.1.json \
              --admission admission-fixture-v0.1.json \
              --out knowledge/cosmos/graph-evolution-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/graph-evolution-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Graph Evolution did not create graph-evolution-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Cosmos Graph Evolution schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "graph_evolution_planned") {
            errors.push(
              `Cosmos Graph Evolution failed: ${payload.status}`
            );
          }

          const state = payload.evolution_state || {};
          const plans = payload.evolution_plans || [];
          const executionQueue = payload.evolution_execution_queue || [];
          const conflictQueue = payload.conflict_review_queue || [];

          if (!Array.isArray(plans) || plans.length !== 5) {
            errors.push(
              `Controlled Graph Evolution expected 5 plans; got ${plans.length}`
            );
          }

          if (Number(state.planned_count) !== plans.length) {
            errors.push(
              "Cosmos Graph Evolution planned_count mismatch"
            );
          }

          if (Number(state.graph_mutation_count) !== 0) {
            errors.push(
              "Cosmos Graph Evolution planning layer must not mutate graph"
            );
          }

          const actionByAdmission = new Map(
            plans.map(row => [
              row.knowledge_admission_id,
              row.evolution_action
            ])
          );

          const expected = new Map([
            ["knowledge_admission_reinforce", "reinforce_existing"],
            ["knowledge_admission_qualify", "qualify_existing"],
            ["knowledge_admission_conflict", "conflict_review_required"],
            ["knowledge_admission_supersede", "supersede_existing"],
            ["knowledge_admission_create", "create_new"]
          ]);

          for (const [admissionId, action] of expected) {
            if (actionByAdmission.get(admissionId) !== action) {
              errors.push(
                `Graph Evolution action mismatch for ${admissionId}: expected ${action}, got ${actionByAdmission.get(admissionId)}`
              );
            }
          }

          const reinforce = plans.find(
            row =>
              row.knowledge_admission_id ===
              "knowledge_admission_reinforce"
          );

          if (!reinforce ||
              reinforce.current_active_records?.length !== 1 ||
              Number(reinforce.incoming?.confidence_score) !== 82 ||
              reinforce.execution_contract?.graph_mutation_performed !== false ||
              reinforce.execution_contract?.preserve_prior_record_history !== true ||
              reinforce.execution_contract?.preserve_prior_audit_history !== true) {
            errors.push(
              "Reinforce-existing plan contract invalid"
            );
          }

          const qualify = plans.find(
            row =>
              row.knowledge_admission_id ===
              "knowledge_admission_qualify"
          );

          if (!qualify ||
              qualify.incoming?.contradiction_present !== true ||
              qualify.evolution_action !== "qualify_existing") {
            errors.push(
              "Qualify-existing contradiction handling invalid"
            );
          }

          const conflict = plans.find(
            row =>
              row.knowledge_admission_id ===
              "knowledge_admission_conflict"
          );

          if (!conflict ||
              conflict.evolution_action !==
                "conflict_review_required" ||
              conflict.execution_contract?.conflict_must_not_be_auto_resolved !== true ||
              conflict.execution_contract?.requires_separate_evolution_executor !== true) {
            errors.push(
              "Conflict-review boundary invalid"
            );
          }

          const supersede = plans.find(
            row =>
              row.knowledge_admission_id ===
              "knowledge_admission_supersede"
          );

          if (!supersede ||
              supersede.evolution_action !== "supersede_existing" ||
              supersede.execution_contract?.deactivate_prior_record_before_supersession !== true ||
              supersede.execution_contract?.preserve_prior_record_history !== true ||
              supersede.execution_contract?.preserve_prior_audit_history !== true ||
              supersede.execution_contract?.reversible !== true) {
            errors.push(
              "Supersede-existing plan contract invalid"
            );
          }

          const create = plans.find(
            row =>
              row.knowledge_admission_id ===
              "knowledge_admission_create"
          );

          if (!create ||
              create.evolution_action !== "create_new" ||
              create.current_active_records?.length !== 0) {
            errors.push(
              "Create-new plan contract invalid"
            );
          }

          if (!Array.isArray(executionQueue) ||
              executionQueue.length !== 4) {
            errors.push(
              `Controlled Graph Evolution expected 4 executable actions; got ${executionQueue.length}`
            );
          }

          if (!Array.isArray(conflictQueue) ||
              conflictQueue.length !== 1 ||
              conflictQueue[0]?.knowledge_admission_id !==
                "knowledge_admission_conflict") {
            errors.push(
              "Graph Evolution conflict review queue invalid"
            );
          }

          for (let i = 0; i < executionQueue.length; i += 1) {
            const row = executionQueue[i];

            if (Number(row.evolution_rank) !== i + 1) {
              errors.push(
                `Graph Evolution execution queue rank invalid at index ${i}`
              );
            }

            if (row.execution_status !== "not_started") {
              errors.push(
                `Graph Evolution execution queue ${row.graph_evolution_id || "(unknown)"} crossed execution boundary`
              );
            }
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.mutates_graph !== false ||
              safeguards.deletes_historical_knowledge !== false ||
              safeguards.preserves_existing_records !== true ||
              safeguards.preserves_audit_history !== true ||
              safeguards.conflicting_polarity_requires_review !== true ||
              safeguards.materially_equivalent_knowledge_not_duplicated !== true ||
              safeguards.supersession_requires_explicit_execution !== true ||
              safeguards.reversibility_required !== true) {
            errors.push(
              "Cosmos Graph Evolution safeguards invalid"
            );
          }

          if (errors.length) {
            console.error(
              "\nCosmos Graph Evolution v0.1 preflight errors:"
            );
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Graph Evolution v0.1 isolated test passed.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Plans:             ${plans.length}`);
          console.log(
            `Create new:        ${state.by_action?.create_new || 0}`
          );
          console.log(
            `Reinforce:         ${state.by_action?.reinforce_existing || 0}`
          );
          console.log(
            `Qualify:           ${state.by_action?.qualify_existing || 0}`
          );
          console.log(
            `Supersede:         ${state.by_action?.supersede_existing || 0}`
          );
          console.log(
            `Conflict review:   ${state.by_action?.conflict_review_required || 0}`
          );
          console.log(`Execution queue:   ${executionQueue.length}`);
          console.log(`Conflict queue:    ${conflictQueue.length}`);
          console.log("Graph mutations:     0");
          console.log(
            "History preservation, explicit supersession, contradiction review and no-mutation safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos Temporal Knowledge v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_temporal_knowledge.cjs \
            "$TEST_DIR/scripts/cosmos_temporal_knowledge.cjs"

          cat > "$TEST_DIR/temporal-graph-fixture-v0.1.json" <<'JSON'
          {
            "schema_version": "0.1",
            "generated_at": "2026-08-27T12:00:00Z",
            "status": "cosmos_graph_snapshot",
            "knowledge_records": [
              {
                "graph_record_id": "graph_past",
                "knowledge_admission_id": "knowledge_admission_past",
                "validation_target_id": "validation_temporal_shared",
                "claim_class": "validated_relationship",
                "validation_disposition": "supported",
                "confidence_score": 68,
                "confidence_band": "moderate",
                "epistemic_status": "persistent_knowledge",
                "evidence_record_ids": ["evidence_past_1"],
                "temporal_scope": "current",
                "geography_scope": "United States",
                "admitted_at": "2026-07-01T00:00:00Z",
                "deactivated_at": "2026-08-01T00:00:00Z",
                "active": false
              },
              {
                "graph_record_id": "graph_current",
                "knowledge_admission_id": "knowledge_admission_current",
                "validation_target_id": "validation_temporal_shared",
                "claim_class": "validated_relationship",
                "validation_disposition": "supported",
                "confidence_score": 82,
                "confidence_band": "high",
                "epistemic_status": "persistent_knowledge",
                "evidence_record_ids": ["evidence_current_1","evidence_current_2"],
                "temporal_scope": "current",
                "geography_scope": "United States",
                "admitted_at": "2026-08-01T00:00:01Z",
                "active": true
              },
              {
                "graph_record_id": "graph_transition",
                "knowledge_admission_id": "knowledge_admission_transition",
                "validation_target_id": "validation_transition",
                "claim_class": "validated_qualified_relationship",
                "validation_disposition": "partially_supported",
                "confidence_score": 74,
                "confidence_band": "high",
                "epistemic_status": "persistent_qualified_knowledge",
                "evidence_record_ids": ["evidence_transition_1"],
                "temporal_scope": "transition",
                "geography_scope": "Global",
                "admitted_at": "2026-08-20T00:00:00Z",
                "active": true
              },
              {
                "graph_record_id": "graph_future",
                "knowledge_admission_id": "knowledge_admission_future",
                "validation_target_id": "validation_future",
                "claim_class": "scenario_relationship",
                "validation_disposition": "partially_supported",
                "confidence_score": 58,
                "confidence_band": "moderate",
                "epistemic_status": "scenario_knowledge",
                "evidence_record_ids": ["evidence_future_1"],
                "temporal_scope": "future",
                "geography_scope": "Global",
                "admitted_at": "2026-08-25T00:00:00Z",
                "active": true
              }
            ],
            "audit_log": []
          }
          JSON

          (
            cd "$TEST_DIR"
            node scripts/cosmos_temporal_knowledge.cjs \
              --graph temporal-graph-fixture-v0.1.json \
              --reference-time 2026-08-27T12:00:00Z \
              --out knowledge/cosmos/temporal-knowledge-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/temporal-knowledge-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos Temporal Knowledge did not create temporal-knowledge-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected Temporal Knowledge schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "temporal_knowledge_resolved") {
            errors.push(
              `Temporal Knowledge failed: ${payload.status}`
            );
          }

          if (payload.reference_time !== "2026-08-27T12:00:00Z") {
            errors.push(
              `Temporal Knowledge reference_time invalid: ${payload.reference_time}`
            );
          }

          const state = payload.temporal_state || {};

          if (Number(state.record_count) !== 4 ||
              Number(state.past_count) !== 1 ||
              Number(state.current_count) !== 1 ||
              Number(state.transition_count) !== 1 ||
              Number(state.future_count) !== 1 ||
              Number(state.unknown_count) !== 0) {
            errors.push(
              `Temporal state counts invalid: ${JSON.stringify(state)}`
            );
          }

          const timeline = payload.timeline || {};

          if ((timeline.past || []).length !== 1 ||
              timeline.past?.[0]?.graph_record_id !== "graph_past" ||
              timeline.past?.[0]?.active !== false) {
            errors.push(
              "Historical record classification invalid"
            );
          }

          if ((timeline.current || []).length !== 1 ||
              timeline.current?.[0]?.graph_record_id !== "graph_current" ||
              timeline.current?.[0]?.active !== true) {
            errors.push(
              "Current record classification invalid"
            );
          }

          if ((timeline.transition || []).length !== 1 ||
              timeline.transition?.[0]?.graph_record_id !== "graph_transition") {
            errors.push(
              "Transition record classification invalid"
            );
          }

          if ((timeline.future || []).length !== 1 ||
              timeline.future?.[0]?.graph_record_id !== "graph_future" ||
              timeline.future?.[0]?.temporal_contract?.future_is_not_current_fact !== true ||
              timeline.future?.[0]?.temporal_contract?.current_state_claimed !== false) {
            errors.push(
              "Future scenario boundary invalid"
            );
          }

          const future = timeline.future?.[0];

          if (!future ||
              Number(future.confidence_score) !== 58 ||
              future.confidence_band !== "moderate" ||
              future.geography_scope !== "Global" ||
              !Array.isArray(future.evidence_record_ids) ||
              future.evidence_record_ids.length !== 1 ||
              future.evidence_record_ids[0] !== "evidence_future_1") {
            errors.push(
              "Future record lost confidence/geography/evidence lineage"
            );
          }

          const transitions = payload.transitions || [];

          if (!Array.isArray(transitions) ||
              transitions.length !== 1) {
            errors.push(
              `Expected one observed historical transition; got ${transitions.length}`
            );
          } else {
            const t = transitions[0];

            if (t.validation_target_id !== "validation_temporal_shared" ||
                t.from_graph_record_id !== "graph_past" ||
                t.to_graph_record_id !== "graph_current" ||
                t.from_temporal_state !== "past" ||
                t.to_temporal_state !== "current" ||
                t.confidence_changed !== true ||
                Number(t.confidence_delta) !== 14 ||
                t.transition_status !== "observed_from_graph_history") {
              errors.push(
                "Past-to-current transition detection invalid"
              );
            }
          }

          const contract = payload.temporal_contract || {};

          if (contract.past_present_future_separated !== true ||
              contract.inactive_records_preserved_as_history !== true ||
              contract.future_scenarios_not_promoted_to_current_fact !== true ||
              contract.transition_state_supported !== true ||
              contract.evidence_lineage_preserved !== true ||
              contract.confidence_preserved !== true ||
              contract.geography_scope_preserved !== true ||
              contract.temporal_scope_preserved !== true) {
            errors.push(
              "Temporal Knowledge contract invalid"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.mutates_graph !== false ||
              safeguards.deletes_historical_knowledge !== false ||
              safeguards.rewrites_original_records !== false ||
              safeguards.promotes_future_to_fact !== false ||
              safeguards.collapses_temporal_states !== false ||
              safeguards.preserves_source_lineage !== true ||
              safeguards.preserves_confidence !== true ||
              safeguards.preserves_epistemic_status !== true) {
            errors.push(
              "Temporal Knowledge safeguards invalid"
            );
          }

          if (errors.length) {
            console.error(
              "\nCosmos Temporal Knowledge v0.1 preflight errors:"
            );
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos Temporal Knowledge v0.1 isolated test passed.");
          console.log(`Status:       ${payload.status}`);
          console.log(`Past:         ${state.past_count}`);
          console.log(`Current:      ${state.current_count}`);
          console.log(`Transition:   ${state.transition_count}`);
          console.log(`Future:       ${state.future_count}`);
          console.log(`Transitions:  ${state.transition_event_count}`);
          console.log(
            "Past/current/transition/future separation, future-not-fact boundary, lineage preservation and historical transition safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Test Cosmos First-Party Identity v0.1 In Isolated Workspace
        shell: bash
        run: |
          set -euo pipefail

          TEST_DIR="$(mktemp -d)"
          trap 'rm -rf "$TEST_DIR"' EXIT

          mkdir -p "$TEST_DIR/scripts"
          mkdir -p "$TEST_DIR/knowledge/cosmos"

          cp scripts/cosmos_first_party_identity.cjs \
            "$TEST_DIR/scripts/cosmos_first_party_identity.cjs"

          cat > "$TEST_DIR/first-party-identity-fixture-v0.1.json" <<'JSON'
          {
            "schema_version": "0.1",
            "generated_at": "2026-08-27T12:00:00Z",
            "status": "first_party_identity_dataset",
            "entities": [
              {
                "entity_id": "entity_ptd_today",
                "canonical_name": "PTD Today",
                "aliases": ["ptdtoday.com"],
                "entity_type": "organization",
                "protected_identity": true
              },
              {
                "entity_id": "entity_cosmos",
                "canonical_name": "Cosmos",
                "aliases": ["PTD Cosmos"],
                "entity_type": "platform",
                "protected_identity": true
              },
              {
                "entity_id": "entity_fixture_founder",
                "canonical_name": "Fixture Founder",
                "aliases": [],
                "entity_type": "person",
                "protected_identity": true,
                "profiles": [
                  {
                    "profile_type": "linkedin",
                    "url": "https://www.linkedin.com/in/fixture-founder"
                  }
                ]
              },
              {
                "entity_id": "entity_external_claimant",
                "canonical_name": "External Claimant",
                "aliases": [],
                "entity_type": "person",
                "protected_identity": false
              }
            ],
            "relationships": [
              {
                "identity_relationship_id": "identity_ptd_founded_by_fixture",
                "subject": "PTD Today",
                "predicate": "founded_by",
                "object": "Fixture Founder",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/about",
                "source_label": "PTD Today official identity record",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-01-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_cosmos_created_by_fixture",
                "subject": "Cosmos",
                "predicate": "created_by",
                "object": "Fixture Founder",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/cosmos",
                "source_label": "Cosmos official identity record",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-08-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_ptd_develops_cosmos",
                "subject": "PTD Today",
                "predicate": "develops",
                "object": "Cosmos",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/cosmos",
                "source_label": "PTD Today Cosmos relationship",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-08-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_external_conflicting_founder",
                "subject": "PTD Today",
                "predicate": "founded_by",
                "object": "External Claimant",
                "source_type": "external_secondary",
                "source_url": "https://example.com/conflicting-founder-claim",
                "source_label": "Controlled conflicting external claim",
                "verification_status": "unverified",
                "verified_at": null,
                "confidence_score": 45,
                "protected_identity": false,
                "valid_from": "2026-01-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_future_creator_fixture",
                "subject": "Cosmos",
                "predicate": "created_by",
                "object": "External Claimant",
                "source_type": "external_secondary",
                "source_url": "https://example.com/future-identity-claim",
                "source_label": "Controlled future identity claim",
                "verification_status": "unverified",
                "verified_at": null,
                "confidence_score": 30,
                "protected_identity": false,
                "valid_from": "2099-01-01T00:00:00Z",
                "valid_to": null
              }
            ]
          }
          JSON

          (
            cd "$TEST_DIR"

            node scripts/cosmos_first_party_identity.cjs \
              --input first-party-identity-fixture-v0.1.json \
              --reference-time 2026-08-27T12:00:00Z \
              --question "Who founded PTD Today?" \
              --question "Who created Cosmos?" \
              --question "What is the relationship between PTD Today and Cosmos?" \
              --out knowledge/cosmos/first-party-identity-test-v0.1.json
          )

          TEST_DIR="$TEST_DIR" node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const root = process.env.TEST_DIR;
          const file = path.join(
            root,
            "knowledge/cosmos/first-party-identity-test-v0.1.json"
          );

          if (!fs.existsSync(file)) {
            throw new Error(
              "Cosmos First-Party Identity did not create first-party-identity-test-v0.1.json"
            );
          }

          const payload = JSON.parse(fs.readFileSync(file, "utf8"));
          const errors = [];

          if (payload.schema_version !== "0.1") {
            errors.push(
              `Unexpected First-Party Identity schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "first_party_identity_resolved") {
            errors.push(
              `First-Party Identity failed: ${payload.status}`
            );
          }

          if (payload.reference_time !== "2026-08-27T12:00:00Z") {
            errors.push(
              `First-Party Identity reference_time invalid: ${payload.reference_time}`
            );
          }

          const relationships = payload.relationships || [];
          const conflicts = payload.conflicts || [];
          const answers = payload.answers || [];

          if (!Array.isArray(relationships) || relationships.length !== 5) {
            errors.push(
              `Controlled First-Party Identity expected 5 relationships; got ${relationships.length}`
            );
          }

          const firstPartyFounder = relationships.find(
            row =>
              row.identity_relationship_id ===
              "identity_ptd_founded_by_fixture"
          );

          if (!firstPartyFounder ||
              firstPartyFounder.subject !== "PTD Today" ||
              firstPartyFounder.predicate !== "founded_by" ||
              firstPartyFounder.object !== "Fixture Founder" ||
              firstPartyFounder.source_type !== "first_party" ||
              firstPartyFounder.verification_status !== "verified" ||
              firstPartyFounder.temporal_state !== "current" ||
              firstPartyFounder.assertion_scope !==
                "entity_self_asserted_identity" ||
              firstPartyFounder.source_url !==
                "https://www.ptdtoday.com/about") {
            errors.push(
              "Controlled first-party founder record invalid"
            );
          }

          const futureClaim = relationships.find(
            row =>
              row.identity_relationship_id ===
              "identity_future_creator_fixture"
          );

          if (!futureClaim ||
              futureClaim.temporal_state !== "future") {
            errors.push(
              "Future identity relationship was not classified as future"
            );
          }

          if (!Array.isArray(conflicts) || conflicts.length !== 1) {
            errors.push(
              `Expected one current identity conflict; got ${conflicts.length}`
            );
          } else {
            const conflict = conflicts[0];

            if (conflict.predicate !== "founded_by" ||
                conflict.temporal_state !== "current" ||
                conflict.first_party_present !== true ||
                conflict.external_present !== true ||
                conflict.resolution_status !== "unresolved" ||
                conflict.silent_overwrite_permitted !== false ||
                conflict.records_preserved !== true ||
                !Array.isArray(conflict.competing_objects) ||
                !conflict.competing_objects.includes("Fixture Founder") ||
                !conflict.competing_objects.includes("External Claimant")) {
              errors.push(
                "Controlled identity conflict preservation invalid"
              );
            }
          }

          if (!Array.isArray(answers) || answers.length !== 3) {
            errors.push(
              `Expected 3 controlled identity answers; got ${answers.length}`
            );
          }

          const founderAnswer = answers.find(
            row => row.predicate === "founded_by"
          );

          if (!founderAnswer ||
              founderAnswer.answer_status !== "resolved" ||
              founderAnswer.selected_relationship?.object !== "Fixture Founder" ||
              founderAnswer.selected_relationship?.source_type !== "first_party" ||
              founderAnswer.selected_relationship?.verification_status !== "verified" ||
              founderAnswer.conflicts?.length !== 1 ||
              founderAnswer.answer_contract?.first_party_selected_as_self_asserted_identity !== true ||
              founderAnswer.answer_contract?.first_party_not_universal_truth !== true ||
              founderAnswer.answer_contract?.conflict_disclosed !== true ||
              founderAnswer.answer_contract?.external_disagreement_preserved !== true ||
              founderAnswer.answer_contract?.provenance_required !== true) {
            errors.push(
              "Founder answer did not preserve first-party/provenance/conflict boundaries"
            );
          }

          const creatorAnswer = answers.find(
            row => row.predicate === "created_by"
          );

          if (!creatorAnswer ||
              creatorAnswer.answer_status !== "resolved" ||
              creatorAnswer.selected_relationship?.object !== "Fixture Founder" ||
              creatorAnswer.selected_relationship?.temporal_state !== "current" ||
              creatorAnswer.selected_relationship?.source_type !== "first_party") {
            errors.push(
              "Cosmos creator answer invalid"
            );
          }

          const relationshipAnswer = answers.find(
            row => row.predicate === "develops"
          );

          if (!relationshipAnswer ||
              relationshipAnswer.answer_status !== "resolved" ||
              relationshipAnswer.selected_relationship?.subject !== "PTD Today" ||
              relationshipAnswer.selected_relationship?.object !== "Cosmos" ||
              relationshipAnswer.selected_relationship?.source_type !== "first_party") {
            errors.push(
              "PTD Today/Cosmos relationship answer invalid"
            );
          }

          const contract = payload.identity_contract || {};

          if (contract.first_party_means_self_asserted_identity_authority !== true ||
              contract.first_party_does_not_mean_universal_truth !== true ||
              contract.protected_identity_records_preserved !== true ||
              contract.external_conflicts_preserved !== true ||
              contract.silent_conflict_overwrite_forbidden !== true ||
              contract.temporal_identity_supported !== true ||
              contract.provenance_required !== true) {
            errors.push(
              "First-Party Identity contract invalid"
            );
          }

          const safeguards = payload.safeguards || {};

          if (safeguards.performs_external_search !== false ||
              safeguards.calls_openai_or_external_api !== false ||
              safeguards.mutates_graph !== false ||
              safeguards.deletes_identity_history !== false ||
              safeguards.overwrites_conflicting_external_claims !== false ||
              safeguards.treats_first_party_as_universal_truth !== false ||
              safeguards.promotes_future_identity_to_current !== false ||
              safeguards.preserves_provenance !== true ||
              safeguards.preserves_conflicts !== true) {
            errors.push(
              "First-Party Identity safeguards invalid"
            );
          }

          if (errors.length) {
            console.error(
              "\nCosmos First-Party Identity v0.1 preflight errors:"
            );
            for (const error of errors) {
              console.error(`- ${error}`);
            }
            process.exit(1);
          }

          console.log("");
          console.log("Cosmos First-Party Identity v0.1 isolated test passed.");
          console.log(`Status:              ${payload.status}`);
          console.log(`Relationships:       ${relationships.length}`);
          console.log(`Current conflicts:   ${conflicts.length}`);
          console.log(`Resolved answers:    ${answers.filter(x => x.answer_status === "resolved").length}`);
          console.log(
            "Founder/creator resolution, provenance, temporal identity, conflict preservation and first-party-not-universal-truth safeguards passed."
          );
          console.log(
            "Repository files were not modified by the isolated test."
          );
          NODE

      - name: Generate Real Cosmos State Output
        if: ${{ inputs.write_cosmos_state == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating real Cosmos State from current committed Cosmos data."
          echo "No OpenAI/API generation is executed by this step."

          node scripts/cosmos_state_builder.cjs

          test -f knowledge/cosmos/cosmos-state-current.json

          node <<'NODE'
          const fs = require("fs");

          const file = "knowledge/cosmos/cosmos-state-current.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.6") {
            throw new Error(
              `Unexpected Cosmos State schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "ready") {
            throw new Error(`Invalid Cosmos State status: ${payload.status}`);
          }

          if (
            payload.methodology?.reasoning_mode !==
            "deterministic_cosmos_state_synthesis"
          ) {
            throw new Error("Cosmos State methodology missing");
          }

          if (
            payload.safeguards?.forecast !== false ||
            payload.safeguards?.evidence_quality_upgraded !== false
          ) {
            throw new Error("Cosmos State safeguards invalid");
          }

          if (!Array.isArray(payload.attention?.clusters)) {
            throw new Error("Cosmos State attention.clusters[] missing");
          }

          if (payload.attention.clusters.length > 5) {
            throw new Error(
              `Cosmos State must retain at most 5 distinct bounded systems; found ${payload.attention.clusters.length}`
            );
          }

          for (const cluster of payload.attention.clusters) {
            if (!Array.isArray(cluster.member_attention_ids) || cluster.member_attention_ids.length < 2) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} is not a real multi-member bounded system`
              );
            }

            if (cluster.member_attention_ids.length > 7) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} exceeds v0.6 7-member bound`
              );
            }

            if (
              !cluster.anchor_attention_id ||
              !["emergence", "pattern"].includes(cluster.anchor_type) ||
              !Array.isArray(cluster.anchor_entity_ids) ||
              cluster.anchor_entity_ids.length < 1
            ) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} missing valid v0.6 anchor identity`
              );
            }

            if (typeof cluster.cross_family !== "boolean") {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} missing cross_family`
              );
            }

            if (!cluster.member_type_counts || typeof cluster.member_type_counts !== "object") {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} missing member_type_counts`
              );
            }

            if (Number(cluster.member_type_counts.impact || 0) < 1) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} must contain at least 1 Impact member in v0.6`
              );
            }

            if (Number(cluster.member_type_counts.emergence || 0) > 3) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} exceeds v0.6 Emergence cap`
              );
            }

            const interpretation = String(cluster.interpretation || "").toLowerCase();
            if (
              !interpretation.includes("qualifies directly against the original anchor") ||
              !interpretation.includes("membership is not transitive") ||
              !interpretation.includes("anchor identity is preserved") ||
              !interpretation.includes("relevant impact signals are explicitly admitted") ||
              !interpretation.includes("near-duplicate emergence signals are suppressed") ||
              !interpretation.includes("does not establish new causality") ||
              !interpretation.includes("verification") ||
              !interpretation.includes("forecast")
            ) {
              throw new Error(
                `Cosmos State cluster ${cluster.cluster_id || "unknown"} missing v0.6 bounded-system safeguard`
              );
            }
          }

          const counts = payload.counts || {};

          if (Number(counts.attention_cluster_members_max || 0) > 7) {
            throw new Error("Cosmos State attention_cluster_members_max exceeds 7");
          }

          if (Number(counts.attention_distinct_system_target_max || 0) !== 5) {
            throw new Error("Cosmos State attention_distinct_system_target_max must equal 5");
          }

          if (
            Number(counts.attention_cross_family_clusters || 0) >
            Number(counts.attention_clusters || 0)
          ) {
            throw new Error("Cosmos State cross-family cluster count invalid");
          }

          if (
            Number(counts.attention_clusters_with_impact || 0) !==
            Number(counts.attention_clusters || 0)
          ) {
            throw new Error(
              `Cosmos State v0.6 requires every retained system to contain Impact; ` +
              `with-impact=${counts.attention_clusters_with_impact}, ` +
              `clusters=${counts.attention_clusters}`
            );
          }

          if (
            Number(counts.attention_cluster_emergence_members_max || 0) > 3
          ) {
            throw new Error(
              "Cosmos State v0.6 attention_cluster_emergence_members_max exceeds 3"
            );
          }

          if (
            Number(counts.attention_patterns || 0) > 8 ||
            Number(counts.attention_emergences || 0) > 6 ||
            Number(counts.attention_impacts || 0) > 6
          ) {
            throw new Error("Cosmos State attention diversity quota invalid");
          }

          console.log("");
          console.log("Real Cosmos State output created.");
          console.log(`Date:       ${payload.date_utc}`);
          console.log(`Attention:  ${payload.counts?.attention_retained ?? 0}`);
          console.log(`Clusters:   ${payload.counts?.attention_clusters ?? 0}`);
          console.log(`With Impact:${payload.counts?.attention_clusters_with_impact ?? 0}`);
          console.log(`Max Emerg.: ${payload.counts?.attention_cluster_emergence_members_max ?? 0}`);
          console.log(`Output:     ${file}`);
          NODE

      - name: Commit Real Cosmos State Output
        if: ${{ inputs.write_cosmos_state == true }}
        shell: bash
        run: |
          set -euo pipefail

          DATE_UTC="$(node -p "require('./knowledge/cosmos/cosmos-state-current.json').date_utc")"
          HISTORY_FILE="knowledge/cosmos/cosmos-state-history/${DATE_UTC}.json"

          if [ ! -f "$HISTORY_FILE" ]; then
            echo "ERROR: Expected Cosmos State history file was not generated: $HISTORY_FILE"
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            knowledge/cosmos/cosmos-state-current.json \
            "$HISTORY_FILE"

          if git diff --cached --quiet; then
            echo "No Cosmos State changes to commit."
          else
            git commit -m "Update Cosmos state ${DATE_UTC}"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Real Cosmos State output is now committed to the repository."
          echo "No OpenAI generation was executed."

      - name: Generate Real Cosmos Core v0.1 Test Output
        if: ${{ inputs.write_cosmos_core_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Core v0.1 output from frozen Cosmos State."
          echo "No OpenAI/API generation is executed by this step."

          cat > /tmp/cosmos-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-core-input-v0.1.json \
            --out knowledge/cosmos/core-test-v0.1.json

          test -f knowledge/cosmos/core-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file = "knowledge/cosmos/core-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Core schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "context_resolved") {
            throw new Error(
              `Cosmos Core failed to resolve controlled context: ${payload.status}`
            );
          }

          if (payload.knowledge_state?.cosmos_state_schema_version !== "0.6") {
            throw new Error(
              "Cosmos Core did not consume frozen Cosmos State v0.6"
            );
          }

          if (payload.infinite_state?.terminal !== false ||
              payload.infinite_state?.continuation_allowed !== true) {
            throw new Error(
              "Cosmos Core Infinite State continuation safeguards invalid"
            );
          }

          if (payload.safeguards?.creates_new_causal_claims !== false ||
              payload.safeguards?.upgrades_evidence_quality !== false ||
              payload.safeguards?.forecast_generated !== false ||
              payload.safeguards?.sector_whitelist_present !== false) {
            throw new Error(
              "Cosmos Core safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Core v0.1 output created.");
          console.log(`Status:        ${payload.status}`);
          console.log(
            `Starting:      ${(payload.resolution?.starting_points || []).map(x => x.name).join(" | ")}`
          );
          console.log(
            `Systems:       ${(payload.selected_context?.systems || []).length}`
          );
          console.log(
            `Relationships: ${(payload.selected_context?.relationships || []).length}`
          );
          console.log(`Output:        ${file}`);
          NODE

      - name: Commit Real Cosmos Core v0.1 Test Output
        if: ${{ inputs.write_cosmos_core_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/core-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Core test changes to commit."
          else
            git commit -m "Add Cosmos Core v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Core v0.1 output is now committed."
          echo "No OpenAI generation was executed."

      - name: Generate Real Cosmos Attention v0.2 Test Output
        if: ${{ inputs.write_cosmos_attention_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Attention v0.2 output."
          echo "No OpenAI/API generation is executed by this step."

          cat > /tmp/cosmos-attention-core-input-v0.2.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_attention_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-attention-core-input-v0.2.json \
            --out /tmp/core-for-attention-v0.2.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-attention-v0.2.json \
            --out knowledge/cosmos/attention-test-v0.2.json

          test -f knowledge/cosmos/attention-test-v0.2.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/attention-test-v0.2.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.2") {
            throw new Error(
              `Unexpected Cosmos Attention schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "attention_resolved") {
            throw new Error(
              `Cosmos Attention failed to resolve: ${payload.status}`
            );
          }

          const active = payload.active_context || {};
          const budget = payload.attention_state?.budget || {};
          const suppressed = payload.attention_state?.suppressed_count || {};

          if ((active.primary_pathways || []).length < 1 ||
              (active.primary_pathways || []).length >
                Number(budget.primary_pathways || 0)) {
            throw new Error("Cosmos Attention primary pathway count invalid");
          }

          const primaryPathways = active.primary_pathways || [];
          const rawEntityPattern = /^ent_[a-z0-9]+$/i;

          for (const pathway of primaryPathways) {
            const fromName = String(pathway.from?.name || "").trim();
            const toName = String(pathway.to?.name || "").trim();

            if (!fromName || !toName ||
                rawEntityPattern.test(fromName) ||
                rawEntityPattern.test(toName) ||
                fromName === pathway.from?.entity_id ||
                toName === pathway.to?.entity_id) {
              throw new Error(
                `Cosmos Attention v0.2 primary pathway exposes unresolved entity: ${pathway.pathway_id || "unknown"}`
              );
            }

            if (!Number.isFinite(Number(pathway.relationship?.semantic_adjustment))) {
              throw new Error(
                `Cosmos Attention v0.2 pathway missing semantic adjustment: ${pathway.pathway_id || "unknown"}`
              );
            }
          }

          const locatedInPrimary = primaryPathways.filter(
            pathway =>
              String(pathway.relationship?.type || "").toUpperCase() === "LOCATED_IN"
          );

          const consequencePrimary = primaryPathways.filter(pathway =>
            ["AFFECTS", "IMPACTS", "DRIVES", "REQUIRES", "DEPENDS_ON",
             "CONSTRAINS", "ENABLES", "CAUSES", "INCREASES", "DECREASES",
             "SUPPLIES", "SUPPORTS"].includes(
              String(pathway.relationship?.type || "").toUpperCase()
            )
          );

          if (locatedInPrimary.length > 0 && consequencePrimary.length > 0) {
            const bestLocated = Math.max(
              ...locatedInPrimary.map(x => Number(x.score || 0))
            );
            const bestConsequence = Math.max(
              ...consequencePrimary.map(x => Number(x.score || 0))
            );

            if (bestLocated >= bestConsequence) {
              throw new Error(
                "Cosmos Attention v0.2 LOCATED_IN pathway outranks consequence pathway for impact question"
              );
            }
          }

          if ((active.patterns || []).length > Number(budget.patterns || 0) ||
              (active.emergences || []).length > Number(budget.emergences || 0) ||
              (active.impacts || []).length > Number(budget.impacts || 0) ||
              (active.relationships || []).length > Number(budget.relationships || 0)) {
            throw new Error(
              "Cosmos Attention active context exceeds configured budget"
            );
          }

          for (const key of ["patterns", "emergences", "impacts", "relationships"]) {
            const discovered = Number(suppressed[key]?.discovered || 0);
            const activeCount = Number(suppressed[key]?.active || 0);
            const expandable =
              Number(suppressed[key]?.available_for_expansion || 0);

            if (activeCount + expandable !== discovered) {
              throw new Error(
                `Cosmos Attention ${key} existence-preservation invariant failed`
              );
            }
          }

          if (payload.infinite_state?.terminal !== false ||
              payload.infinite_state?.continuation_allowed !== true ||
              payload.infinite_state?.attention_can_move !== true ||
              payload.infinite_state?.underlying_context_preserved !== true) {
            throw new Error("Cosmos Attention Infinite State safeguards invalid");
          }

          if (payload.safeguards?.creates_new_causal_claims !== false ||
              payload.safeguards?.upgrades_evidence_quality !== false ||
              payload.safeguards?.forecast_generated !== false ||
              payload.safeguards?.sector_whitelist_present !== false ||
              payload.safeguards?.visibility_suppression_only !== true ||
              payload.safeguards?.existence_preserved !== true ||
              payload.safeguards?.primary_pathways_require_resolved_entities !== true ||
              payload.safeguards?.descriptive_relationships_are_intent_weighted !== true ||
              payload.safeguards?.source_lineage_preserved !== true) {
            throw new Error("Cosmos Attention safeguards invalid");
          }

          console.log("");
          console.log("Real controlled Cosmos Attention v0.2 output created.");
          console.log(`Status:           ${payload.status}`);
          console.log(`Primary paths:    ${(active.primary_pathways || []).length}`);
          console.log(
            `Patterns:         ${suppressed.patterns?.discovered || 0} -> ${(active.patterns || []).length}`
          );
          console.log(
            `Emergences:       ${suppressed.emergences?.discovered || 0} -> ${(active.emergences || []).length}`
          );
          console.log(
            `Impacts:          ${suppressed.impacts?.discovered || 0} -> ${(active.impacts || []).length}`
          );
          console.log(
            `Relationships:    ${suppressed.relationships?.discovered || 0} -> ${(active.relationships || []).length}`
          );
          console.log(`Output:           ${file}`);
          NODE

      - name: Commit Real Cosmos Attention v0.2 Test Output
        if: ${{ inputs.write_cosmos_attention_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/attention-test-v0.2.json

          if git diff --cached --quiet; then
            echo "No Cosmos Attention test changes to commit."
          else
            git commit -m "Add Cosmos Attention v0.2 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Attention v0.2 output is now committed."
          echo "No OpenAI generation was executed."

      - name: Generate Real Cosmos Consequence v0.1 Test Output
        if: ${{ inputs.write_cosmos_consequence_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Consequence v0.1 output."
          echo "No OpenAI/API generation is executed by this step."

          cat > /tmp/cosmos-consequence-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_consequence_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-consequence-core-input-v0.1.json \
            --out /tmp/core-for-consequence-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-consequence-v0.1.json \
            --out /tmp/attention-for-consequence-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-consequence-v0.2.json \
            --out knowledge/cosmos/consequence-test-v0.1.json

          test -f knowledge/cosmos/consequence-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/consequence-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Consequence schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "consequence_frontier_resolved") {
            throw new Error(
              `Cosmos Consequence failed: ${payload.status}`
            );
          }

          if (payload.source_attention?.schema_version !== "0.2") {
            throw new Error(
              "Cosmos Consequence did not consume Attention v0.2"
            );
          }

          const rows = payload.consequence_field?.consequences || [];
          if (rows.length < 2) {
            throw new Error(
              "Controlled consequence output did not produce multiple consequences"
            );
          }

          const distances = rows
            .map(x => Number(x.butterfly_distance))
            .filter(Number.isFinite);

          if (!distances.includes(1) || !distances.some(x => x >= 2)) {
            throw new Error(
              "Controlled consequence output lacks direct and deeper ripple layers"
            );
          }

          if (payload.butterfly_state?.conceptual_distance_limit !== null ||
              payload.butterfly_state?.runtime_distance_is_computational_budget_only !== true ||
              payload.butterfly_state?.continuation_possible !== true ||
              payload.butterfly_state?.frontier_is_terminal !== false ||
              payload.butterfly_state?.consequence_can_become_new_origin !== true) {
            throw new Error(
              "Cosmos Consequence butterfly-state invariants invalid"
            );
          }

          if (payload.safeguards?.creates_new_causal_claims !== false ||
              payload.safeguards?.upgrades_evidence_quality !== false ||
              payload.safeguards?.numeric_forecast_generated !== false ||
              payload.safeguards?.unidentified_material_is_not_assumed !== true ||
              payload.safeguards?.reverse_relationships_are_not_treated_as_forward_causality !== true ||
              payload.safeguards?.alternatives_require_validation !== true ||
              payload.safeguards?.runtime_budget_is_not_conceptual_limit !== true ||
              payload.safeguards?.source_lineage_preserved !== true) {
            throw new Error(
              "Cosmos Consequence safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Consequence v0.1 output created.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Consequences:      ${rows.length}`);
          console.log(`Max distance seen: ${Math.max(...distances)}`);
          console.log(`Actors:            ${(payload.affected_actors || []).length}`);
          console.log(`Alternatives:      ${(payload.alternatives?.items || []).length}`);
          console.log(`Knowledge gaps:    ${(payload.knowledge_gaps?.items || []).length}`);
          console.log(`Output:            ${file}`);
          NODE

      - name: Commit Real Cosmos Consequence v0.1 Test Output
        if: ${{ inputs.write_cosmos_consequence_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/consequence-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Consequence test changes to commit."
          else
            git commit -m "Add Cosmos Consequence v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Consequence v0.1 output is now committed."
          echo "No OpenAI generation was executed."

      - name: Generate Real Cosmos Discovery v0.1 Test Output
        if: ${{ inputs.write_cosmos_discovery_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Discovery v0.1 output."
          echo "No OpenAI/API generation or external search is executed by this step."

          cat > /tmp/cosmos-discovery-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_discovery_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-discovery-core-input-v0.1.json \
            --out /tmp/core-for-discovery-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-discovery-v0.1.json \
            --out /tmp/attention-for-discovery-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-discovery-v0.2.json \
            --out /tmp/consequence-for-discovery-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-discovery-v0.1.json \
            --out knowledge/cosmos/discovery-test-v0.1.json

          test -f knowledge/cosmos/discovery-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/discovery-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Discovery schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "discovery_targets_resolved") {
            throw new Error(
              `Cosmos Discovery failed: ${payload.status}`
            );
          }

          if (payload.source_consequence?.schema_version !== "0.1") {
            throw new Error(
              "Cosmos Discovery did not consume Consequence v0.1"
            );
          }

          const targets =
            payload.discovery_targets?.items ||
            payload.discovery_targets ||
            [];

          const queue = payload.next_acquisition_queue || [];

          if (!Array.isArray(targets) || targets.length < 1) {
            throw new Error("Controlled Discovery output produced no discovery targets");
          }

          if (!Array.isArray(queue) || queue.length < 1) {
            throw new Error("Controlled Discovery output produced no acquisition queue");
          }

          if (payload.curiosity_state?.continuation_possible !== true) {
            throw new Error(
              "Cosmos Discovery did not preserve consequence continuation"
            );
          }

          if (payload.curiosity_state?.conceptual_distance_limit !== null) {
            throw new Error(
              "Cosmos Discovery imposed a conceptual distance limit"
            );
          }

          const reopenableTargets = targets.filter(
            x => x.reopens_frontier_if_resolved === true
          );

          if (reopenableTargets.length < 1) {
            throw new Error(
              "Controlled Discovery output produced no frontier-reopening target"
            );
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.invents_missing_entities !== false ||
              payload.safeguards?.upgrades_evidence_quality !== false ||
              payload.safeguards?.converts_unknown_to_fact !== false ||
              payload.safeguards?.acquisition_targets_are_requests_for_knowledge_not_claims !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.consequence_frontier_may_be_reopened !== true) {
            throw new Error("Cosmos Discovery safeguards invalid");
          }

          console.log("");
          console.log("Real controlled Cosmos Discovery v0.1 output created.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Discovery targets: ${targets.length}`);
          console.log(`Acquisition queue: ${queue.length}`);
          console.log(
            `Reopenable:         ${targets.filter(x => x.reopens_frontier_if_resolved === true).length}`
          );
          console.log(`Output:            ${file}`);
          NODE

      - name: Commit Real Cosmos Discovery v0.1 Test Output
        if: ${{ inputs.write_cosmos_discovery_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/discovery-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Discovery test changes to commit."
          else
            git commit -m "Add Cosmos Discovery v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Discovery v0.1 output is now committed."
          echo "No OpenAI generation or external search was executed."

      - name: Generate Real Cosmos Acquisition v0.1 Test Output
        if: ${{ inputs.write_cosmos_acquisition_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Acquisition v0.1 output."
          echo "No OpenAI/API generation, external search, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-acquisition-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_acquisition_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-acquisition-core-input-v0.1.json \
            --out /tmp/core-for-acquisition-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-acquisition-v0.1.json \
            --out /tmp/attention-for-acquisition-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-acquisition-v0.2.json \
            --out /tmp/consequence-for-acquisition-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-acquisition-v0.1.json \
            --out /tmp/discovery-for-acquisition-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-acquisition-v0.1.json \
            --out knowledge/cosmos/acquisition-test-v0.1.json

          test -f knowledge/cosmos/acquisition-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/acquisition-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Acquisition schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "acquisition_plans_resolved") {
            throw new Error(
              `Cosmos Acquisition failed: ${payload.status}`
            );
          }

          if (payload.source_discovery?.schema_version !== "0.1") {
            throw new Error(
              "Cosmos Acquisition did not consume Discovery v0.1"
            );
          }

          const plans = payload.acquisition_plans || [];
          const executorQueue = payload.future_executor_queue || [];

          if (!Array.isArray(plans) || plans.length < 1) {
            throw new Error(
              "Controlled Acquisition output produced no acquisition plans"
            );
          }

          if (!Array.isArray(executorQueue) ||
              executorQueue.length !== plans.length) {
            throw new Error(
              "Controlled Acquisition output executor queue invalid"
            );
          }

          if (payload.acquisition_state?.execution_mode !== "planned_only" ||
              payload.acquisition_state?.external_execution_connected !== false ||
              payload.acquisition_state?.evidence_validation_required_before_graph_admission !== true ||
              payload.acquisition_state?.conceptual_distance_limit !== null ||
              payload.acquisition_state?.continuation_possible !== true) {
            throw new Error(
              "Cosmos Acquisition state invariants invalid"
            );
          }

          for (const plan of plans) {
            if (!plan.discovery_target_id ||
                !Array.isArray(plan.query_templates) ||
                plan.query_templates.length < 1 ||
                !Array.isArray(plan.source_strategy) ||
                plan.source_strategy.length < 1 ||
                plan.evidence_contract?.evidence_is_not_knowledge_until_validated !== true ||
                plan.evidence_contract?.knowledge_admission_status !== "not_evaluated" ||
                plan.execution?.mode !== "planned_only" ||
                plan.execution?.network_execution_performed !== false ||
                plan.execution?.executor_status !== "not_connected" ||
                Number(plan.execution?.result_count) !== 0) {
              throw new Error(
                `Cosmos Acquisition plan invalid: ${plan.acquisition_plan_id || "unknown"}`
              );
            }
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.invents_entities !== false ||
              payload.safeguards?.upgrades_evidence_quality !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.converts_acquisition_plan_to_fact !== false ||
              payload.safeguards?.evidence_requires_validation !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.discovery_lineage_preserved !== true ||
              payload.safeguards?.frontier_reopens_only_after_validation !== true) {
            throw new Error(
              "Cosmos Acquisition safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Acquisition v0.1 output created.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Plans:             ${plans.length}`);
          console.log(`Executor queue:    ${executorQueue.length}`);
          console.log(
            `Reopening plans:   ${plans.filter(x => x.frontier_effect?.reopens_frontier_if_resolved === true).length}`
          );
          console.log(`Output:            ${file}`);
          NODE

      - name: Commit Real Cosmos Acquisition v0.1 Test Output
        if: ${{ inputs.write_cosmos_acquisition_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/acquisition-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Acquisition test changes to commit."
          else
            git commit -m "Add Cosmos Acquisition v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Acquisition v0.1 output is now committed."
          echo "No OpenAI generation, external search, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Acquisition Applicability v0.1 Test Output
        if: ${{ inputs.write_cosmos_applicability_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Acquisition Applicability v0.1 output."
          echo "No OpenAI/API generation, external search, evidence execution, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-applicability-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_applicability_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-applicability-core-input-v0.1.json \
            --out /tmp/core-for-applicability-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-applicability-v0.1.json \
            --out /tmp/attention-for-applicability-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-applicability-v0.2.json \
            --out /tmp/consequence-for-applicability-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-applicability-v0.1.json \
            --out /tmp/discovery-for-applicability-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-applicability-v0.1.json \
            --out /tmp/acquisition-for-applicability-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-applicability-v0.1.json \
            --out knowledge/cosmos/applicability-test-v0.1.json

          test -f knowledge/cosmos/applicability-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/applicability-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Applicability schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "acquisition_applicability_resolved") {
            throw new Error(
              `Cosmos Applicability failed: ${payload.status}`
            );
          }

          if (payload.source_acquisition?.schema_version !== "0.1") {
            throw new Error(
              "Cosmos Applicability did not consume Acquisition v0.1"
            );
          }

          const evaluations = payload.evaluations || [];
          const executable = payload.executable_queue || [];
          const decompositions = payload.decomposition_queue || [];

          if (!Array.isArray(evaluations) || evaluations.length < 1) {
            throw new Error(
              "Controlled Applicability output produced no evaluations"
            );
          }

          if (!Array.isArray(executable) ||
              !Array.isArray(decompositions)) {
            throw new Error(
              "Controlled Applicability queues invalid"
            );
          }

          if (payload.applicability_state?.external_execution_connected !== false ||
              payload.applicability_state?.conceptual_distance_limit !== null ||
              payload.applicability_state?.continuation_possible !== true) {
            throw new Error(
              "Cosmos Applicability state invariants invalid"
            );
          }

          const aiBlocks = evaluations.filter(row =>
            row.applicability_status === "decomposition_required" &&
            /artificial intelligence/i.test(row.subject) &&
            ["identify_components", "identify_materials", "identify_capacity"]
              .includes(row.target_type)
          );

          if (aiBlocks.length < 1) {
            throw new Error(
              "Controlled Applicability output did not block abstract AI physical investigation"
            );
          }

          const transformerApplicable = evaluations.filter(row =>
            row.applicability_status === "applicable" &&
            /transformers/i.test(row.subject) &&
            ["identify_components", "identify_materials", "identify_capacity"]
              .includes(row.target_type)
          );

          if (transformerApplicable.length < 1) {
            throw new Error(
              "Controlled Applicability output did not retain Transformer physical investigation"
            );
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.invents_entities !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.mutates_source_acquisition_plans !== false ||
              payload.safeguards?.executes_decomposition_hints_as_facts !== false ||
              payload.safeguards?.abstract_targets_blocked_from_physical_execution !== true ||
              payload.safeguards?.source_lineage_preserved !== true) {
            throw new Error(
              "Cosmos Applicability safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Acquisition Applicability v0.1 output created.");
          console.log(`Status:                 ${payload.status}`);
          console.log(`Evaluations:            ${evaluations.length}`);
          console.log(`Executable:             ${executable.length}`);
          console.log(`Decomposition required: ${decompositions.length}`);
          console.log(`Output:                 ${file}`);
          NODE

      - name: Commit Real Cosmos Acquisition Applicability v0.1 Test Output
        if: ${{ inputs.write_cosmos_applicability_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/applicability-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Applicability test changes to commit."
          else
            git commit -m "Add Cosmos Acquisition Applicability v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Acquisition Applicability v0.1 output is now committed."
          echo "No OpenAI generation, external search, evidence execution, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Decomposition v0.1 Test Output
        if: ${{ inputs.write_cosmos_decomposition_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Decomposition v0.1 output."
          echo "No OpenAI/API generation, external search, candidate validation, execution, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-decomposition-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_decomposition_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-decomposition-core-input-v0.1.json \
            --out /tmp/core-for-decomposition-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-decomposition-v0.1.json \
            --out /tmp/attention-for-decomposition-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-decomposition-v0.2.json \
            --out /tmp/consequence-for-decomposition-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-decomposition-v0.1.json \
            --out /tmp/discovery-for-decomposition-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-decomposition-v0.1.json \
            --out /tmp/acquisition-for-decomposition-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-decomposition-v0.1.json \
            --out /tmp/applicability-for-decomposition-v0.1.json

          node scripts/cosmos_decomposition.cjs \
            --input /tmp/applicability-for-decomposition-v0.1.json \
            --out knowledge/cosmos/decomposition-test-v0.1.json

          test -f knowledge/cosmos/decomposition-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/decomposition-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Decomposition schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "decomposition_candidates_resolved") {
            throw new Error(
              `Cosmos Decomposition failed: ${payload.status}`
            );
          }

          if (payload.source_applicability?.schema_version !== "0.1") {
            throw new Error(
              "Cosmos Decomposition did not consume Applicability v0.1"
            );
          }

          const decompositions = payload.decompositions || [];
          const candidates = decompositions.flatMap(
            row => Array.isArray(row.candidates) ? row.candidates : []
          );
          const queue = payload.candidate_validation_queue || [];

          if (!Array.isArray(decompositions) || decompositions.length < 1 ||
              candidates.length < 1 ||
              !Array.isArray(queue) ||
              queue.length !== candidates.length) {
            throw new Error(
              "Controlled Decomposition output structure invalid"
            );
          }

          if (payload.decomposition_state?.validated_candidate_count !== 0 ||
              payload.decomposition_state?.executable_candidate_count !== 0 ||
              payload.decomposition_state?.external_execution_connected !== false ||
              payload.decomposition_state?.conceptual_distance_limit !== null ||
              payload.decomposition_state?.continuation_possible !== true) {
            throw new Error(
              "Cosmos Decomposition state invariants invalid"
            );
          }

          for (const candidate of candidates) {
            if (candidate.epistemic_status !== "provisional_candidate" ||
                candidate.validated !== false ||
                candidate.executable !== false ||
                !Array.isArray(candidate.validation_requirements) ||
                !Array.isArray(candidate.proposed_investigation_types)) {
              throw new Error(
                `Cosmos Decomposition candidate invalid: ${candidate.decomposition_candidate_id || "unknown"}`
              );
            }
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.promotes_candidates_to_facts !== false ||
              payload.safeguards?.executes_candidates !== false ||
              payload.safeguards?.validates_candidates !== false ||
              payload.safeguards?.parent_acquisition_plans_remain_blocked !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.unlimited_conceptual_continuation_preserved !== true) {
            throw new Error(
              "Cosmos Decomposition safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Decomposition v0.1 output created.");
          console.log(`Status:           ${payload.status}`);
          console.log(`Decompositions:   ${decompositions.length}`);
          console.log(`Candidates:       ${candidates.length}`);
          console.log(`Validation queue: ${queue.length}`);
          console.log(`Output:           ${file}`);
          NODE

      - name: Commit Real Cosmos Decomposition v0.1 Test Output
        if: ${{ inputs.write_cosmos_decomposition_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/decomposition-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Decomposition test changes to commit."
          else
            git commit -m "Add Cosmos Decomposition v0.1 controlled test output"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Decomposition v0.1 output is now committed."
          echo "No OpenAI generation, external search, candidate validation, execution, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Candidate Validation Planner v0.1 Test Output
        if: ${{ inputs.write_cosmos_candidate_validation_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Candidate Validation Planner v0.1 output."
          echo "No OpenAI/API generation, external search, candidate validation, execution, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-candidate-validation-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_candidate_validation_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-candidate-validation-core-input-v0.1.json \
            --out /tmp/core-for-candidate-validation-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-candidate-validation-v0.1.json \
            --out /tmp/attention-for-candidate-validation-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-candidate-validation-v0.2.json \
            --out /tmp/consequence-for-candidate-validation-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-candidate-validation-v0.1.json \
            --out /tmp/discovery-for-candidate-validation-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-candidate-validation-v0.1.json \
            --out /tmp/acquisition-for-candidate-validation-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-candidate-validation-v0.1.json \
            --out /tmp/applicability-for-candidate-validation-v0.1.json

          node scripts/cosmos_decomposition.cjs \
            --input /tmp/applicability-for-candidate-validation-v0.1.json \
            --out /tmp/decomposition-for-candidate-validation-v0.1.json

          node scripts/cosmos_candidate_validation.cjs \
            --input /tmp/decomposition-for-candidate-validation-v0.1.json \
            --out knowledge/cosmos/candidate-validation-test-v0.1.json

          test -f knowledge/cosmos/candidate-validation-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/candidate-validation-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Candidate Validation schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "candidate_validation_targets_resolved") {
            throw new Error(
              `Candidate Validation Planner failed: ${payload.status}`
            );
          }

          if (payload.source_decomposition?.schema_version !== "0.1") {
            throw new Error(
              "Candidate Validation Planner did not consume Decomposition v0.1"
            );
          }

          const state = payload.validation_state || {};
          const targets = payload.validation_targets || [];
          const queue = payload.future_evidence_queue || [];

          if (Number(state.raw_candidate_count) !== 21 ||
              Number(state.unique_validation_target_count) !== 7 ||
              Number(state.duplicate_candidate_count) !== 14) {
            throw new Error(
              `Candidate Validation consolidation invariant failed: raw=${state.raw_candidate_count}, unique=${state.unique_validation_target_count}, duplicates=${state.duplicate_candidate_count}`
            );
          }

          if (!Array.isArray(targets) || targets.length !== 7 ||
              !Array.isArray(queue) || queue.length !== targets.length) {
            throw new Error(
              "Controlled Candidate Validation output target/evidence queue invalid"
            );
          }

          if (Number(state.validated_target_count) !== 0 ||
              Number(state.executable_target_count) !== 0 ||
              state.external_execution_connected !== false ||
              state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            throw new Error(
              "Candidate Validation state invariants invalid"
            );
          }

          const dataCenters = targets.find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters ||
              Number(dataCenters.merged_from_candidate_count) !== 3) {
            throw new Error(
              "Candidate Validation did not correctly consolidate data centers candidate"
            );
          }

          for (const expectedType of [
            "identify_components",
            "identify_materials",
            "identify_capacity"
          ]) {
            if (!(dataCenters.original_target_types || []).includes(expectedType)) {
              throw new Error(
                `Candidate Validation data centers target lost ${expectedType}`
              );
            }
          }

          for (const target of targets) {
            if (target.epistemic_status !== "provisional_candidate" ||
                target.validation_status !== "not_started" ||
                target.executable !== false ||
                target.knowledge_status !== "not_admitted" ||
                !target.validation_objective ||
                !Array.isArray(target.validation_requirements) ||
                !target.evidence_contract ||
                !Array.isArray(target.post_validation_fanout?.if_supported)) {
              throw new Error(
                `Candidate Validation target invalid: ${target.validation_target_id || "unknown"}`
              );
            }
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.validates_candidates !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.promotes_candidates_to_facts !== false ||
              payload.safeguards?.duplicate_semantic_candidates_consolidated !== true ||
              payload.safeguards?.parent_lineage_preserved !== true ||
              payload.safeguards?.investigation_intents_preserved !== true ||
              payload.safeguards?.evidence_required_before_validation !== true ||
              payload.safeguards?.validation_required_before_execution !== true ||
              payload.safeguards?.unlimited_conceptual_continuation_preserved !== true) {
            throw new Error(
              "Candidate Validation Planner safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Candidate Validation Planner v0.1 output created.");
          console.log(`Status:                ${payload.status}`);
          console.log(`Raw candidates:        ${state.raw_candidate_count}`);
          console.log(`Unique targets:        ${state.unique_validation_target_count}`);
          console.log(`Duplicates collapsed:  ${state.duplicate_candidate_count}`);
          console.log(`Future evidence queue: ${queue.length}`);
          console.log(`Output:                ${file}`);
          NODE

      - name: Commit Real Cosmos Candidate Validation Planner v0.1 Test Output
        if: ${{ inputs.write_cosmos_candidate_validation_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/candidate-validation-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Candidate Validation test changes to commit."
          else
            git commit -m "Add Cosmos Candidate Validation Planner v0.1 controlled test output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Candidate Validation Planner v0.1 output is now committed."
          echo "No OpenAI generation, external search, validation execution, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Evidence Strategy v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_strategy_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Evidence Strategy v0.1 output."
          echo "No OpenAI/API generation, external search, evidence collection, validation, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-evidence-strategy-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_evidence_strategy_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-evidence-strategy-core-input-v0.1.json \
            --out /tmp/core-for-evidence-strategy-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-evidence-strategy-v0.1.json \
            --out /tmp/attention-for-evidence-strategy-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-evidence-strategy-v0.2.json \
            --out /tmp/consequence-for-evidence-strategy-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-evidence-strategy-v0.1.json \
            --out /tmp/discovery-for-evidence-strategy-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-evidence-strategy-v0.1.json \
            --out /tmp/acquisition-for-evidence-strategy-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-evidence-strategy-v0.1.json \
            --out /tmp/applicability-for-evidence-strategy-v0.1.json

          node scripts/cosmos_decomposition.cjs \
            --input /tmp/applicability-for-evidence-strategy-v0.1.json \
            --out /tmp/decomposition-for-evidence-strategy-v0.1.json

          node scripts/cosmos_candidate_validation.cjs \
            --input /tmp/decomposition-for-evidence-strategy-v0.1.json \
            --out /tmp/candidate-validation-for-evidence-strategy-v0.1.json

          node scripts/cosmos_evidence_strategy.cjs \
            --input /tmp/candidate-validation-for-evidence-strategy-v0.1.json \
            --out knowledge/cosmos/evidence-strategy-test-v0.1.json

          test -f knowledge/cosmos/evidence-strategy-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/evidence-strategy-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected Cosmos Evidence Strategy schema_version: ${payload.schema_version}`
            );
          }

          if (payload.status !== "evidence_strategy_resolved") {
            throw new Error(
              `Cosmos Evidence Strategy failed: ${payload.status}`
            );
          }

          if (payload.source_candidate_validation?.schema_version !== "0.1") {
            throw new Error(
              "Cosmos Evidence Strategy did not consume Candidate Validation v0.1"
            );
          }

          const state = payload.evidence_state || {};
          const tasks = payload.evidence_tasks || [];
          const queue = payload.executor_queue || [];

          if (Number(state.validation_target_count) !== 7 ||
              Number(state.evidence_task_count) !== 7 ||
              !Array.isArray(tasks) ||
              tasks.length !== 7 ||
              !Array.isArray(queue) ||
              queue.length !== 7) {
            throw new Error(
              "Controlled Evidence Strategy target/task/queue invariants failed"
            );
          }

          if (Number(state.evidence_collected_count) !== 0 ||
              Number(state.validation_decision_count) !== 0 ||
              Number(state.knowledge_admission_count) !== 0 ||
              state.external_execution_connected !== false ||
              state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            throw new Error(
              "Cosmos Evidence Strategy state invariants invalid"
            );
          }

          for (const task of tasks) {
            if (!task.evidence_task_id ||
                !task.validation_target_id ||
                !Array.isArray(task.evidence_questions) ||
                task.evidence_questions.length < 1 ||
                !Array.isArray(task.query_intents) ||
                task.query_intents.length < 1 ||
                !Array.isArray(task.source_strategy) ||
                task.source_strategy.length < 1 ||
                Number(task.sufficiency_contract?.minimum_total_sources) !== 2 ||
                Number(task.sufficiency_contract?.minimum_independent_sources) !== 2 ||
                task.sufficiency_contract?.contradiction_search_required !== true ||
                task.validation_boundary?.evidence_collected !== false ||
                task.validation_boundary?.validation_decision_made !== false ||
                task.validation_boundary?.candidate_promoted !== false ||
                task.validation_boundary?.executable !== false ||
                task.validation_boundary?.knowledge_admitted !== false ||
                task.execution?.mode !== "planned_only" ||
                task.execution?.external_search_performed !== false ||
                task.execution?.executor_status !== "not_connected" ||
                Number(task.execution?.result_count) !== 0) {
              throw new Error(
                `Cosmos Evidence Strategy task invalid: ${task.evidence_task_id || "unknown"}`
              );
            }
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.collects_evidence !== false ||
              payload.safeguards?.validates_candidates !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.promotes_candidates_to_facts !== false ||
              payload.safeguards?.source_authority_ranked !== true ||
              payload.safeguards?.contradiction_search_required !== true ||
              payload.safeguards?.independent_corroboration_planned !== true ||
              payload.safeguards?.validation_required_before_execution !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.unlimited_conceptual_continuation_preserved !== true) {
            throw new Error(
              "Cosmos Evidence Strategy safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Evidence Strategy v0.1 output created.");
          console.log(`Status:             ${payload.status}`);
          console.log(`Validation targets: ${state.validation_target_count}`);
          console.log(`Evidence tasks:     ${state.evidence_task_count}`);
          console.log(`Executor queue:     ${queue.length}`);
          console.log(`Output:             ${file}`);
          NODE

      - name: Commit Real Cosmos Evidence Strategy v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_strategy_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/evidence-strategy-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Evidence Strategy test changes to commit."
          else
            git commit -m "Add Cosmos Evidence Strategy v0.1 controlled test output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Evidence Strategy v0.1 output is now committed."
          echo "No OpenAI generation, external search, evidence collection, validation execution, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Evidence Executor v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_executor_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Evidence Executor v0.1 output."
          echo "The adapter input is a deterministic fixture. No public web/API search, validation decision, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-evidence-executor-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_evidence_executor_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-evidence-executor-core-input-v0.1.json \
            --out /tmp/core-for-evidence-executor-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-evidence-executor-v0.1.json \
            --out /tmp/attention-for-evidence-executor-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-evidence-executor-v0.2.json \
            --out /tmp/consequence-for-evidence-executor-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-evidence-executor-v0.1.json \
            --out /tmp/discovery-for-evidence-executor-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-evidence-executor-v0.1.json \
            --out /tmp/acquisition-for-evidence-executor-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-evidence-executor-v0.1.json \
            --out /tmp/applicability-for-evidence-executor-v0.1.json

          node scripts/cosmos_decomposition.cjs \
            --input /tmp/applicability-for-evidence-executor-v0.1.json \
            --out /tmp/decomposition-for-evidence-executor-v0.1.json

          node scripts/cosmos_candidate_validation.cjs \
            --input /tmp/decomposition-for-evidence-executor-v0.1.json \
            --out /tmp/candidate-validation-for-evidence-executor-v0.1.json

          node scripts/cosmos_evidence_strategy.cjs \
            --input /tmp/candidate-validation-for-evidence-executor-v0.1.json \
            --out /tmp/evidence-strategy-for-executor-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const strategy = JSON.parse(
            fs.readFileSync(
              "/tmp/evidence-strategy-for-executor-v0.1.json",
              "utf8"
            )
          );

          const dataCenters = (strategy.evidence_tasks || []).find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            throw new Error(
              "Controlled Evidence Executor fixture could not locate the data centers task."
            );
          }

          const now = new Date().toISOString();

          const adapter = {
            schema_version: "0.1-fixture",
            generated_at: now,
            status: "adapter_results_ready",
            adapter_name: "cosmos_preflight_mock_adapter",
            adapter_mode: "deterministic_fixture",
            results: [
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://operator/ai-data-center-1",
                source_type: "official_project_or_operator_document",
                source_title: "Operator AI Infrastructure Requirement",
                source_publisher_or_owner: "Fixture Operator A",
                source_date_or_event_date: "2026-01-15",
                retrieved_at: now,
                extracted_fact: "The operator identifies data-center infrastructure as a direct platform for AI compute workloads.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 100,
                independence_group: "operator-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" official",
                source_rank: 1
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://government/ai-data-center-2",
                source_type: "regulatory_or_government_document",
                source_title: "Government AI Compute Infrastructure Assessment",
                source_publisher_or_owner: "Fixture Government Agency",
                source_date_or_event_date: "2026-02-20",
                retrieved_at: now,
                extracted_fact: "The assessment describes data centers as infrastructure supporting large-scale AI training and inference.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 96,
                independence_group: "government-a",
                geography_scope: "United States",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" infrastructure",
                source_rank: 2
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://research/ai-edge-counterexample",
                source_type: "industry_body_or_research_institution",
                source_title: "Edge AI Deployment Note",
                source_publisher_or_owner: "Fixture Research Institution",
                source_date_or_event_date: "2026-03-10",
                retrieved_at: now,
                extracted_fact: "Some AI inference workloads can execute on distributed edge devices without relying on centralized data centers.",
                supports_or_contradicts: "contradicts",
                directness: "direct",
                authority_score: 82,
                independence_group: "research-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" limitation",
                source_rank: 3
              },
              {
                evidence_task_id: "unknown_fixture_task",
                source_url_or_identifier: "fixture://orphan/result",
                source_title: "Unmapped Adapter Result",
                extracted_fact: "This result intentionally exercises orphan-result preservation.",
                supports_or_contradicts: "context_only",
                directness: "contextual",
                authority_score: 50,
                independence_group: "orphan-fixture"
              }
            ]
          };

          fs.writeFileSync(
            "/tmp/evidence-adapter-results-v0.1.json",
            JSON.stringify(adapter, null, 2)
          );
          NODE

          node scripts/cosmos_evidence_executor.cjs \
            --strategy /tmp/evidence-strategy-for-executor-v0.1.json \
            --adapter-results /tmp/evidence-adapter-results-v0.1.json \
            --out knowledge/cosmos/evidence-executor-test-v0.1.json

          test -f knowledge/cosmos/evidence-executor-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/evidence-executor-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1" ||
              payload.status !== "evidence_execution_normalized") {
            throw new Error(
              `Controlled Evidence Executor output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const state = payload.execution_state || {};
          const records = payload.evidence_records || [];
          const orphans = payload.orphan_results || [];

          if (Number(state.evidence_task_count) !== 7 ||
              Number(state.adapter_result_count) !== 4 ||
              Number(state.normalized_evidence_record_count) !== 3 ||
              Number(state.orphan_result_count) !== 1 ||
              Number(state.validation_decision_count) !== 0 ||
              Number(state.knowledge_admission_count) !== 0 ||
              state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            throw new Error(
              "Controlled Evidence Executor state invariants invalid"
            );
          }

          if (records.filter(
                r => r.supports_or_contradicts === "contradicts"
              ).length !== 1) {
            throw new Error(
              "Controlled Evidence Executor did not preserve contradictory evidence"
            );
          }

          if (!Array.isArray(payload.validation_queue) ||
              payload.validation_queue.length !== 7 ||
              payload.validation_queue.some(
                row =>
                  row.validation_status !== "not_started" ||
                  row.knowledge_admission_status !== "not_started"
              )) {
            throw new Error(
              "Controlled Evidence Executor validation queue invalid"
            );
          }

          if (!Array.isArray(orphans) ||
              orphans.length !== 1 ||
              orphans[0].reason !== "unknown_evidence_task_id") {
            throw new Error(
              "Controlled Evidence Executor orphan handling invalid"
            );
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.adapter_results_are_treated_as_unvalidated_input !== true ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.validates_candidates !== false ||
              payload.safeguards?.promotes_candidates_to_facts !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.preserves_contradictory_evidence !== true ||
              payload.safeguards?.preserves_orphan_adapter_results !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.validation_required_before_knowledge_admission !== true ||
              payload.safeguards?.unlimited_conceptual_continuation_preserved !== true) {
            throw new Error(
              "Controlled Evidence Executor safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Evidence Executor v0.1 output created.");
          console.log(`Status:             ${payload.status}`);
          console.log(`Evidence tasks:     ${state.evidence_task_count}`);
          console.log(`Adapter results:    ${state.adapter_result_count}`);
          console.log(`Normalized records: ${state.normalized_evidence_record_count}`);
          console.log(`Orphan results:     ${state.orphan_result_count}`);
          console.log(`Output:             ${file}`);
          NODE

      - name: Commit Real Cosmos Evidence Executor v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_executor_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/evidence-executor-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Evidence Executor test changes to commit."
          else
            git commit -m "Add Cosmos Evidence Executor v0.1 controlled test output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Evidence Executor v0.1 output is now committed."
          echo "No public web/API search, validation decision, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Evidence Validator v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_validator_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Evidence Validator v0.1 output."
          echo "No public web/API search, permanent knowledge admission, or knowledge-graph write is executed by this step."

          cat > /tmp/cosmos-evidence-validator-core-input-v0.1.json <<'JSON'
          {
            "text": "What could AI data-center growth affect?",
            "language": "en",
            "entity_hints": [
              "Artificial Intelligence",
              "Data Centers"
            ],
            "mode": "open",
            "metadata": {
              "purpose": "controlled_evidence_validator_repository_test"
            }
          }
          JSON

          node scripts/cosmos_core.cjs \
            --input /tmp/cosmos-evidence-validator-core-input-v0.1.json \
            --out /tmp/core-for-evidence-validator-v0.1.json

          node scripts/cosmos_attention.cjs \
            --input /tmp/core-for-evidence-validator-v0.1.json \
            --out /tmp/attention-for-evidence-validator-v0.2.json

          node scripts/cosmos_consequence.cjs \
            --input /tmp/attention-for-evidence-validator-v0.2.json \
            --out /tmp/consequence-for-evidence-validator-v0.1.json

          node scripts/cosmos_discovery.cjs \
            --input /tmp/consequence-for-evidence-validator-v0.1.json \
            --out /tmp/discovery-for-evidence-validator-v0.1.json

          node scripts/cosmos_acquisition.cjs \
            --input /tmp/discovery-for-evidence-validator-v0.1.json \
            --out /tmp/acquisition-for-evidence-validator-v0.1.json

          node scripts/cosmos_acquisition_applicability.cjs \
            --input /tmp/acquisition-for-evidence-validator-v0.1.json \
            --out /tmp/applicability-for-evidence-validator-v0.1.json

          node scripts/cosmos_decomposition.cjs \
            --input /tmp/applicability-for-evidence-validator-v0.1.json \
            --out /tmp/decomposition-for-evidence-validator-v0.1.json

          node scripts/cosmos_candidate_validation.cjs \
            --input /tmp/decomposition-for-evidence-validator-v0.1.json \
            --out /tmp/candidate-validation-for-evidence-validator-v0.1.json

          node scripts/cosmos_evidence_strategy.cjs \
            --input /tmp/candidate-validation-for-evidence-validator-v0.1.json \
            --out /tmp/evidence-strategy-for-validator-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const strategy = JSON.parse(
            fs.readFileSync(
              "/tmp/evidence-strategy-for-validator-v0.1.json",
              "utf8"
            )
          );

          const dataCenters = (strategy.evidence_tasks || []).find(
            x => String(x.candidate_label || "").toLowerCase() === "data centers"
          );

          if (!dataCenters) {
            throw new Error(
              "Controlled Evidence Validator fixture could not locate the data centers task."
            );
          }

          const now = new Date().toISOString();

          const adapter = {
            schema_version: "0.1-fixture",
            generated_at: now,
            status: "adapter_results_ready",
            adapter_name: "cosmos_preflight_mock_adapter",
            adapter_mode: "deterministic_fixture",
            results: [
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://operator/ai-data-center-1",
                source_type: "official_project_or_operator_document",
                source_title: "Operator AI Infrastructure Requirement",
                source_publisher_or_owner: "Fixture Operator A",
                source_date_or_event_date: "2026-01-15",
                retrieved_at: now,
                extracted_fact: "The operator identifies data-center infrastructure as a direct platform for AI compute workloads.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 100,
                independence_group: "operator-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" official",
                source_rank: 1
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://government/ai-data-center-2",
                source_type: "regulatory_or_government_document",
                source_title: "Government AI Compute Infrastructure Assessment",
                source_publisher_or_owner: "Fixture Government Agency",
                source_date_or_event_date: "2026-02-20",
                retrieved_at: now,
                extracted_fact: "The assessment describes data centers as infrastructure supporting large-scale AI training and inference.",
                supports_or_contradicts: "supports",
                directness: "direct",
                authority_score: 96,
                independence_group: "government-a",
                geography_scope: "United States",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" infrastructure",
                source_rank: 2
              },
              {
                evidence_task_id: dataCenters.evidence_task_id,
                source_url_or_identifier: "fixture://research/ai-edge-counterexample",
                source_type: "industry_body_or_research_institution",
                source_title: "Edge AI Deployment Note",
                source_publisher_or_owner: "Fixture Research Institution",
                source_date_or_event_date: "2026-03-10",
                retrieved_at: now,
                extracted_fact: "Some AI inference workloads can execute on distributed edge devices without relying on centralized data centers.",
                supports_or_contradicts: "contradicts",
                directness: "direct",
                authority_score: 82,
                independence_group: "research-a",
                geography_scope: "global",
                temporal_scope: "current",
                entity_ids: [],
                relationship_ids: [],
                query_used: "\"data centers\" \"Artificial Intelligence\" limitation",
                source_rank: 3
              }
            ]
          };

          fs.writeFileSync(
            "/tmp/evidence-adapter-results-for-validator-v0.1.json",
            JSON.stringify(adapter, null, 2)
          );
          NODE

          node scripts/cosmos_evidence_executor.cjs \
            --strategy /tmp/evidence-strategy-for-validator-v0.1.json \
            --adapter-results /tmp/evidence-adapter-results-for-validator-v0.1.json \
            --out /tmp/evidence-executor-for-validator-v0.1.json

          node scripts/cosmos_evidence_validator.cjs \
            --input /tmp/evidence-executor-for-validator-v0.1.json \
            --out knowledge/cosmos/evidence-validator-test-v0.1.json

          test -f knowledge/cosmos/evidence-validator-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");
          const file = "knowledge/cosmos/evidence-validator-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1" ||
              payload.status !== "evidence_validation_resolved") {
            throw new Error(
              `Controlled Evidence Validator output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const state = payload.validation_state || {};
          const validations = payload.validations || [];
          const queue = payload.knowledge_admission_queue || [];

          if (Number(state.validation_target_count) !== 7 ||
              Number(state.completed_validation_count) !== 7 ||
              Number(state.knowledge_admission_count) !== 0 ||
              state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            throw new Error(
              "Controlled Evidence Validator state invariants invalid"
            );
          }

          if (!Array.isArray(validations) || validations.length !== 7) {
            throw new Error(
              "Controlled Evidence Validator validations[] invalid"
            );
          }

          const dataCenters = validations.find(
            row => Number(row.evidence_record_count) === 3
          );

          if (!dataCenters ||
              dataCenters.disposition !== "partially_supported" ||
              dataCenters.validation_status !== "completed" ||
              dataCenters.knowledge_admission_status !== "not_started" ||
              dataCenters.executable_for_knowledge_admission !== true) {
            throw new Error(
              "Controlled Data Centers validation did not resolve as partially_supported and admission-eligible"
            );
          }

          if (validations.filter(
                row => row.disposition === "insufficient_evidence"
              ).length !== 6) {
            throw new Error(
              "Controlled Evidence Validator expected 6 insufficient-evidence validations"
            );
          }

          if (!Array.isArray(queue) || queue.length !== 1 ||
              queue[0].disposition !== "partially_supported" ||
              queue[0].admission_status !== "not_started") {
            throw new Error(
              "Controlled Evidence Validator knowledge_admission_queue invalid"
            );
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.invents_evidence !== false ||
              payload.safeguards?.preserves_contradictory_evidence !== true ||
              payload.safeguards?.validation_uses_authority_directness_independence_and_completeness !== true ||
              payload.safeguards?.validation_does_not_equal_knowledge_admission !== true ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.promotes_candidates_to_facts !== false ||
              payload.safeguards?.unresolved_or_insufficient_targets_not_admission_eligible !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.unlimited_conceptual_continuation_preserved !== true) {
            throw new Error(
              "Controlled Evidence Validator safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Evidence Validator v0.1 output created.");
          console.log(`Status:                ${payload.status}`);
          console.log(`Validations:           ${validations.length}`);
          console.log(`Partially supported:   ${state.by_disposition?.partially_supported || 0}`);
          console.log(`Insufficient evidence: ${state.by_disposition?.insufficient_evidence || 0}`);
          console.log(`Admission queue:       ${queue.length}`);
          console.log(`Output:                ${file}`);
          NODE

      - name: Commit Real Cosmos Evidence Validator v0.1 Test Output
        if: ${{ inputs.write_cosmos_evidence_validator_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/evidence-validator-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Evidence Validator test changes to commit."
          else
            git commit -m "Add Cosmos Evidence Validator v0.1 controlled test output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Evidence Validator v0.1 output is now committed."
          echo "No public web/API search, permanent knowledge admission, or knowledge-graph write was executed."

      - name: Generate Real Cosmos Knowledge Admission v0.1 Test Output
        if: ${{ inputs.write_cosmos_knowledge_admission_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Knowledge Admission v0.1 output."
          echo "No public web/API search and no direct knowledge-graph mutation is executed by this step."

          node <<'NODE'
          const fs = require("fs");

          const partiallySupported = {
            evidence_validation_id: "evidence_validation_5ca25c15",
            evidence_task_id: "evidence_task_115f3b3e",
            validation_target_id: "validation_3642f09d",
            evidence_record_ids: [
              "evidence_d0143201",
              "evidence_6cccfc61",
              "evidence_65ed10af"
            ],
            evidence_record_count: 3,
            disposition: "partially_supported",
            confidence_score: 76,
            confidence_band: "high",
            evidence_balance: {
              weighted_support: 196,
              weighted_contradiction: 82,
              independent_source_groups: 3,
              direct_high_authority_records: 2
            },
            validation_status: "completed",
            knowledge_admission_status: "not_started",
            executable_for_knowledge_admission: true
          };

          const insufficientIds = [
            ["evidence_validation_3bf0d71e", "evidence_task_a6c1f318", "validation_787b6815"],
            ["evidence_validation_64afcb5a", "evidence_task_3e80975d", "validation_83169e60"],
            ["evidence_validation_932cf23f", "evidence_task_64771149", "validation_4600b8de"],
            ["evidence_validation_e319e364", "evidence_task_2f80881f", "validation_224b2a14"],
            ["evidence_validation_eb71dfb6", "evidence_task_e1e94d3d", "validation_e81fcda8"],
            ["evidence_validation_60545a0e", "evidence_task_39c6b18f", "validation_04b40a48"]
          ];

          const insufficient = insufficientIds.map(([validationId, taskId, targetId]) => ({
            evidence_validation_id: validationId,
            evidence_task_id: taskId,
            validation_target_id: targetId,
            evidence_record_ids: [],
            evidence_record_count: 0,
            disposition: "insufficient_evidence",
            confidence_score: 0,
            confidence_band: "very_low",
            evidence_balance: {
              weighted_support: 0,
              weighted_contradiction: 0,
              independent_source_groups: 0,
              direct_high_authority_records: 0
            },
            validation_status: "completed",
            knowledge_admission_status: "not_started",
            executable_for_knowledge_admission: false
          }));

          const fixture = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "evidence_validation_resolved",
            validation_state: {
              validation_target_count: 7,
              completed_validation_count: 7,
              knowledge_admission_count: 0,
              by_disposition: {
                insufficient_evidence: 6,
                partially_supported: 1
              },
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            validations: [
              insufficient[0],
              insufficient[1],
              partiallySupported,
              insufficient[2],
              insufficient[3],
              insufficient[4],
              insufficient[5]
            ],
            knowledge_admission_queue: [
              {
                admission_rank: 1,
                evidence_validation_id: partiallySupported.evidence_validation_id,
                evidence_task_id: partiallySupported.evidence_task_id,
                validation_target_id: partiallySupported.validation_target_id,
                disposition: "partially_supported",
                confidence_score: 76,
                confidence_band: "high",
                evidence_record_ids: partiallySupported.evidence_record_ids,
                admission_status: "not_started"
              }
            ]
          };

          fs.writeFileSync(
            "/tmp/evidence-validator-for-knowledge-admission-v0.1.json",
            JSON.stringify(fixture, null, 2)
          );
          NODE

          node scripts/cosmos_knowledge_admission.cjs \
            --input /tmp/evidence-validator-for-knowledge-admission-v0.1.json \
            --out knowledge/cosmos/knowledge-admission-test-v0.1.json

          test -f knowledge/cosmos/knowledge-admission-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file = "knowledge/cosmos/knowledge-admission-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1" ||
              payload.status !== "knowledge_admission_resolved") {
            throw new Error(
              `Controlled Knowledge Admission output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const state = payload.admission_state || {};
          const admissions = payload.admissions || [];
          const queue = payload.graph_write_queue || [];

          if (Number(state.validation_count) !== 7 ||
              Number(state.reviewed_count) !== 7 ||
              Number(state.validator_queue_count) !== 1 ||
              Number(state.graph_write_eligible_count) !== 1 ||
              Number(state.graph_write_count) !== 0 ||
              state.conceptual_distance_limit !== null ||
              state.continuation_possible !== true) {
            throw new Error(
              "Controlled Knowledge Admission state invariants invalid"
            );
          }

          const qualified = admissions.filter(
            row => row.decision === "admit_with_qualification"
          );

          const rejected = admissions.filter(
            row => row.decision === "reject_for_now"
          );

          if (qualified.length !== 1 ||
              rejected.length !== 6 ||
              qualified[0].validation_disposition !== "partially_supported" ||
              Number(qualified[0].confidence_score) !== 76 ||
              qualified[0].qualification?.must_preserve_contradiction !== true ||
              qualified[0].graph_write_eligibility !== true) {
            throw new Error(
              "Controlled Knowledge Admission decisions invalid"
            );
          }

          if (!Array.isArray(queue) ||
              queue.length !== 1 ||
              queue[0].admitted_claim_class !== "validated_qualified_relationship" ||
              queue[0].graph_write_status !== "not_started") {
            throw new Error(
              "Controlled Knowledge Admission graph_write_queue invalid"
            );
          }

          if (payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.admission_is_not_graph_mutation !== true ||
              payload.safeguards?.insufficient_or_unresolved_not_admitted !== true ||
              payload.safeguards?.partially_supported_requires_qualification !== true ||
              payload.safeguards?.contradictory_evidence_preserved !== true ||
              payload.safeguards?.confidence_preserved !== true ||
              payload.safeguards?.source_lineage_preserved !== true ||
              payload.safeguards?.graph_writes_are_reversible !== true ||
              payload.safeguards?.separate_graph_writer_required !== true) {
            throw new Error(
              "Controlled Knowledge Admission safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Knowledge Admission v0.1 output created.");
          console.log(`Status:                   ${payload.status}`);
          console.log(`Validations reviewed:     ${state.reviewed_count}`);
          console.log(`Admit with qualification: ${state.by_decision?.admit_with_qualification || 0}`);
          console.log(`Reject for now:           ${state.by_decision?.reject_for_now || 0}`);
          console.log(`Graph-write eligible:     ${state.graph_write_eligible_count}`);
          console.log(`Graph writes performed:   ${state.graph_write_count}`);
          console.log(`Output:                   ${file}`);
          NODE

      - name: Commit Real Cosmos Knowledge Admission v0.1 Test Output
        if: ${{ inputs.write_cosmos_knowledge_admission_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/knowledge-admission-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Knowledge Admission test changes to commit."
          else
            git commit -m "Add Cosmos Knowledge Admission v0.1 controlled test output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Knowledge Admission v0.1 output is now committed."
          echo "No public web/API search or direct knowledge-graph mutation was executed."

      - name: Generate Real Cosmos Graph Writer v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_writer_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Graph Writer v0.1 snapshot."
          echo "This does NOT mutate the production Cosmos graph in place."

          node <<'NODE'
          const fs = require("fs");

          const qualified = {
            knowledge_admission_id: "knowledge_admission_graph_writer_fixture_qualified",
            evidence_validation_id: "evidence_validation_5ca25c15",
            evidence_task_id: "evidence_task_115f3b3e",
            validation_target_id: "validation_3642f09d",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_d0143201",
              "evidence_6cccfc61",
              "evidence_65ed10af"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const rejected = {
            knowledge_admission_id: "knowledge_admission_graph_writer_fixture_rejected",
            evidence_validation_id: "evidence_validation_rejected_fixture",
            evidence_task_id: "evidence_task_rejected_fixture",
            validation_target_id: "validation_rejected_fixture",
            admitted_claim_class: null,
            validation_disposition: "insufficient_evidence",
            decision: "reject_for_now",
            confidence_score: 0,
            confidence_band: "very_low",
            persistence_level: "none",
            evidence_record_ids: [],
            qualification: {
              contradiction_present: false,
              must_preserve_contradiction: false
            },
            graph_write_eligibility: false,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              validation_count: 2,
              reviewed_count: 2,
              graph_write_eligible_count: 1,
              graph_write_count: 0,
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified, rejected],
            graph_write_queue: [
              { ...qualified, graph_write_rank: 1 },
              { ...rejected, graph_write_rank: 2 }
            ]
          };

          const graph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          fs.writeFileSync(
            "/tmp/cosmos-graph-writer-admission-v0.1.json",
            JSON.stringify(admission, null, 2)
          );
          fs.writeFileSync(
            "/tmp/cosmos-graph-writer-before-v0.1.json",
            JSON.stringify(graph, null, 2)
          );
          NODE

          BEFORE_HASH="$(sha256sum /tmp/cosmos-graph-writer-before-v0.1.json | awk '{print $1}')"

          node scripts/cosmos_graph_writer.cjs \
            --admission /tmp/cosmos-graph-writer-admission-v0.1.json \
            --graph /tmp/cosmos-graph-writer-before-v0.1.json \
            --out knowledge/cosmos/graph-writer-test-v0.1.json \
            --report knowledge/cosmos/graph-writer-report-v0.1.json

          AFTER_HASH="$(sha256sum /tmp/cosmos-graph-writer-before-v0.1.json | awk '{print $1}')"

          if [ "$BEFORE_HASH" != "$AFTER_HASH" ]; then
            echo "ERROR: Graph Writer mutated its input graph."
            exit 1
          fi

          node scripts/cosmos_graph_writer.cjs \
            --admission /tmp/cosmos-graph-writer-admission-v0.1.json \
            --graph knowledge/cosmos/graph-writer-test-v0.1.json \
            --out /tmp/cosmos-graph-writer-second-v0.1.json \
            --report /tmp/cosmos-graph-writer-second-report-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const graph = JSON.parse(
            fs.readFileSync(
              "knowledge/cosmos/graph-writer-test-v0.1.json",
              "utf8"
            )
          );
          const report = JSON.parse(
            fs.readFileSync(
              "knowledge/cosmos/graph-writer-report-v0.1.json",
              "utf8"
            )
          );
          const graph2 = JSON.parse(
            fs.readFileSync(
              "/tmp/cosmos-graph-writer-second-v0.1.json",
              "utf8"
            )
          );
          const report2 = JSON.parse(
            fs.readFileSync(
              "/tmp/cosmos-graph-writer-second-report-v0.1.json",
              "utf8"
            )
          );

          if (report.status !== "graph_write_resolved") {
            throw new Error(`Graph Writer report status invalid: ${report.status}`);
          }

          if (report.graph_write_state?.written_count !== 1 ||
              report.graph_write_state?.error_count !== 1) {
            throw new Error(
              "First Graph Writer run must write exactly 1 eligible record and refuse exactly 1 non-eligible record."
            );
          }

          if (!Array.isArray(graph.knowledge_records) ||
              graph.knowledge_records.length !== 1 ||
              !Array.isArray(graph.audit_log) ||
              graph.audit_log.length !== 1) {
            throw new Error(
              "Controlled graph snapshot must contain exactly 1 knowledge record and 1 audit record."
            );
          }

          const record = graph.knowledge_records[0];

          if (record.validation_disposition !== "partially_supported" ||
              Number(record.confidence_score) !== 76 ||
              record.qualification?.contradiction_present !== true ||
              record.qualification?.must_preserve_contradiction !== true ||
              !Array.isArray(record.evidence_record_ids) ||
              record.evidence_record_ids.length !== 3 ||
              record.reversibility?.reversible !== true) {
            throw new Error(
              "Graph Writer failed to preserve qualification, contradiction, confidence, evidence lineage or reversibility."
            );
          }

          if (report2.graph_write_state?.written_count !== 0 ||
              report2.graph_write_state?.idempotent_skip_count !== 1 ||
              graph2.knowledge_records?.length !== 1 ||
              graph2.audit_log?.length !== 1) {
            throw new Error(
              "Graph Writer idempotency test failed on second execution."
            );
          }

          if (report.safeguards?.mutates_input_graph_in_place !== false ||
              report.safeguards?.writes_only_explicit_output_snapshot !== true ||
              report.safeguards?.requires_graph_write_eligibility !== true ||
              report.safeguards?.preserves_qualification !== true ||
              report.safeguards?.preserves_contradiction !== true ||
              report.safeguards?.preserves_confidence !== true ||
              report.safeguards?.preserves_evidence_lineage !== true ||
              report.safeguards?.idempotent_by_knowledge_admission_id !== true ||
              report.safeguards?.audit_log_required !== true ||
              report.safeguards?.graph_records_reversible !== true ||
              report.safeguards?.rejected_admissions_not_written !== true) {
            throw new Error("Graph Writer safeguards invalid.");
          }

          console.log("");
          console.log("Controlled Cosmos Graph Writer v0.1 output created.");
          console.log(`Graph records:       ${graph.knowledge_records.length}`);
          console.log(`Audit records:       ${graph.audit_log.length}`);
          console.log(`First-run writes:    ${report.graph_write_state.written_count}`);
          console.log(`Second-run writes:   ${report2.graph_write_state.written_count}`);
          console.log(`Idempotent skips:    ${report2.graph_write_state.idempotent_skip_count}`);
          console.log("Production graph mutation: NONE");
          NODE

      - name: Commit Real Cosmos Graph Writer v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_writer_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            knowledge/cosmos/graph-writer-test-v0.1.json \
            knowledge/cosmos/graph-writer-report-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Graph Writer controlled-output changes to commit."
          else
            git commit -m "Add Cosmos Graph Writer v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Graph Writer v0.1 snapshot/report committed."
          echo "Production Cosmos graph was not mutated."

      - name: Generate Real Cosmos Graph Integrity v0.1 Controlled Report
        if: ${{ inputs.write_cosmos_graph_integrity_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Graph Integrity v0.1 report."
          echo "This validates an isolated snapshot and does not mutate the production graph."

          node <<'NODE'
          const fs = require("fs");

          const qualified = {
            knowledge_admission_id: "knowledge_admission_integrity_repository_fixture",
            evidence_validation_id: "evidence_validation_integrity_repository_fixture",
            evidence_task_id: "evidence_task_integrity_repository_fixture",
            validation_target_id: "validation_integrity_repository_fixture",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_integrity_repository_1",
              "evidence_integrity_repository_2",
              "evidence_integrity_repository_3"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified],
            graph_write_queue: [
              { ...qualified, graph_write_rank: 1 }
            ]
          };

          const graph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          fs.writeFileSync(
            "/tmp/cosmos-integrity-admission-v0.1.json",
            JSON.stringify(admission, null, 2)
          );

          fs.writeFileSync(
            "/tmp/cosmos-integrity-empty-graph-v0.1.json",
            JSON.stringify(graph, null, 2)
          );
          NODE

          node scripts/cosmos_graph_writer.cjs \
            --admission /tmp/cosmos-integrity-admission-v0.1.json \
            --graph /tmp/cosmos-integrity-empty-graph-v0.1.json \
            --out /tmp/cosmos-integrity-valid-graph-v0.1.json \
            --report /tmp/cosmos-integrity-writer-report-v0.1.json

          node scripts/cosmos_graph_integrity.cjs \
            --graph /tmp/cosmos-integrity-valid-graph-v0.1.json \
            --out knowledge/cosmos/graph-integrity-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file = "knowledge/cosmos/graph-integrity-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1" ||
              payload.status !== "graph_integrity_passed") {
            throw new Error(
              `Controlled Graph Integrity output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          if (Number(payload.integrity_state?.error_count) !== 0 ||
              payload.integrity_state?.production_write_safe !== true) {
            throw new Error(
              "Controlled Graph Integrity report must have zero errors and production_write_safe=true"
            );
          }

          if (Number(payload.graph_state?.knowledge_record_count) !== 1 ||
              Number(payload.graph_state?.active_record_count) !== 1 ||
              Number(payload.graph_state?.audit_record_count) !== 1) {
            throw new Error(
              "Controlled Graph Integrity graph-state counts invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Graph Integrity v0.1 report created.");
          console.log(`Status:                ${payload.status}`);
          console.log(`Errors:                ${payload.integrity_state.error_count}`);
          console.log(`Warnings:              ${payload.integrity_state.warning_count}`);
          console.log(`Production write safe: ${payload.integrity_state.production_write_safe}`);
          console.log(`Output:                ${file}`);
          NODE

      - name: Commit Real Cosmos Graph Integrity v0.1 Controlled Report
        if: ${{ inputs.write_cosmos_graph_integrity_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/graph-integrity-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Graph Integrity controlled-report changes to commit."
          else
            git commit -m "Add Cosmos Graph Integrity v0.1 controlled report"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Graph Integrity v0.1 report committed."
          echo "Production Cosmos graph was not mutated."

      - name: Generate Real Cosmos Graph Reversal v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_reversal_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Graph Reversal v0.1 output."
          echo "This operates on an isolated graph snapshot, not the production Cosmos graph."

          node <<'NODE'
          const fs = require("fs");

          const qualified = {
            knowledge_admission_id: "knowledge_admission_reversal_repository_fixture",
            evidence_validation_id: "evidence_validation_reversal_repository_fixture",
            evidence_task_id: "evidence_task_reversal_repository_fixture",
            validation_target_id: "validation_reversal_repository_fixture",
            admitted_claim_class: "validated_qualified_relationship",
            validation_disposition: "partially_supported",
            decision: "admit_with_qualification",
            confidence_score: 76,
            confidence_band: "high",
            persistence_level: "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_reversal_repository_1",
              "evidence_reversal_repository_2",
              "evidence_reversal_repository_3"
            ],
            qualification: {
              contradiction_present: true,
              must_preserve_contradiction: true,
              confidence_must_be_preserved: true,
              source_lineage_must_be_preserved: true,
              temporal_scope_must_be_preserved: true,
              geography_scope_must_be_preserved: true
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: new Date().toISOString(),
            status: "knowledge_admission_resolved",
            admission_state: {
              conceptual_distance_limit: null,
              continuation_possible: true
            },
            admissions: [qualified],
            graph_write_queue: [
              { ...qualified, graph_write_rank: 1 }
            ]
          };

          const graph = {
            schema_version: "0.1",
            generated_at: null,
            status: "cosmos_graph_snapshot",
            knowledge_records: [],
            audit_log: []
          };

          const request = {
            schema_version: "0.1",
            knowledge_admission_id:
              "knowledge_admission_reversal_repository_fixture",
            reason:
              "controlled_repository_reversal_test",
            requested_by:
              "cosmos_preflight"
          };

          fs.writeFileSync(
            "/tmp/cosmos-reversal-admission-v0.1.json",
            JSON.stringify(admission, null, 2)
          );
          fs.writeFileSync(
            "/tmp/cosmos-reversal-empty-graph-v0.1.json",
            JSON.stringify(graph, null, 2)
          );
          fs.writeFileSync(
            "/tmp/cosmos-reversal-request-v0.1.json",
            JSON.stringify(request, null, 2)
          );
          NODE

          node scripts/cosmos_graph_writer.cjs \
            --admission /tmp/cosmos-reversal-admission-v0.1.json \
            --graph /tmp/cosmos-reversal-empty-graph-v0.1.json \
            --out /tmp/cosmos-pre-reversal-graph-v0.1.json \
            --report /tmp/cosmos-pre-reversal-writer-report-v0.1.json

          node scripts/cosmos_graph_reversal.cjs \
            --graph /tmp/cosmos-pre-reversal-graph-v0.1.json \
            --request /tmp/cosmos-reversal-request-v0.1.json \
            --out knowledge/cosmos/graph-reversal-test-v0.1.json \
            --report knowledge/cosmos/graph-reversal-report-v0.1.json

          node scripts/cosmos_graph_integrity.cjs \
            --graph knowledge/cosmos/graph-reversal-test-v0.1.json \
            --out knowledge/cosmos/graph-reversal-integrity-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const graphFile =
            "knowledge/cosmos/graph-reversal-test-v0.1.json";
          const reportFile =
            "knowledge/cosmos/graph-reversal-report-v0.1.json";
          const integrityFile =
            "knowledge/cosmos/graph-reversal-integrity-test-v0.1.json";

          const graph = JSON.parse(fs.readFileSync(graphFile, "utf8"));
          const report = JSON.parse(fs.readFileSync(reportFile, "utf8"));
          const integrity = JSON.parse(
            fs.readFileSync(integrityFile, "utf8")
          );

          if (report.status !== "graph_reversal_resolved" ||
              Number(report.reversal_state?.reversed_count) !== 1 ||
              Number(report.reversal_state?.error_count) !== 0) {
            throw new Error(
              "Controlled Graph Reversal output invalid"
            );
          }

          if (graph.knowledge_records?.length !== 1 ||
              graph.knowledge_records?.[0]?.active !== false ||
              graph.audit_log?.length !== 2) {
            throw new Error(
              "Controlled reversed graph must contain 1 inactive historical record and 2 audits"
            );
          }

          if (integrity.status !== "graph_integrity_passed" ||
              Number(integrity.integrity_state?.error_count) !== 0 ||
              integrity.integrity_state?.production_write_safe !== true) {
            throw new Error(
              "Controlled reversed graph failed post-reversal Graph Integrity"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Graph Reversal v0.1 output created.");
          console.log(`Reversed records: ${report.reversal_state.reversed_count}`);
          console.log(`Active records:   ${integrity.graph_state.active_record_count}`);
          console.log(`Inactive records: ${integrity.graph_state.inactive_record_count}`);
          console.log(`Audit records:    ${integrity.graph_state.audit_record_count}`);
          console.log(`Integrity:        ${integrity.status}`);
          console.log(`Graph:            ${graphFile}`);
          console.log(`Report:           ${reportFile}`);
          console.log(`Integrity report: ${integrityFile}`);
          NODE

      - name: Commit Real Cosmos Graph Reversal v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_reversal_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            knowledge/cosmos/graph-reversal-test-v0.1.json \
            knowledge/cosmos/graph-reversal-report-v0.1.json \
            knowledge/cosmos/graph-reversal-integrity-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Graph Reversal controlled-output changes to commit."
          else
            git commit -m "Add Cosmos Graph Reversal v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Graph Reversal v0.1 output committed."
          echo "Production Cosmos graph was not mutated."

      - name: Generate Real Cosmos Causal Narrative v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_causal_narrative_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Causal Narrative v0.1 output."
          echo "No external search, OpenAI/API generation, or graph mutation is executed by this step."

          cat > /tmp/cosmos-causal-narrative-input-v0.1.json <<'JSON'
          {
            "focal_signal": {
              "node_id": "substation_pressure",
              "label": "Substation capacity pressure",
              "type": "signal",
              "role": "focal_signal"
            },
            "nodes": [
              {
                "node_id": "ai_compute",
                "label": "AI compute demand growth",
                "type": "driver",
                "role": "upstream_driver"
              },
              {
                "node_id": "data_center_expansion",
                "label": "Data-center capacity expansion",
                "type": "driver",
                "role": "upstream_driver"
              },
              {
                "node_id": "substation_pressure",
                "label": "Substation capacity pressure",
                "type": "signal",
                "role": "focal_signal"
              },
              {
                "node_id": "transformer_demand",
                "label": "Transformer capacity demand",
                "type": "consequence"
              },
              {
                "node_id": "oem_capacity_pressure",
                "label": "OEM manufacturing capacity pressure",
                "type": "consequence"
              },
              {
                "node_id": "lead_time_pressure",
                "label": "Transformer lead-time pressure",
                "type": "consequence"
              },
              {
                "node_id": "project_schedule_pressure",
                "label": "Project schedule pressure",
                "type": "consequence"
              },
              {
                "node_id": "investment_relocation",
                "label": "Potential geographic investment relocation",
                "type": "scenario"
              }
            ],
            "edges": [
              {
                "edge_id": "edge_ai_to_dc",
                "from_node_id": "ai_compute",
                "to_node_id": "data_center_expansion",
                "relationship": "drives",
                "evidence_status": "supported",
                "confidence_score": 92,
                "source_record_ids": [
                  "evidence_ai_dc_1",
                  "evidence_ai_dc_2"
                ],
                "time_horizon": "current_to_short",
                "geography_scope": "global",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_dc_to_substation",
                "from_node_id": "data_center_expansion",
                "to_node_id": "substation_pressure",
                "relationship": "increases",
                "evidence_status": "supported",
                "confidence_score": 88,
                "source_record_ids": [
                  "evidence_dc_grid_1",
                  "evidence_dc_grid_2"
                ],
                "time_horizon": "short",
                "geography_scope": "United States",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_substation_to_transformer",
                "from_node_id": "substation_pressure",
                "to_node_id": "transformer_demand",
                "relationship": "increases",
                "evidence_status": "supported",
                "confidence_score": 84,
                "source_record_ids": [
                  "evidence_substation_transformer_1"
                ],
                "time_horizon": "short",
                "geography_scope": "United States",
                "temporal_scope": "current"
              },
              {
                "edge_id": "edge_transformer_to_oem",
                "from_node_id": "transformer_demand",
                "to_node_id": "oem_capacity_pressure",
                "relationship": "constrains",
                "evidence_status": "inferred",
                "confidence_score": 78,
                "source_record_ids": [
                  "evidence_oem_capacity_1"
                ],
                "time_horizon": "short_to_mid",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_oem_to_lead",
                "from_node_id": "oem_capacity_pressure",
                "to_node_id": "lead_time_pressure",
                "relationship": "increases",
                "evidence_status": "scenario",
                "confidence_score": 74,
                "source_record_ids": [
                  "evidence_lead_time_1"
                ],
                "time_horizon": "mid",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_lead_to_schedule",
                "from_node_id": "lead_time_pressure",
                "to_node_id": "project_schedule_pressure",
                "relationship": "increases",
                "evidence_status": "scenario",
                "confidence_score": 70,
                "source_record_ids": [
                  "evidence_schedule_1"
                ],
                "time_horizon": "mid_to_long",
                "geography_scope": "global",
                "temporal_scope": "forward"
              },
              {
                "edge_id": "edge_schedule_to_relocation",
                "from_node_id": "project_schedule_pressure",
                "to_node_id": "investment_relocation",
                "relationship": "may_influence",
                "evidence_status": "speculative",
                "confidence_score": 62,
                "source_record_ids": [
                  "evidence_location_scenario_1"
                ],
                "time_horizon": "long",
                "geography_scope": "multi-region",
                "temporal_scope": "forward"
              }
            ],
            "activation_events": [
              {
                "activation_id": "activation_1",
                "label": "Recent data-center project announcements increased",
                "kind": "new_evidence",
                "evidence_status": "supported",
                "confidence_score": 88,
                "source_record_ids": [
                  "activation_evidence_1"
                ],
                "occurred_at": "2026-08-27T12:00:00Z",
                "relevance_score": 95
              },
              {
                "activation_id": "activation_2",
                "label": "Utility load forecasts strengthened",
                "kind": "strengthened_evidence",
                "evidence_status": "supported",
                "confidence_score": 84,
                "source_record_ids": [
                  "activation_evidence_2"
                ],
                "occurred_at": "2026-08-27T10:00:00Z",
                "relevance_score": 90
              },
              {
                "activation_id": "activation_3",
                "label": "Transformer lead-time pressure remained unresolved",
                "kind": "related_signal",
                "evidence_status": "supported",
                "confidence_score": 80,
                "source_record_ids": [
                  "activation_evidence_3"
                ],
                "occurred_at": "2026-08-26T18:00:00Z",
                "relevance_score": 82
              }
            ],
            "max_causal_depth": 5
          }
          JSON

          node scripts/cosmos_causal_narrative.cjs \
            --input /tmp/cosmos-causal-narrative-input-v0.1.json \
            --out knowledge/cosmos/causal-narrative-test-v0.1.json

          test -f knowledge/cosmos/causal-narrative-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file =
            "knowledge/cosmos/causal-narrative-test-v0.1.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1" ||
              payload.status !== "causal_narrative_resolved") {
            throw new Error(
              `Controlled Causal Narrative output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const story = payload.downstream_story || {};
          const downstream =
            payload.butterfly_effect?.all_downstream_edges || [];
          const candidates =
            payload.butterfly_effect?.potential_new_signals || [];

          if (payload.why_now?.explanation_status !==
                "activation_evidence_available" ||
              Number(payload.why_now?.activation_event_count) !== 3) {
            throw new Error(
              "Controlled Causal Narrative why_now block invalid"
            );
          }

          if ((payload.upstream_drivers || []).length !== 2 ||
              (story.direct_effects || []).length !== 1 ||
              (story.second_order_effects || []).length !== 1 ||
              (story.third_order_effects || []).length !== 1 ||
              (story.emerging_effects || []).length !== 2 ||
              downstream.length !== 5) {
            throw new Error(
              "Controlled Causal Narrative causal grouping/counts invalid"
            );
          }

          let prior = Infinity;

          for (const row of [...downstream].sort(
            (a,b) => Number(a.causal_depth)-Number(b.causal_depth)
          )) {
            const effective =
              Number(row.effective_confidence_score);
            const original =
              Number(row.original_confidence_score);

            if (effective > original + 1e-9 ||
                effective > prior + 1e-9) {
              throw new Error(
                `Controlled Causal Narrative confidence inflation at depth ${row.causal_depth}`
              );
            }

            prior = effective;
          }

          if (!candidates.length ||
              candidates.some(
                row =>
                  row.status !== "candidate_only" ||
                  row.promotion_to_signal !== false
              )) {
            throw new Error(
              "Controlled Causal Narrative candidate-signal boundary invalid"
            );
          }

          if (payload.safeguards?.performs_external_search !== false ||
              payload.safeguards?.calls_openai_or_external_api !== false ||
              payload.safeguards?.writes_to_knowledge_graph !== false ||
              payload.safeguards?.mutates_input_graph !== false ||
              payload.safeguards?.creates_unverified_facts !== false ||
              payload.safeguards?.causal_distance_penalizes_confidence !== true ||
              payload.safeguards?.activation_logic_separate_from_causality !== true ||
              payload.safeguards?.candidate_signals_not_auto_promoted !== true) {
            throw new Error(
              "Controlled Causal Narrative safeguards invalid"
            );
          }

          console.log("");
          console.log("Real controlled Cosmos Causal Narrative v0.1 output created.");
          console.log(`Status:            ${payload.status}`);
          console.log(`Why-now events:    ${payload.why_now.activation_event_count}`);
          console.log(`Upstream drivers:  ${payload.upstream_drivers.length}`);
          console.log(`Butterfly edges:   ${downstream.length}`);
          console.log(`Candidate signals: ${candidates.length}`);
          console.log(`Output:            ${file}`);
          NODE

      - name: Commit Real Cosmos Causal Narrative v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_causal_narrative_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/causal-narrative-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Causal Narrative controlled-output changes to commit."
          else
            git commit -m "Add Cosmos Causal Narrative v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Causal Narrative v0.1 output committed."
          echo "No external search, OpenAI/API generation, or production graph mutation was executed."

      - name: Generate Real Cosmos Graph Evolution v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_evolution_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Graph Evolution v0.1 planning output."
          echo "No external search, OpenAI/API generation, graph mutation or historical deletion is executed."

          node <<'NODE'
          const fs = require("fs");

          const graph = {
            schema_version: "0.1",
            generated_at: "2026-08-26T12:00:00Z",
            status: "cosmos_graph_snapshot",
            knowledge_records: [
              {
                graph_record_id: "graph_existing_repository_fixture",
                knowledge_admission_id:
                  "knowledge_admission_existing_repository_fixture",
                validation_target_id:
                  "validation_repository_fixture",
                claim_class:
                  "validated_qualified_relationship",
                validation_disposition:
                  "partially_supported",
                confidence_score: 70,
                confidence_band: "high",
                epistemic_status:
                  "persistent_qualified_knowledge",
                evidence_record_ids: [
                  "evidence_repository_old_1"
                ],
                qualification: {
                  contradiction_present: false
                },
                admitted_at: "2026-08-20T12:00:00Z",
                active: true
              }
            ],
            audit_log: [
              {
                audit_id: "audit_repository_existing",
                operation: "create_graph_record",
                graph_record_id:
                  "graph_existing_repository_fixture",
                knowledge_admission_id:
                  "knowledge_admission_existing_repository_fixture"
              }
            ]
          };

          const incoming = {
            knowledge_admission_id:
              "knowledge_admission_repository_reinforce",
            evidence_validation_id:
              "evidence_validation_repository_reinforce",
            validation_target_id:
              "validation_repository_fixture",
            admitted_claim_class:
              "validated_qualified_relationship",
            validation_disposition:
              "partially_supported",
            decision:
              "admit_with_qualification",
            confidence_score: 84,
            confidence_band: "high",
            persistence_level:
              "persistent_qualified_knowledge",
            evidence_record_ids: [
              "evidence_repository_old_1",
              "evidence_repository_new_2"
            ],
            qualification: {
              contradiction_present: false
            },
            graph_write_eligibility: true,
            graph_write_status: "not_started"
          };

          const admission = {
            schema_version: "0.1",
            generated_at: "2026-08-27T12:00:00Z",
            status: "knowledge_admission_resolved",
            admissions: [incoming],
            graph_write_queue: [
              {
                ...incoming,
                graph_write_rank: 1
              }
            ]
          };

          fs.writeFileSync(
            "/tmp/cosmos-graph-evolution-graph-v0.1.json",
            JSON.stringify(graph, null, 2)
          );

          fs.writeFileSync(
            "/tmp/cosmos-graph-evolution-admission-v0.1.json",
            JSON.stringify(admission, null, 2)
          );
          NODE

          node scripts/cosmos_graph_evolution.cjs \
            --graph /tmp/cosmos-graph-evolution-graph-v0.1.json \
            --admission /tmp/cosmos-graph-evolution-admission-v0.1.json \
            --out knowledge/cosmos/graph-evolution-test-v0.1.json

          test -f knowledge/cosmos/graph-evolution-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file =
            "knowledge/cosmos/graph-evolution-test-v0.1.json";
          const payload = JSON.parse(
            fs.readFileSync(file, "utf8")
          );

          if (payload.schema_version !== "0.1" ||
              payload.status !== "graph_evolution_planned") {
            throw new Error(
              `Controlled Graph Evolution output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const plans = payload.evolution_plans || [];

          if (plans.length !== 1 ||
              plans[0]?.evolution_action !==
                "reinforce_existing") {
            throw new Error(
              "Controlled Graph Evolution expected one reinforce_existing plan"
            );
          }

          if (Number(payload.evolution_state?.graph_mutation_count) !== 0 ||
              payload.safeguards?.mutates_graph !== false ||
              payload.safeguards?.deletes_historical_knowledge !== false ||
              payload.safeguards?.preserves_existing_records !== true ||
              payload.safeguards?.preserves_audit_history !== true ||
              payload.safeguards?.conflicting_polarity_requires_review !== true ||
              payload.safeguards?.supersession_requires_explicit_execution !== true ||
              payload.safeguards?.reversibility_required !== true) {
            throw new Error(
              "Controlled Graph Evolution safeguards invalid"
            );
          }

          console.log("");
          console.log(
            "Real controlled Cosmos Graph Evolution v0.1 planning output created."
          );
          console.log(`Status: ${payload.status}`);
          console.log(
            `Action: ${plans[0].evolution_action}`
          );
          console.log(
            `Graph mutations: ${payload.evolution_state.graph_mutation_count}`
          );
          console.log(`Output: ${file}`);
          NODE

      - name: Commit Real Cosmos Graph Evolution v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_graph_evolution_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/graph-evolution-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Graph Evolution controlled-output changes to commit."
          else
            git commit -m "Add Cosmos Graph Evolution v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Graph Evolution v0.1 output committed."
          echo "Production Cosmos graph was not mutated."

      - name: Generate Real Cosmos Temporal Knowledge v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_temporal_knowledge_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos Temporal Knowledge v0.1 output."
          echo "No external search, OpenAI/API generation, graph mutation or historical deletion is executed."

          cat > /tmp/cosmos-temporal-knowledge-graph-v0.1.json <<'JSON'
          {
            "schema_version": "0.1",
            "generated_at": "2026-08-27T12:00:00Z",
            "status": "cosmos_graph_snapshot",
            "knowledge_records": [
              {
                "graph_record_id": "graph_temporal_repository_past",
                "knowledge_admission_id": "knowledge_temporal_repository_past",
                "validation_target_id": "validation_temporal_repository",
                "claim_class": "validated_relationship",
                "validation_disposition": "supported",
                "confidence_score": 66,
                "confidence_band": "moderate",
                "epistemic_status": "persistent_knowledge",
                "evidence_record_ids": ["evidence_temporal_repository_past"],
                "temporal_scope": "current",
                "geography_scope": "United States",
                "admitted_at": "2026-07-15T00:00:00Z",
                "deactivated_at": "2026-08-15T00:00:00Z",
                "active": false
              },
              {
                "graph_record_id": "graph_temporal_repository_current",
                "knowledge_admission_id": "knowledge_temporal_repository_current",
                "validation_target_id": "validation_temporal_repository",
                "claim_class": "validated_relationship",
                "validation_disposition": "supported",
                "confidence_score": 81,
                "confidence_band": "high",
                "epistemic_status": "persistent_knowledge",
                "evidence_record_ids": [
                  "evidence_temporal_repository_current_1",
                  "evidence_temporal_repository_current_2"
                ],
                "temporal_scope": "current",
                "geography_scope": "United States",
                "admitted_at": "2026-08-15T00:00:01Z",
                "active": true
              },
              {
                "graph_record_id": "graph_temporal_repository_future",
                "knowledge_admission_id": "knowledge_temporal_repository_future",
                "validation_target_id": "validation_temporal_repository_future",
                "claim_class": "scenario_relationship",
                "validation_disposition": "partially_supported",
                "confidence_score": 55,
                "confidence_band": "moderate",
                "epistemic_status": "scenario_knowledge",
                "evidence_record_ids": ["evidence_temporal_repository_future"],
                "temporal_scope": "future",
                "geography_scope": "Global",
                "admitted_at": "2026-08-26T00:00:00Z",
                "active": true
              }
            ],
            "audit_log": []
          }
          JSON

          node scripts/cosmos_temporal_knowledge.cjs \
            --graph /tmp/cosmos-temporal-knowledge-graph-v0.1.json \
            --reference-time 2026-08-27T12:00:00Z \
            --out knowledge/cosmos/temporal-knowledge-test-v0.1.json

          test -f knowledge/cosmos/temporal-knowledge-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file =
            "knowledge/cosmos/temporal-knowledge-test-v0.1.json";
          const payload = JSON.parse(
            fs.readFileSync(file, "utf8")
          );

          if (payload.schema_version !== "0.1" ||
              payload.status !== "temporal_knowledge_resolved") {
            throw new Error(
              `Controlled Temporal Knowledge output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          if (Number(payload.temporal_state?.past_count) !== 1 ||
              Number(payload.temporal_state?.current_count) !== 1 ||
              Number(payload.temporal_state?.future_count) !== 1 ||
              Number(payload.temporal_state?.transition_count) !== 0 ||
              Number(payload.temporal_state?.unknown_count) !== 0) {
            throw new Error(
              "Controlled Temporal Knowledge temporal counts invalid"
            );
          }

          const future = payload.timeline?.future?.[0];

          if (!future ||
              future.temporal_contract?.future_is_not_current_fact !== true ||
              future.temporal_contract?.current_state_claimed !== false) {
            throw new Error(
              "Controlled future scenario was not safely separated from current fact"
            );
          }

          if (payload.safeguards?.mutates_graph !== false ||
              payload.safeguards?.deletes_historical_knowledge !== false ||
              payload.safeguards?.promotes_future_to_fact !== false ||
              payload.safeguards?.collapses_temporal_states !== false ||
              payload.safeguards?.preserves_source_lineage !== true ||
              payload.safeguards?.preserves_confidence !== true) {
            throw new Error(
              "Controlled Temporal Knowledge safeguards invalid"
            );
          }

          console.log("");
          console.log(
            "Real controlled Cosmos Temporal Knowledge v0.1 output created."
          );
          console.log(`Status:  ${payload.status}`);
          console.log(`Past:    ${payload.temporal_state.past_count}`);
          console.log(`Current: ${payload.temporal_state.current_count}`);
          console.log(`Future:  ${payload.temporal_state.future_count}`);
          console.log(`Output:  ${file}`);
          NODE

      - name: Commit Real Cosmos Temporal Knowledge v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_temporal_knowledge_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/temporal-knowledge-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos Temporal Knowledge controlled-output changes to commit."
          else
            git commit -m "Add Cosmos Temporal Knowledge v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos Temporal Knowledge v0.1 output committed."
          echo "Production Cosmos graph was not mutated."

      - name: Generate Real Cosmos First-Party Identity v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_first_party_identity_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating controlled Cosmos First-Party Identity v0.1 output."
          echo "This is a deterministic fixture only; it does not publish a live founder identity record."

          cat > /tmp/cosmos-first-party-identity-fixture-v0.1.json <<'JSON'
          {
            "schema_version": "0.1",
            "generated_at": "2026-08-27T12:00:00Z",
            "status": "first_party_identity_dataset",
            "entities": [
              {
                "entity_id": "entity_ptd_today",
                "canonical_name": "PTD Today",
                "aliases": ["ptdtoday.com"],
                "entity_type": "organization",
                "protected_identity": true
              },
              {
                "entity_id": "entity_cosmos",
                "canonical_name": "Cosmos",
                "aliases": ["PTD Cosmos"],
                "entity_type": "platform",
                "protected_identity": true
              },
              {
                "entity_id": "entity_fixture_founder",
                "canonical_name": "Fixture Founder",
                "aliases": [],
                "entity_type": "person",
                "protected_identity": true,
                "profiles": [
                  {
                    "profile_type": "linkedin",
                    "url": "https://www.linkedin.com/in/fixture-founder"
                  }
                ]
              },
              {
                "entity_id": "entity_external_claimant",
                "canonical_name": "External Claimant",
                "aliases": [],
                "entity_type": "person",
                "protected_identity": false
              }
            ],
            "relationships": [
              {
                "identity_relationship_id": "identity_ptd_founded_by_fixture",
                "subject": "PTD Today",
                "predicate": "founded_by",
                "object": "Fixture Founder",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/about",
                "source_label": "PTD Today official identity record",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-01-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_cosmos_created_by_fixture",
                "subject": "Cosmos",
                "predicate": "created_by",
                "object": "Fixture Founder",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/cosmos",
                "source_label": "Cosmos official identity record",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-08-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_ptd_develops_cosmos",
                "subject": "PTD Today",
                "predicate": "develops",
                "object": "Cosmos",
                "source_type": "first_party",
                "source_url": "https://www.ptdtoday.com/cosmos",
                "source_label": "PTD Today Cosmos relationship",
                "verification_status": "verified",
                "verified_at": "2026-08-27T12:00:00Z",
                "confidence_score": 100,
                "protected_identity": true,
                "valid_from": "2026-08-01T00:00:00Z",
                "valid_to": null
              },
              {
                "identity_relationship_id": "identity_external_conflicting_founder",
                "subject": "PTD Today",
                "predicate": "founded_by",
                "object": "External Claimant",
                "source_type": "external_secondary",
                "source_url": "https://example.com/conflicting-founder-claim",
                "source_label": "Controlled conflicting external claim",
                "verification_status": "unverified",
                "verified_at": null,
                "confidence_score": 45,
                "protected_identity": false,
                "valid_from": "2026-01-01T00:00:00Z",
                "valid_to": null
              }
            ]
          }
          JSON

          node scripts/cosmos_first_party_identity.cjs \
            --input /tmp/cosmos-first-party-identity-fixture-v0.1.json \
            --reference-time 2026-08-27T12:00:00Z \
            --question "Who founded PTD Today?" \
            --question "Who created Cosmos?" \
            --question "What is the relationship between PTD Today and Cosmos?" \
            --out knowledge/cosmos/first-party-identity-test-v0.1.json

          test -f knowledge/cosmos/first-party-identity-test-v0.1.json

          node <<'NODE'
          const fs = require("fs");

          const file =
            "knowledge/cosmos/first-party-identity-test-v0.1.json";
          const payload = JSON.parse(
            fs.readFileSync(file, "utf8")
          );

          if (payload.schema_version !== "0.1" ||
              payload.status !== "first_party_identity_resolved") {
            throw new Error(
              `Controlled First-Party Identity output invalid: ${payload.schema_version} / ${payload.status}`
            );
          }

          const founder = (payload.answers || []).find(
            row => row.predicate === "founded_by"
          );

          if (!founder ||
              founder.selected_relationship?.object !== "Fixture Founder" ||
              founder.selected_relationship?.source_type !== "first_party" ||
              founder.answer_contract?.first_party_not_universal_truth !== true ||
              founder.answer_contract?.conflict_disclosed !== true) {
            throw new Error(
              "Controlled founder identity resolution invalid"
            );
          }

          if ((payload.conflicts || []).length !== 1 ||
              payload.safeguards?.mutates_graph !== false ||
              payload.safeguards?.overwrites_conflicting_external_claims !== false ||
              payload.safeguards?.treats_first_party_as_universal_truth !== false ||
              payload.safeguards?.preserves_provenance !== true ||
              payload.safeguards?.preserves_conflicts !== true) {
            throw new Error(
              "Controlled First-Party Identity safeguards invalid"
            );
          }

          console.log("");
          console.log(
            "Real controlled Cosmos First-Party Identity v0.1 output created."
          );
          console.log(`Status:    ${payload.status}`);
          console.log(`Answers:   ${(payload.answers || []).length}`);
          console.log(`Conflicts: ${(payload.conflicts || []).length}`);
          console.log(`Output:    ${file}`);
          NODE

      - name: Commit Real Cosmos First-Party Identity v0.1 Controlled Output
        if: ${{ inputs.write_cosmos_first_party_identity_test == true }}
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add knowledge/cosmos/first-party-identity-test-v0.1.json

          if git diff --cached --quiet; then
            echo "No Cosmos First-Party Identity controlled-output changes to commit."
          else
            git commit -m "Add Cosmos First-Party Identity v0.1 controlled output"

            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Controlled Cosmos First-Party Identity v0.1 output committed."
          echo "No live founder identity record was published by this controlled test."

      - name: Generate Real Emergence Output
        if: ${{ inputs.write_emergence == true }}
        shell: bash
        run: |
          set -euo pipefail

          echo "Generating real Emergence output from the current committed Cosmos data."
          echo "No OpenAI/API generation is executed by this step."

          node scripts/cosmos_emergence_engine.cjs

          test -f knowledge/cosmos/emergence-current.json

          node <<'NODE'
          const fs = require("fs");

          const file = "knowledge/cosmos/emergence-current.json";
          const payload = JSON.parse(fs.readFileSync(file, "utf8"));

          if (payload.schema_version !== "0.1") {
            throw new Error(
              `Unexpected emergence schema_version: ${payload.schema_version}`
            );
          }

          if (
            !["ready", "no_emergence_above_threshold"].includes(payload.status)
          ) {
            throw new Error(
              `Invalid emergence status: ${payload.status}`
            );
          }

          if (!Array.isArray(payload.emergences)) {
            throw new Error("emergence-current.json: emergences[] missing");
          }

          console.log("");
          console.log("Real Emergence output created.");
          console.log(`Date:       ${payload.date_utc}`);
          console.log(`Status:     ${payload.status}`);
          console.log(`Emergences: ${payload.summary?.emergence_count ?? payload.emergences.length}`);
          console.log(`Output:     ${file}`);
          NODE

      - name: Commit Real Emergence Output
        if: ${{ inputs.write_emergence == true }}
        shell: bash
        run: |
          set -euo pipefail

          DATE_UTC="$(node -p "require('./knowledge/cosmos/emergence-current.json').date_utc")"
          HISTORY_FILE="knowledge/cosmos/emergence-history/${DATE_UTC}.json"

          if [ ! -f "$HISTORY_FILE" ]; then
            echo "ERROR: Expected emergence history file was not generated: $HISTORY_FILE"
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add \
            knowledge/cosmos/emergence-current.json \
            "$HISTORY_FILE"

          if git diff --cached --quiet; then
            echo "No Emergence changes to commit."
          else
            git commit -m "Update Cosmos emergence ${DATE_UTC}"
            # Another workflow may have updated main while this job was running.
            # Rebase this generated-output commit onto the latest remote tip before pushing.
            pushed=false
            for attempt in 1 2 3; do
              echo "Synchronizing with origin/${GITHUB_REF_NAME} before push (attempt ${attempt}/3)..."
              git fetch origin "${GITHUB_REF_NAME}"

              if ! git rebase "origin/${GITHUB_REF_NAME}"; then
                echo "ERROR: Could not rebase generated Cosmos output onto latest ${GITHUB_REF_NAME}."
                git rebase --abort || true
                exit 1
              fi

              if git push origin "HEAD:${GITHUB_REF_NAME}"; then
                pushed=true
                break
              fi

              echo "Remote changed again before push; retrying..."
              sleep $((attempt * 2))
            done

            if [ "$pushed" != "true" ]; then
              echo "ERROR: Could not push after 3 synchronization attempts."
              exit 1
            fi
          fi

          echo ""
          echo "Real Emergence output is now committed to the repository."
          echo "No OpenAI generation was executed."

      - name: Preflight Complete
        run: |
          echo "======================================================"
          echo "COSMOS PREFLIGHT PASSED"
          echo "No OpenAI generation was executed by this workflow."
          if [ "${{ inputs.write_emergence }}" = "true" ]; then
            echo "Real emergence-current.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_state }}" = "true" ]; then
            echo "Real cosmos-state-current.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_core_test }}" = "true" ]; then
            echo "Controlled core-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_attention_test }}" = "true" ]; then
            echo "Controlled attention-test-v0.2.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_consequence_test }}" = "true" ]; then
            echo "Controlled consequence-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_discovery_test }}" = "true" ]; then
            echo "Controlled discovery-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_acquisition_test }}" = "true" ]; then
            echo "Controlled acquisition-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_applicability_test }}" = "true" ]; then
            echo "Controlled applicability-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_decomposition_test }}" = "true" ]; then
            echo "Controlled decomposition-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_candidate_validation_test }}" = "true" ]; then
            echo "Controlled candidate-validation-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_evidence_strategy_test }}" = "true" ]; then
            echo "Controlled evidence-strategy-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_evidence_executor_test }}" = "true" ]; then
            echo "Controlled evidence-executor-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_evidence_validator_test }}" = "true" ]; then
            echo "Controlled evidence-validator-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_knowledge_admission_test }}" = "true" ]; then
            echo "Controlled knowledge-admission-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_graph_writer_test }}" = "true" ]; then
            echo "Controlled graph-writer-test-v0.1.json and graph-writer-report-v0.1.json were generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_graph_integrity_test }}" = "true" ]; then
            echo "Controlled graph-integrity-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_graph_reversal_test }}" = "true" ]; then
            echo "Controlled graph-reversal-test-v0.1.json, graph-reversal-report-v0.1.json and graph-reversal-integrity-test-v0.1.json were generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_causal_narrative_test }}" = "true" ]; then
            echo "Controlled causal-narrative-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_graph_evolution_test }}" = "true" ]; then
            echo "Controlled graph-evolution-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_temporal_knowledge_test }}" = "true" ]; then
            echo "Controlled temporal-knowledge-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_cosmos_first_party_identity_test }}" = "true" ]; then
            echo "Controlled first-party-identity-test-v0.1.json was generated and committed."
          fi
          if [ "${{ inputs.write_emergence }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_state }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_core_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_attention_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_consequence_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_discovery_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_acquisition_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_applicability_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_decomposition_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_candidate_validation_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_evidence_strategy_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_evidence_executor_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_evidence_validator_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_knowledge_admission_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_graph_writer_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_graph_integrity_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_graph_reversal_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_causal_narrative_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_graph_evolution_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_temporal_knowledge_test }}" != "true" ] && \
             [ "${{ inputs.write_cosmos_first_party_identity_test }}" != "true" ]; then
            echo "Isolated validation only; repository intelligence files were not changed."
          else
            echo "Upload the newly generated output file(s) to ChatGPT for inspection before production integration."
          fi
          echo "======================================================"
