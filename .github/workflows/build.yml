name: Build PTD Today (Node)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */2 * * *"   # every 2 hours

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run builder (fetch feeds, enrich thumbnails, write data & short pages)
        run: node scripts/build.mjs

      - name: Commit updated data and short pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data s
          git commit -m "chore: refresh news and shortlinks ($(date -u +'%Y-%m-%dT%H:%M:%SZ'))" || echo "No changes"
          git push